---
phase: 06-integration-testing-release
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - crates/legion-cli/src/main.rs
  - crates/legion-app/tests/phase6_acceptance.rs
files_forbidden:
  - xtask/src/golden_path_5.rs
  - xtask/src/release_pipeline.rs
  - .github/workflows/
  - crates/legion-desktop/src/theme.rs
  - crates/legion-desktop/src/view.rs
sequential_files: []
expected_artifacts:
  - path: "crates/legion-app/tests/phase6_acceptance.rs"
    provides: "Acceptance test proving the Phase 6 done-when criterion through AppComposition"
    required: true
autonomous: false
agents: [engineering-senior-developer]
requirements: [req_phase-6-integration-testing-release]
user_setup: []
verification_commands:
  - "cargo check -p legion-cli"
  - "cargo check -p legion-app --test phase6_acceptance"
  - "cargo test -p legion-app --test phase6_acceptance"

must_haves:
  truths:
    - "Acceptance test exercises open workspace → open file → edit_active_buffer → save_active_buffer → verify syntax highlights exist → terminal echo (skip if no PTY) → git commit cycle"
    - "CLI evidence gate uses identifier 'finish-phase6' (NOT 'gui-phase6' which already exists for a different phase)"
    - "Test uses AppComposition (not mocks) to prove real integration"
    - "Acceptance test follows GP-1 API patterns: AppComposition, edit_active_buffer(), save_active_buffer(), CommandDispatchIntent for git operations"
  artifacts:
    - path: "crates/legion-app/tests/phase6_acceptance.rs"
      provides: "Phase 6 acceptance integration test"
      min_lines: 100
      contains: "AppComposition"
    - path: "crates/legion-cli/src/main.rs"
      provides: "Updated CLI with finish-phase6 evidence gate"
      min_lines: 1600
      contains: "finish_phase6"
  key_links:
    - from: "crates/legion-app/tests/phase6_acceptance.rs"
      to: "crates/legion-app/src/lib.rs"
      via: "Test uses AppComposition for real integration"
      pattern: "AppComposition"
    - from: "crates/legion-cli/src/main.rs"
      to: "crates/legion-app/tests/phase6_acceptance.rs"
      via: "CLI validates that acceptance test artifact exists"
      pattern: "finish_phase6"
---

<objective>
Write the Phase 6 acceptance proof: an integration test that proves the "done when" criterion (open project, edit, run, commit) through AppComposition, and a CLI evidence gate that validates all Phase 6 artifacts.

Purpose: This is the capstone — it proves Phase 6 is complete by exercising the full user journey through AppComposition and validating all evidence artifacts exist.
Output: `crates/legion-app/tests/phase6_acceptance.rs`, updated `crates/legion-cli/src/main.rs`
</objective>

<context>
@FINISH-PLAN.md
@.legion/project/changes/chg_phase-6-integration-testing-release/06-CONTEXT.md
@.legion/project/changes/chg_phase-6-integration-testing-release/06-01-SUMMARY.md
@.legion/project/changes/chg_phase-6-integration-testing-release/06-02-SUMMARY.md

Relevant source files:
@crates/legion-app/src/lib.rs — AppComposition API: new(), open_workspace(), open_file(), edit_active_buffer(TextEdit), save_active_buffer()
@crates/legion-app/src/bin/golden_path_1.rs — GP-1 as reference: exact API call patterns for workspace open, file edit, LSP, terminal, git stage/commit
@crates/legion-cli/src/main.rs — existing evidence check gates. IMPORTANT: `gui-phase6` (EvidencePhase::GuiPhase6) ALREADY EXISTS for a DIFFERENT phase (GUI productization Phase 6 packaging). The new gate MUST use a different identifier: `finish-phase6` (EvidencePhase::FinishPhase6).
@crates/legion-app/tests/daily_editing_contracts.rs — existing legion-app integration test patterns
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: Senior Developer implementing acceptance proof and evidence gate
Task: Write Phase 6 acceptance test and CLI evidence gate. Covers req_phase-6-integration-testing-release.
Scope:
- Read targets: `crates/legion-app/src/lib.rs`, `crates/legion-app/src/bin/golden_path_1.rs`, `crates/legion-cli/src/main.rs`, `crates/legion-app/tests/daily_editing_contracts.rs`
- Write targets: `crates/legion-app/tests/phase6_acceptance.rs`, `crates/legion-cli/src/main.rs`
- Forbidden targets: xtask/src/golden_path_5.rs, xtask/src/release_pipeline.rs, .github/workflows/, crates/legion-desktop/src/theme.rs, crates/legion-desktop/src/view.rs
Allowed tools/actions:
- Read source files to understand AppComposition API and CLI evidence patterns
- Create new test file
- Modify legion-cli main.rs to add finish-phase6 evidence gate
- Run cargo check/test to verify
Forbidden actions:
- Do NOT modify GP-5 or release pipeline (owned by Plan 06-02)
- Do NOT modify GitHub workflows
- Do NOT use 'gui-phase6' identifier — it already exists for a different phase
- Do NOT add unplanned dependencies
- Do NOT modify view.rs or theme.rs
Implementation sequence:
1. Read `crates/legion-app/src/bin/golden_path_1.rs` completely to understand the exact API patterns:
   - How AppComposition is constructed
   - How open_workspace() is called with WorkspaceTrustState::Trusted
   - How files are opened (app.open_file() or equivalent)
   - How edits are applied (app.edit_active_buffer(TextEdit { ... }))
   - How saves work (app.save_active_buffer())
   - How git operations work (CommandDispatchIntent::RefreshGit, StageGitHunk, CommitGitChanges)
   - How terminal is launched and used
2. Read `crates/legion-cli/src/main.rs` to understand the EvidencePhase enum and check_*_evidence functions
3. VERIFY that `gui-phase6` / `GuiPhase6` already exists — grep for it — to confirm we must use a different name
4. Write phase6_acceptance.rs following GP-1 API patterns
5. Add FinishPhase6 variant and check_finish_phase6_evidence function to legion-cli
6. Run cargo check and cargo test to verify
Required interfaces/content structure:
- phase6_acceptance.rs uses the same imports as golden_path_1.rs: AppComposition, TextEdit, TextPosition, TextRange, CommandDispatchIntent, etc.
- Tests create a temp fixture dir, git init it, open workspace as Trusted, and exercise the journey
- Method names to use (from GP-1, NOT invented):
  - `AppComposition::new()` or equivalent constructor
  - `app.open_workspace(path, WorkspaceTrustState::Trusted)` or equivalent
  - `app.edit_active_buffer(TextEdit { ... })` — NOT apply_edit()
  - `app.save_active_buffer()` — NOT save()
  - For git: dispatch CommandDispatchIntent variants via the app's intent mechanism
- CLI evidence gate:
  - Add `FinishPhase6` variant to `EvidencePhase` enum
  - Add `"finish-phase6"` to argument parsing
  - Add `check_finish_phase6_evidence()` function checking:
    - GP-5 report: target/golden-path/gp5_report.toml exists
    - Rendering tests: crates/legion-desktop/tests/user_journey_rendering.rs exists
    - Release workflow: .github/workflows/legion-release.yml exists
    - Acceptance test: crates/legion-app/tests/phase6_acceptance.rs exists
Edge/error cases:
- Temp fixture dir creation failure: skip test with informative message
- PTY unavailable (CI environments): terminal step produces SKIP, not failure
- Git not available: document but don't block
- If 06-01-SUMMARY.md or 06-02-SUMMARY.md does not exist: emit BLOCKED (wave 1 plans must complete first)
Verification criteria:
- cargo check -p legion-cli exits 0
- cargo check -p legion-app --test phase6_acceptance exits 0
- cargo test -p legion-app --test phase6_acceptance exits 0
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- 06-01-SUMMARY.md does not exist or shows incomplete/failed status — wave 1 must complete first
- 06-02-SUMMARY.md does not exist or shows incomplete/failed status — wave 1 must complete first
- AppComposition API has changed from what GP-1 uses (open_workspace, edit_active_buffer, save_active_buffer methods missing or different)
- The EvidencePhase enum in legion-cli has changed from the pattern described here
- cargo check fails with errors that cannot be resolved by adjusting the test/CLI code
- The `gui-phase6` / `GuiPhase6` identifier conflicts with the new gate in unexpected ways
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase 06-CONTEXT.md, Plans 06-01 and 06-02 SUMMARYs, and any SUMMARY for this plan.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run completed task verification commands before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 6 acceptance integration test</name>
  <files>crates/legion-app/tests/phase6_acceptance.rs</files>
  <action>
Read `crates/legion-app/src/bin/golden_path_1.rs` COMPLETELY to extract the exact API patterns. Pay special attention to:
- Lines 24-46: import list (AppComposition, TextEdit, TextPosition, TextRange, CommandDispatchIntent, etc.)
- Lines around s1: how workspace is created and opened with trust state
- Lines around s3: how edits are applied with edit_active_buffer()
- Lines around s5: how terminal is launched and polled
- Lines around s6: how git stage/commit is dispatched

Also read `crates/legion-app/tests/daily_editing_contracts.rs` for integration test conventions in the legion-app test suite.

Create `crates/legion-app/tests/phase6_acceptance.rs` with tests:

1. `acceptance_open_workspace_and_edit` — Create temp dir with a Rust file (e.g., write "fn main() {}" to main.rs). Git init. Open workspace via AppComposition as Trusted. Open the file. Apply edit via edit_active_buffer() (add a comment). Save via save_active_buffer(). Verify file on disk contains the edit.

2. `acceptance_syntax_highlighting_present` — Open a Rust file via AppComposition. Use the highlight/language API (read GP-1 s3 or s4 for exact call) to verify non-empty syntax captures are returned.

3. `acceptance_terminal_available` — Attempt to create a terminal session (read GP-1 s5 for exact API). If PTY available, run `echo gp5-acceptance` and poll for output (use GP-1's polling loop pattern with reasonable timeout — study `daily_editing_contracts.rs` for how existing tests handle async polling in `#[test]` context). If PTY not available, log skip reason and mark as skipped (not failed).

4. `acceptance_git_commit_cycle` — Open git-initialized fixture workspace. Edit and save a file. Dispatch RefreshGit intent (with polling wait for git state to refresh — follow GP-1 s6 timing pattern). Dispatch StageGitHunk. Dispatch CommitGitChanges with message. Poll for clean status after commit. (Follow GP-1 s6 pattern exactly, adapting any thread::sleep or polling loops from the binary context to the `#[test]` context.)

5. `acceptance_full_journey` — Chain steps 1-4 into a single sequential test proving the complete user journey in order.

IMPORTANT: Use ONLY method names that exist on AppComposition as shown in GP-1:
- edit_active_buffer() — NOT apply_edit()
- save_active_buffer() — NOT save()
- Follow GP-1's exact intent dispatch mechanism for git operations

> verification: cargo check -p legion-app --test phase6_acceptance
> verification: cargo test -p legion-app --test phase6_acceptance
  </action>
  <verify>
cargo check -p legion-app --test phase6_acceptance
cargo test -p legion-app --test phase6_acceptance
  </verify>
  <done>Phase 6 acceptance test written and passing, proving the full user journey through AppComposition.</done>
</task>

<task type="auto">
  <name>Task 2: Add finish-phase6 evidence gate to legion-cli</name>
  <files>crates/legion-cli/src/main.rs</files>
  <action>
Read `crates/legion-cli/src/main.rs` to understand the evidence check pattern:
1. Find the `EvidencePhase` enum — grep for "enum EvidencePhase"
2. Find existing phase variants (Phase0, Phase3, GuiPhase6, GuiPhase7, GuiPhase8, Phase8)
3. Find the argument parser that maps string identifiers to enum variants
4. Find an existing check_*_evidence function to use as template (e.g., check_gui_phase6_evidence)

VERIFY: grep for "GuiPhase6" to confirm it already exists and uses the "gui-phase6" string identifier. This confirms we MUST use a different identifier.

Add `FinishPhase6` variant and evidence gate:
1. Add `FinishPhase6` to the `EvidencePhase` enum (clap's `#[derive(ValueEnum)]` automatically registers `finish-phase6` as the CLI string — no manual parser modification needed)
2. Add `EvidencePhase::FinishPhase6 => check_finish_phase6_evidence(...)` match arm in `run_evidence_check()` dispatch
3. Add `check_finish_phase6_evidence(workspace_root: &Path)` function checking:
   - `crates/legion-desktop/tests/user_journey_rendering.rs` exists (Plan 06-01 output)
   - `crates/legion-app/src/bin/golden_path_5.rs` exists (Plan 06-02 output)
   - `.github/workflows/legion-release.yml` exists (Plan 06-02 output)
   - `crates/legion-app/tests/phase6_acceptance.rs` exists (Plan 06-03 output)
   - `xtask/src/golden_path_5.rs` exists (Plan 06-02 output)
4. Wire the check into the main evidence dispatch match

Follow the exact pattern of existing evidence gates for the function structure and output format.

> verification: cargo check -p legion-cli
> verification: grep -q "finish_phase6\|FinishPhase6" crates/legion-cli/src/main.rs
  </action>
  <verify>
cargo check -p legion-cli
grep -q "finish_phase6\|FinishPhase6" crates/legion-cli/src/main.rs
  </verify>
  <done>finish-phase6 evidence gate added to legion-cli, validating all Phase 6 acceptance artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Run full acceptance verification</name>
  <files>read-only verification</files>
  <action>
Run comprehensive verification:

1. `cargo test -p legion-app --test phase6_acceptance` — acceptance tests pass
2. `cargo test -p legion-desktop --test user_journey_rendering` — rendering tests pass (Plan 06-01)
3. `cargo check -p xtask` — GP-5 compiles (Plan 06-02)
4. `cargo check -p legion-cli` — evidence gate compiles
5. `cargo test -p legion-desktop --lib` — no regressions
6. `cargo test -p legion-app --lib` — no regressions (run subset if full suite is slow)

Document pre-existing failures separately. All new code must pass.

> verification: cargo test -p legion-app --test phase6_acceptance
> verification: cargo check -p legion-cli
> verification: cargo check -p xtask
  </action>
  <verify>
cargo test -p legion-app --test phase6_acceptance
cargo check -p legion-cli
cargo check -p xtask
  </verify>
  <done>Full Phase 6 acceptance verification passes — all new code compiles and tests green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p legion-app --test phase6_acceptance` passes all tests
- [ ] `cargo check -p legion-cli` exits 0
- [ ] finish-phase6 evidence gate exists (NOT gui-phase6)
- [ ] Acceptance test uses AppComposition with correct method names (edit_active_buffer, save_active_buffer)
- [ ] No regressions in existing app or desktop tests
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- phase6_acceptance.rs has 5 integration tests proving the full user journey
- legion-cli evidence check supports finish-phase6 identifier
- Acceptance test uses AppComposition directly (not mocks)
- The "done when" criterion is provably met: open project, edit, run (terminal), commit
</success_criteria>

<output>
After completion, create `.legion/project/changes/chg_phase-6-integration-testing-release/06-03-SUMMARY.md`

Update FINISH-PLAN.md Phase 6 as complete.
Update state: Phase 6 complete.
</output>
