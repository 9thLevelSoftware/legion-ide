---
phase: 06-integration-testing-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - xtask/src/golden_path_5.rs
  - xtask/src/lib.rs
  - xtask/src/main.rs
  - crates/legion-app/src/bin/golden_path_5.rs
  - .github/workflows/legion-release.yml
files_forbidden:
  - crates/legion-desktop/src/
  - crates/legion-desktop/tests/
  - crates/legion-app/src/lib.rs
  - crates/legion-app/src/updater.rs
  - crates/legion-cli/
  - xtask/src/release_pipeline.rs
  - xtask/src/signing.rs
sequential_files: []
expected_artifacts:
  - path: "xtask/src/golden_path_5.rs"
    provides: "GP-5 orchestrator: spawns golden_path_5 binary as subprocess"
    required: true
  - path: "crates/legion-app/src/bin/golden_path_5.rs"
    provides: "GP-5 binary: exercises full user journey through AppComposition (headless, no GUI)"
    required: true
  - path: ".github/workflows/legion-release.yml"
    provides: "Tag-triggered release workflow producing signed artifacts for 3 platforms"
    required: true
autonomous: false
agents: [engineering-infrastructure-devops]
requirements: [req_phase-6-integration-testing-release]
user_setup: []
verification_commands:
  - "cargo check -p xtask"
  - "cargo check -p legion-app --bin golden_path_5"
  - "test -f .github/workflows/legion-release.yml"

must_haves:
  truths:
    - "GP-5 binary uses AppComposition (like GP-1-4) — NOT by launching legion-desktop GUI binary"
    - "GP-5 exercises: open workspace → open file → edit → save → verify syntax highlights → terminal echo → git commit"
    - "GP-5 writes evidence TOML to target/golden-path/gp5_report.toml"
    - "Release workflow triggers on v* tags and manual dispatch, 3-OS matrix, runs cargo build --release then xtask release-pipeline --from-artifacts"
  artifacts:
    - path: "xtask/src/golden_path_5.rs"
      provides: "GP-5 xtask orchestrator module"
      min_lines: 40
      contains: "run_golden_path_5"
    - path: "crates/legion-app/src/bin/golden_path_5.rs"
      provides: "GP-5 binary using AppComposition"
      min_lines: 100
      contains: "AppComposition"
    - path: ".github/workflows/legion-release.yml"
      provides: "GitHub Actions release workflow"
      min_lines: 30
      contains: "release-pipeline"
  key_links:
    - from: "xtask/src/golden_path_5.rs"
      to: "crates/legion-app/src/bin/golden_path_5.rs"
      via: "xtask spawns the GP-5 binary as subprocess via cargo run"
      pattern: "golden_path_5"
    - from: ".github/workflows/legion-release.yml"
      to: "xtask/src/release_pipeline.rs"
      via: "Workflow invokes xtask release-pipeline --from-artifacts"
      pattern: "release-pipeline"
---

<objective>
Create GP-5 golden-path smoke test (full user journey through AppComposition) and a tag-triggered release workflow.

Purpose: GP-5 serves as the acceptance smoke test for the IDE's core user journey. The release workflow enables producing signed release artifacts for distribution. GP-5 uses AppComposition in-process (like GP-1-4), NOT by launching the desktop GUI binary.
Output: GP-5 binary + xtask orchestrator, release workflow
</objective>

<context>
@FINISH-PLAN.md
@.legion/project/changes/chg_phase-6-integration-testing-release/06-CONTEXT.md

Relevant source files:
@xtask/src/golden_path.rs — GP-1 xtask orchestrator: GoldenPath1Options struct, run_golden_path_1() function, subprocess spawn via `cargo run -p legion-app --bin golden_path_1 --no-default-features`
@crates/legion-app/src/bin/golden_path_1.rs — GP-1 binary: uses AppComposition directly (NOT desktop GUI), headless, StepStatus/StepRecord pattern, evidence TOML output, steps s1-s7
@xtask/src/main.rs — xtask subcommand registration pattern for GP-1-4
@xtask/src/lib.rs — xtask module export pattern
@.github/workflows/legion-gates.yml — CI workflow conventions (3-OS matrix, Rust toolchain, caching, Linux deps)
@.github/workflows/legion-preview.yml — preview build workflow pattern
@xtask/src/release_pipeline.rs — release pipeline API: plan_release_pipeline(workspace_root, config, channel, dry_run, artifacts_dir: Option<&Path>). Uses --dry-run flag and --from-artifacts <dir>, NOT --mode build.
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: Infrastructure/DevOps Engineer implementing GP-5 and release workflow
Task: Create GP-5 golden-path binary + xtask orchestrator, and tag-triggered release workflow. Covers req_phase-6-integration-testing-release.
Scope:
- Read targets: `xtask/src/golden_path.rs`, `crates/legion-app/src/bin/golden_path_1.rs`, `xtask/src/main.rs`, `xtask/src/lib.rs`, `.github/workflows/legion-gates.yml`, `.github/workflows/legion-preview.yml`, `xtask/src/release_pipeline.rs`
- Write targets: `xtask/src/golden_path_5.rs`, `xtask/src/lib.rs`, `xtask/src/main.rs`, `crates/legion-app/src/bin/golden_path_5.rs`, `.github/workflows/legion-release.yml`
- Forbidden targets: crates/legion-desktop/src/, crates/legion-desktop/tests/, crates/legion-app/src/lib.rs, crates/legion-app/src/updater.rs, crates/legion-cli/, xtask/src/release_pipeline.rs (do NOT modify the existing release pipeline), xtask/src/signing.rs
Allowed tools/actions:
- Read source files to understand existing patterns
- Create new files (GP-5 orchestrator, GP-5 binary, release workflow)
- Modify xtask lib.rs (add module export) and main.rs (add subcommand)
- Run cargo check to verify
Forbidden actions:
- Do NOT modify release_pipeline.rs — the existing API already supports --from-artifacts for real builds
- Do NOT launch legion-desktop as a GUI process — GP-5 must use AppComposition in-process like GP-1
- Do NOT add unplanned dependencies
- Do NOT change signing infrastructure
Implementation sequence:
1. Read xtask/src/golden_path.rs to copy the exact GoldenPath1Options / run_golden_path_1 / subprocess spawn pattern
2. Read crates/legion-app/src/bin/golden_path_1.rs to copy the exact StepStatus / StepRecord / Args / evidence TOML pattern
3. Read xtask/src/main.rs to find where GP-1-4 subcommands are registered and follow the same pattern
4. Create xtask/src/golden_path_5.rs mirroring golden_path.rs exactly
5. Create crates/legion-app/src/bin/golden_path_5.rs with 7 steps using AppComposition
6. Add `pub mod golden_path_5;` to xtask/src/lib.rs alongside existing golden_path exports
7. Add `GoldenPath5` subcommand and `run_golden_path_5_command` to xtask/src/main.rs following GP-1-4 pattern
8. Read .github/workflows/legion-gates.yml and legion-preview.yml for CI conventions
9. Create .github/workflows/legion-release.yml
10. Run cargo check -p xtask and cargo check -p legion-app --bin golden_path_5
Required interfaces/content structure:
- GP-5 xtask orchestrator (`xtask/src/golden_path_5.rs`):
  - `pub struct GoldenPath5Options { pub fixture_dir: String, pub out_dir: String, pub record_evidence: Option<String> }`
  - `pub fn run_golden_path_5(workspace_root: &Path, opts: &GoldenPath5Options) -> i32`
  - Spawns subprocess: `cargo run -p legion-app --no-default-features --bin golden_path_5 -- --fixture-dir {dir} --out-dir {dir}`
- GP-5 binary (`crates/legion-app/src/bin/golden_path_5.rs`):
  - Uses `legion_app::AppComposition` directly (import pattern from golden_path_1.rs)
  - Steps:
    - s1 `copy-fixture`: copy fixture to temp dir, git init, open as Trusted workspace via AppComposition
    - s2 `open-file`: open main.rs from fixture, assert buffer loaded
    - s3 `edit-and-save`: call `app.edit_active_buffer(edit)` then `app.save_active_buffer()`, verify file changed on disk
    - s4 `syntax-check`: use `legion_index::TreeSitterParser::new().highlight_captures_from_text(&LanguageId::from("rust"), &text)` (import from `legion-index` crate, NOT an AppComposition method — this step is new to GP-5 and has no GP-1 equivalent). Assert non-empty captures.
    - s5 `terminal-echo`: if PTY available, launch terminal, run `echo gp5-ok`, verify output (SKIP if no PTY, same as GP-1 s5)
    - s6 `git-commit`: refresh git, stage hunk, commit changes, verify clean status (same pattern as GP-1 s6)
    - s7 `evidence`: write `target/golden-path/gp5_report.toml` with StepRecord for each step
  - StepStatus enum: Passed, Failed, Skipped (same as GP-1)
  - Evidence TOML format: same as GP-1's gp1_report.toml
- Release workflow (`.github/workflows/legion-release.yml`):
  - Triggers: push tags `v*`, workflow_dispatch with channel input (preview/stable)
  - Jobs: build-test (cargo fmt check, cargo check, cargo test, cargo clippy), release (cargo build --release -p legion-desktop, run packaging script, xtask release-pipeline --from-artifacts target/release/ --channel {channel}, golden-path-5), upload (upload artifacts)
  - 3-OS matrix (ubuntu-latest, windows-latest, macos-latest)
  - Same Rust toolchain, caching, and Linux dependencies as legion-gates.yml
  - Timeout: 120 minutes
Edge/error cases:
- GP-5 fixture directory not found: exit 2 (same as GP-1)
- PTY unavailable: s5 terminal-echo produces StepStatus::Skipped (not Failed)
- Release workflow on manual dispatch without tag: builds preview channel
- Signing key not available in CI: produces unsigned-beta artifacts (existing release pipeline fallback)
Verification criteria:
- cargo check -p xtask exits 0
- cargo check -p legion-app --bin golden_path_5 exits 0
- .github/workflows/legion-release.yml exists
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- GP-1's binary imports or AppComposition API have changed since this plan was written and the documented API patterns don't match
- xtask/src/main.rs subcommand registration pattern has changed from what GP-1-4 use
- The release pipeline `--from-artifacts` flag does not exist or works differently than documented
- cargo check fails on xtask or the GP-5 binary with unresolvable errors
- The CI workflow YAML conventions in legion-gates.yml have changed significantly from what's documented here
- `scripts/package-windows.ps1` or `scripts/package-preview.sh` do not exist — the release workflow references them
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase 06-CONTEXT.md, and any SUMMARY artifact for this plan.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run completed task verification commands before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Create GP-5 golden-path binary and xtask orchestrator</name>
  <files>xtask/src/golden_path_5.rs, xtask/src/lib.rs, xtask/src/main.rs, crates/legion-app/src/bin/golden_path_5.rs</files>
  <action>
Read the following files completely to understand the exact patterns:
1. `xtask/src/golden_path.rs` — full file, GP-1 orchestrator pattern
2. `crates/legion-app/src/bin/golden_path_1.rs` — full file, GP-1 binary pattern (imports, StepStatus, StepRecord, Args, step functions, evidence TOML writer)
3. `xtask/src/lib.rs` — module export lines
4. `xtask/src/main.rs` — GP-1-4 subcommand registration and run_golden_path_N_command functions

Create `xtask/src/golden_path_5.rs`:
- Copy the exact structure of `golden_path.rs` (GP-1 orchestrator)
- Rename types: GoldenPath5Options, run_golden_path_5
- Binary name: golden_path_5
- Default fixture_dir: "fixtures/gp1-rust" (reuse GP-1's fixture)
- Default out_dir: "target/golden-path"

Create `crates/legion-app/src/bin/golden_path_5.rs`:
- Copy the exact structure of golden_path_1.rs
- Same imports (AppComposition, TextEdit, TextPosition, etc.)
- 7 steps using AppComposition:
  - s1 copy-fixture: same as GP-1 s1
  - s2 open-file: same as GP-1 (open main.rs from fixture)
  - s3 edit-and-save: use app.edit_active_buffer() + app.save_active_buffer(), verify disk file changed
  - s4 syntax-check: use `legion_index::TreeSitterParser::new().highlight_captures_from_text(&LanguageId::from("rust"), &text)` — import from legion-index, NOT AppComposition (new to GP-5, no GP-1 equivalent)
  - s5 terminal-echo: same as GP-1 s5 (launch terminal, run echo, SKIP if no PTY)
  - s6 git-commit: same as GP-1 s6 (stage + commit)
  - s7 evidence: write gp5_report.toml

Register in xtask:
- Add `pub mod golden_path_5;` to lib.rs (after golden_path_4)
- Add GoldenPath5 subcommand to main.rs enum and run_golden_path_5_command function (copy GP-1-4 pattern exactly)

> verification: cargo check -p xtask
> verification: cargo check -p legion-app --bin golden_path_5
  </action>
  <verify>
cargo check -p xtask
cargo check -p legion-app --bin golden_path_5
  </verify>
  <done>GP-5 golden-path binary and xtask orchestrator created, compiling cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create tag-triggered release workflow</name>
  <files>.github/workflows/legion-release.yml</files>
  <action>
Read `.github/workflows/legion-gates.yml` and `.github/workflows/legion-preview.yml` completely to understand:
- OS matrix configuration (ubuntu-latest, windows-latest, macos-latest)
- Rust toolchain setup steps
- Caching configuration
- Linux dependency installation (libxkbcommon, libwayland, etc.)
- Timeout settings

Create `.github/workflows/legion-release.yml`:

The workflow has two trigger modes:
1. Tag push (`v*`) — auto-detect channel from tag (v*-preview → preview, v* → stable)
2. Manual dispatch — user selects channel (preview/stable)

Jobs:

```
job 1: build-and-test
  - 3-OS matrix
  - Install Rust toolchain (same as legion-gates.yml)
  - cargo fmt --check
  - cargo check --workspace
  - cargo test --workspace
  - cargo clippy --workspace

job 2: release (needs: build-and-test)
  - 3-OS matrix
  - cargo build --release -p legion-desktop
  - Run platform packaging script (verify scripts exist first — stop gate if missing):
    - Windows: powershell scripts/package-windows.ps1
    - Linux/macOS: bash scripts/package-preview.sh
  - Run: cargo run -p xtask -- release-pipeline --from-artifacts target/release/ --channel $CHANNEL
    (Note: use --from-artifacts, NOT --mode build. The existing API accepts --from-artifacts <dir>.)
  - Run xtask golden-path-5 (GP-5 verification)
  - Upload artifacts to GitHub release (if tag-triggered)
```

Use the same Linux CI dependencies, caching, and timeout as legion-gates.yml.

> verification: test -f .github/workflows/legion-release.yml
> verification: grep -q "from-artifacts" .github/workflows/legion-release.yml
  </action>
  <verify>
test -f .github/workflows/legion-release.yml
grep -q "from-artifacts" .github/workflows/legion-release.yml
  </verify>
  <done>Tag-triggered release workflow created following existing CI conventions.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all new code compiles</name>
  <files>read-only verification</files>
  <action>
Run full verification:
1. `cargo check -p xtask` — xtask compiles with GP-5 module
2. `cargo check -p legion-app --bin golden_path_5` — GP-5 binary compiles
3. Verify `.github/workflows/legion-release.yml` exists and contains expected content

> verification: cargo check -p xtask
> verification: cargo check -p legion-app --bin golden_path_5
> verification: test -f .github/workflows/legion-release.yml
  </action>
  <verify>
cargo check -p xtask
cargo check -p legion-app --bin golden_path_5
test -f .github/workflows/legion-release.yml
  </verify>
  <done>All GP-5 and release workflow code compiles; artifacts verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p xtask` exits 0
- [ ] `cargo check -p legion-app --bin golden_path_5` exits 0
- [ ] `.github/workflows/legion-release.yml` exists with v* tag trigger and --from-artifacts usage
- [ ] GP-5 uses AppComposition (NOT desktop GUI launch)
- [ ] GP-5 follows GP-1-4 subprocess pattern exactly
- [ ] Release workflow uses existing release-pipeline --from-artifacts (NOT invented --mode build)
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- GP-5 binary compiles and exercises the full user journey through AppComposition
- GP-5 xtask orchestrator registered and callable via `cargo run -p xtask -- golden-path-5`
- Release workflow triggers on v* tags and produces 3-OS artifacts using --from-artifacts
- All code follows existing conventions exactly
</success_criteria>

<output>
After completion, create `.legion/project/changes/chg_phase-6-integration-testing-release/06-02-SUMMARY.md`

Update state: Plan 06-02 complete, wave 1 progress.
</output>
