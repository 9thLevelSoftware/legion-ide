---
phase: 06-integration-testing-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/legion-desktop/tests/user_journey_rendering.rs
files_forbidden:
  - crates/legion-desktop/src/
  - xtask/
  - .github/
  - scripts/
  - crates/legion-app/src/bin/
  - crates/legion-cli/
sequential_files: []
expected_artifacts:
  - path: "crates/legion-desktop/tests/user_journey_rendering.rs"
    provides: "View-model integration tests proving the full user journey projects correctly through DesktopProjectionViewModel"
    required: true
autonomous: false
agents: [engineering-senior-developer]
requirements: [req_phase-6-integration-testing-release]
user_setup: []
verification_commands:
  - "cargo check -p legion-desktop --tests"
  - "cargo test -p legion-desktop --test user_journey_rendering"

must_haves:
  truths:
    - "Tests verify syntax highlighting produces DesktopCodeHighlightSpan entries with correct ViewportSemanticTokenKind values (Keyword, String, Comment) at expected column ranges"
    - "Tests verify diagnostic problems from the language tooling projection appear in model.language_rows with appropriate severity text"
    - "Tests verify terminal session content appears in model.terminal_rows"
    - "Tests verify tab state (active, dirty, clean) is correctly represented in model.tab_rows"
    - "Tests verify the editor-to-terminal-to-git journey produces correct view model state at each stage"
  artifacts:
    - path: "crates/legion-desktop/tests/user_journey_rendering.rs"
      provides: "View-model integration tests for full user journey"
      min_lines: 150
      contains: "#[test]"
  key_links:
    - from: "crates/legion-desktop/tests/user_journey_rendering.rs"
      to: "crates/legion-desktop/src/view.rs"
      via: "Tests call DesktopProjectionViewModel::from_snapshot()"
      pattern: "DesktopProjectionViewModel::from_snapshot"
---

<objective>
Write view-model integration tests that prove the full user journey projects correctly through `DesktopProjectionViewModel`: syntax highlighting spans, diagnostic rows, terminal rows, tab state, and git status.

Purpose: GP-1 proves the app-layer user journey works through AppComposition. This plan proves the desktop view-model layer correctly projects that state into testable display models. Note: actual egui rendering (Color32 values, underline geometry) is private and untestable from integration tests. We test the projection pipeline that feeds the renderer.
Output: `crates/legion-desktop/tests/user_journey_rendering.rs`
</objective>

<context>
@FINISH-PLAN.md
@.legion/project/changes/chg_phase-6-integration-testing-release/06-CONTEXT.md

Relevant source files:
@crates/legion-desktop/src/view.rs — DesktopProjectionViewModel (line 487), DesktopCodeLineViewModel (line 218), DesktopCodeHighlightSpan (line 229), from_snapshot() (line 661)
@crates/legion-desktop/tests/projection_rendering.rs — CANONICAL test pattern: construct ShellProjectionSnapshot, call DesktopProjectionViewModel::from_snapshot(), assert on view model fields
@crates/legion-desktop/tests/common/mod.rs — contains only strip_comments_and_strings(); fixtures are defined inline per test file
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: Senior Developer implementing view-model integration tests
Task: Write integration tests proving the full user journey projects correctly through DesktopProjectionViewModel. Covers req_phase-6-integration-testing-release.
Scope:
- Read targets: `crates/legion-desktop/src/view.rs` (DesktopProjectionViewModel, DesktopCodeLineViewModel, DesktopCodeHighlightSpan types), `crates/legion-desktop/tests/projection_rendering.rs` (canonical test patterns — populated_snapshot(), highlighted_snapshot(), assertion conventions), `crates/legion-desktop/tests/common/mod.rs`
- Write targets: `crates/legion-desktop/tests/user_journey_rendering.rs`
- Forbidden targets: crates/legion-desktop/src/ (do NOT modify view.rs or theme.rs), xtask/, .github/, scripts/, crates/legion-app/src/bin/, crates/legion-cli/
Allowed tools/actions:
- Read source files to understand view model types and test patterns
- Write new test file
- Run cargo check/test to verify
Forbidden actions:
- Do not modify view.rs, theme.rs, or any source file
- Do not modify existing tests
- Do not add unplanned dependencies
- Do not attempt to call private render_* functions — they require egui::Ui and are not accessible from integration tests
- Do not attempt to assert on Color32 values — theme tokens are pub(crate) and inaccessible from integration tests
Implementation sequence:
1. Read `crates/legion-desktop/tests/projection_rendering.rs` lines 1-200 to study imports, helper functions (coord(), range(), fingerprint()), and snapshot builders (populated_snapshot(), highlighted_snapshot())
2. Read `crates/legion-desktop/src/view.rs` lines 218-240 (DesktopCodeLineViewModel, DesktopCodeHighlightSpan) and lines 487-600 (DesktopProjectionViewModel fields)
3. Read `crates/legion-desktop/tests/projection_rendering.rs` lines 1400-1500 to study how highlighted_snapshot() tests assert on DesktopCodeHighlightSpan.kind values
4. Write user_journey_rendering.rs following the same import pattern and fixture style
5. Run cargo check -p legion-desktop --tests and cargo test -p legion-desktop --test user_journey_rendering
Required interfaces/content structure:
- Import from `legion_desktop::view::{DesktopProjectionViewModel, DesktopCodeHighlightSpan, DesktopCodeLineViewModel}`
- Import from `legion_protocol::{ViewportSemanticTokenKind, ViewportSemanticTokenOverlay, ...}` — same set as projection_rendering.rs
- Import from `legion_ui::{Shell, ShellProjectionSnapshot, ...}` — same set as projection_rendering.rs
- Build ShellProjectionSnapshot instances with known data inline (same pattern as populated_snapshot() and highlighted_snapshot())
- Call `DesktopProjectionViewModel::from_snapshot(&snapshot)` to get the view model
- Assert on: model.active_buffer_code_lines[i].highlights[j].kind == ViewportSemanticTokenKind::Keyword
- Assert on: model.tab_rows contents for active/dirty/clean state
- Assert on: model.terminal_rows contents for terminal session output
- Assert on: model.language_rows contents for diagnostic problem text
- Assert on: model.git_rows contents for git status
Edge/error cases:
- Empty shell (Shell::empty("Manual")) — should produce empty view model without panics
- Snapshot with no active buffer — tab_rows may be empty
- Snapshot with no terminal session — terminal_rows should be empty or contain "no session"
- Snapshot with no git repo — git_rows should reflect no-repo state
Verification criteria:
- cargo check -p legion-desktop --tests exits 0
- cargo test -p legion-desktop --test user_journey_rendering exits 0
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `DesktopProjectionViewModel::from_snapshot` is not public or has a different signature than expected
- `DesktopCodeHighlightSpan` or `ViewportSemanticTokenKind` are not importable from integration tests
- The `ShellProjectionSnapshot` type cannot be constructed with the fields needed for the test scenarios
- `projection_rendering.rs` uses a fundamentally different approach than described here
- cargo check fails on the new test file with errors that cannot be resolved by adjusting imports or field access
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase 06-CONTEXT.md, and any SUMMARY artifact for this plan.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run completed task verification commands before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Study projection_rendering.rs patterns and view model types</name>
  <files>read-only: crates/legion-desktop/tests/projection_rendering.rs, crates/legion-desktop/src/view.rs</files>
  <action>
Read the following to understand the exact test pattern:

1. `crates/legion-desktop/tests/projection_rendering.rs` lines 1-60 — imports and helper functions
2. `crates/legion-desktop/tests/projection_rendering.rs` lines 154-300 — `populated_snapshot()` builder function
3. `crates/legion-desktop/tests/projection_rendering.rs` lines 496-640 — `highlighted_snapshot()` builder function
4. `crates/legion-desktop/tests/projection_rendering.rs` lines 1420-1500 — tests asserting on DesktopCodeHighlightSpan.kind values
5. `crates/legion-desktop/src/view.rs` lines 218-240 — DesktopCodeLineViewModel and DesktopCodeHighlightSpan struct definitions
6. `crates/legion-desktop/src/view.rs` lines 487-580 — DesktopProjectionViewModel field list

Document:
- The exact import set needed
- How ShellProjectionSnapshot is constructed (which fields must be populated)
- How highlighted_snapshot() adds ViewportSemanticTokenOverlay entries
- How assertions on model.active_buffer_code_lines[i].highlights work
- The string format of tab_rows, terminal_rows, language_rows, git_rows

> verification: test -f crates/legion-desktop/tests/projection_rendering.rs
> verification: grep -q "from_snapshot" crates/legion-desktop/tests/projection_rendering.rs
  </action>
  <verify>
test -f crates/legion-desktop/tests/projection_rendering.rs
grep -q "from_snapshot" crates/legion-desktop/tests/projection_rendering.rs
  </verify>
  <done>Projection rendering test patterns documented; view model types and assertion conventions identified.</done>
</task>

<task type="auto">
  <name>Task 2: Write user journey view-model integration tests</name>
  <files>crates/legion-desktop/tests/user_journey_rendering.rs</files>
  <action>
Create `crates/legion-desktop/tests/user_journey_rendering.rs` following the EXACT conventions from projection_rendering.rs.

Use the same import pattern from projection_rendering.rs lines 1-40. Define inline helper functions and snapshot builders (do NOT put them in common/mod.rs — follow the inline-per-file convention).

Tests to write:

1. `syntax_highlights_map_to_correct_token_kinds` — Build a ShellProjectionSnapshot with ViewportSemanticTokenOverlay entries for Keyword, String, Comment, Function at known byte ranges (follow highlighted_snapshot() pattern). Call from_snapshot(). Assert model.active_buffer_code_lines contains DesktopCodeHighlightSpan entries with the matching ViewportSemanticTokenKind values at the expected column ranges.

2. `diagnostic_problems_appear_in_language_rows` — Build a ShellProjectionSnapshot with language_tooling_projection.problems containing error and warning diagnostics at known positions. Call from_snapshot(). Assert model.language_rows contains strings with the diagnostic message text and severity indicators.

3. `terminal_session_content_appears_in_terminal_rows` — Build a ShellProjectionSnapshot with terminal_panel_projection containing a terminal session with output text. Call from_snapshot(). Assert model.terminal_rows contains the expected content.

4. `tab_strip_reflects_active_dirty_clean_state` — Build a ShellProjectionSnapshot with multiple EditorTabProjection entries (one active, one dirty, one clean). Call from_snapshot(). Assert model.tab_rows contains rows reflecting each tab's state (active marker, dirty marker, label).

5. `git_status_appears_in_git_rows` — Build a ShellProjectionSnapshot with git_projection containing modified/staged/clean status. Call from_snapshot(). Assert model.git_rows contains the expected git status text.

6. `empty_shell_produces_no_panic` — Call Shell::empty("Manual").projection_snapshot(), then from_snapshot(). Assert model.tab_rows is empty or contains expected empty-state text. Assert model.terminal_rows handles no-session gracefully.

Each test follows the pattern:
```rust
#[test]
fn test_name() {
    let snapshot = build_snapshot_with_desired_state();
    let model = DesktopProjectionViewModel::from_snapshot(&snapshot);
    assert!(model.some_field.contains(&expected_value));
}
```

> verification: cargo check -p legion-desktop --tests
> verification: cargo test -p legion-desktop --test user_journey_rendering
  </action>
  <verify>
cargo check -p legion-desktop --tests
cargo test -p legion-desktop --test user_journey_rendering
  </verify>
  <done>User journey view-model integration tests written and passing, covering syntax highlights, diagnostics, terminal, tabs, git, and empty-shell safety.</done>
</task>

<task type="auto">
  <name>Task 3: Verify no regressions in full desktop test suite</name>
  <files>read-only verification</files>
  <action>
Run the complete desktop test suite to verify no regressions:

1. `cargo test -p legion-desktop --lib` (unit tests)
2. `cargo test -p legion-desktop --test user_journey_rendering` (new tests)
3. `cargo check -p legion-desktop --tests` (all test files compile)

Pre-existing test failures should be documented but do not block this plan.

> verification: cargo test -p legion-desktop --lib
> verification: cargo test -p legion-desktop --test user_journey_rendering
  </action>
  <verify>
cargo test -p legion-desktop --lib
cargo test -p legion-desktop --test user_journey_rendering
  </verify>
  <done>Full desktop test suite passes with new user journey view-model tests included.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p legion-desktop --tests` exits 0
- [ ] `cargo test -p legion-desktop --test user_journey_rendering` passes all tests
- [ ] Tests cover at least 5 view-model projection stages (syntax highlights, diagnostics, terminal, tabs, git)
- [ ] Tests use ShellProjectionSnapshot + DesktopProjectionViewModel::from_snapshot() — NOT private render_* functions
- [ ] Tests assert on ViewportSemanticTokenKind values and string row contents — NOT Color32 values
- [ ] Tests follow projection_rendering.rs conventions exactly (imports, helpers, assertion style)
- [ ] No modifications to view.rs, theme.rs, or existing test files
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- user_journey_rendering.rs exists with at least 6 integration tests
- All tests pass (`cargo test -p legion-desktop --test user_journey_rendering`)
- Tests prove syntax highlights, diagnostics, terminal, tabs, git, and empty-shell safety project correctly through the view model
- No regressions in existing desktop tests
</success_criteria>

<output>
After completion, create `.legion/project/changes/chg_phase-6-integration-testing-release/06-01-SUMMARY.md`

Update state: Plan 06-01 complete, wave 1 progress.
</output>
