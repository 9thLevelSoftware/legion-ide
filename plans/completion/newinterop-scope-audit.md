# External interoperability scope audit

Baseline requirements: `7a7cf0a` (345 rows before this family replacement). This increment replaces the aggregate `COMP-SCOPE-FAMILY-15` row only and leaves the other 344 requirement objects byte-for-byte unchanged.

## Atomic coverage

| Row | Promise | Package | Implementation | Dependencies |
| --- | --- | --- | --- | --- |
| `COMP-SCOPE-FAMILY-15-01` | Stage 0 publishes the S0-02 compatibility contract naming supported MCP and ACP protocol versions, ratified transports, named peer implementations, roles, capabilities, credentials, privacy/redaction boundaries, and executable acceptance configurations for S4-01 to consume. | `S0-02` | `absent` | — |
| `COMP-SCOPE-FAMILY-15-02` | Legion interoperates as an authenticated MCP client with named supported servers over the S0-02 transports, negotiating peer identity/version and capabilities and handling tools, resources, prompts, credentials, privacy/redaction, bounded results, malformed/error frames, oversized output, timeout, process death, cancellation, reconnect, permission revocation, and audit without direct workspace mutation. | `S4-04` | `partial` | `COMP-SCOPE-FAMILY-15-01`, `COMP-SCOPE-FAMILY-14-10`, `COMP-PROV-002` |
| `COMP-SCOPE-FAMILY-15-03` | Legion exposes its required MCP server role to a named supported external MCP client over a ratified S0-02 transport, authenticating the peer and credentials and handling capability discovery, bounded tool/resource/prompt exchange, privacy/redaction, malformed/error requests, timeout, process death, cancellation, reconnect, permission revocation, audit, and clean disconnect while preserving proposal mediation. | `S4-04` | `partial` | `COMP-SCOPE-FAMILY-15-01`, `COMP-SCOPE-FAMILY-14-10`, `COMP-P5-F1-T4-1`, `COMP-P3-F2-T3-1` |
| `COMP-SCOPE-FAMILY-15-04` | Legion interoperates as an authenticated ACP host with a named supported external agent through negotiated peer identity/version and capabilities, credential and privacy/redaction policy, scoped task/context exchange, streaming progress, proposal/evidence return, malformed/error and timeout handling, process death, cancellation, disconnect/reconnect, restart, and peer revocation. | `S4-05` | `partial` | `COMP-SCOPE-FAMILY-15-01`, `COMP-SCOPE-FAMILY-14-11`, `COMP-P5-F1-T4-1`, `COMP-P3-F2-T3-1`, `COMP-PROV-002` |
| `COMP-SCOPE-FAMILY-15-05` | External MCP and ACP peers run under equivalent Legion containment: scoped paths and commands, network and budget policy, process cleanup, and observable denial, cancellation, disconnect, and restart outcomes. | `S4-05` | `partial` | `COMP-SCOPE-FAMILY-15-02`, `COMP-SCOPE-FAMILY-15-03`, `COMP-SCOPE-FAMILY-15-04`, `COMP-SCOPE-FAMILY-14-03`, `COMP-SCOPE-FAMILY-14-06` |

The replacement keeps orchestration integration canonical in family 14: family-14-10 owns the production MCP-client workflow, family-14-11 owns external ACP-agent workflow, and family-14-12 owns packaged multi-agent/MCP/ACP qualification. These rows isolate external protocol roles, named-peer interoperability, and external containment; they do not duplicate provider/model, context/index/memory, trust/proposal, Delegate, or workflow qualification outcomes. Credential lifecycle is inherited from `COMP-PROV-002`; bounded/redacted tool output from `COMP-P5-F1-T4-1`; and structured evidence from `COMP-P3-F2-T3-1`. Correlation, causality, proposal mediation, and remaining trust controls remain their existing canonical owners.

Source paths in the replacement requirements are literal repository paths and contain no fragment references. Current evidence is conservative: `McpClient<T>` and stdio/Streamable HTTP conformance fixtures provide an MCP substrate, `mcp_server.rs` provides a local server boundary without named external-client qualification, and `AcpHostCommand` plus `legion-agent::external` provide a supervised env-var adapter and proposal/evidence conversion rather than ACP session/initialize interoperability. No current source establishes a Stage 0 named-peer compatibility matrix or packaged real-peer acceptance. Acceptance is unassessed for every replacement row.

The old `plans/adrs/ADR-0039-agent-interop.md` local-stdio/post-GA server boundary is retained as historical decision context and is not modified here. The approved full-product design and `docs/superpowers/plans/2026-09-04-ai-team-completion.md` supersede that limitation as the target scope: the inventory therefore retains the required MCP server role, named peers, and ratified transports while leaving transport selection and implementation qualification to S0-02/S4. Full real-peer contract evidence belongs to S0-02; current implementation remains `partial` and acceptance remains `unassessed`.
