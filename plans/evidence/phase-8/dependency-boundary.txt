Command: cargo run -p xtask -- check-deps
Result: passed

Implemented boundary updates:
- Phase 8 runtime crates remain protocol-mediated and default-deny.
- `devil-remote-transport` now uses rustls/tokio/tokio-rustls/rustls-pki-types only for outbound TLS/mTLS carrier construction.
- `devil-terminal` now depends on `devil-platform` for platform PTY/process boundary use.
- `devil-platform` now declares `windows` for ConPTY probing and `nix` for Unix PTY/process-group plumbing.
- `devil-telemetry` now declares rustls-only `reqwest` for hosted HTTP export while keeping the exporter disabled by default.
- `devil-retention` now declares `keyring` for bundled OS-keyring key-provider support and keeps cloud KMS SDKs out of the workspace.
- `devil-telemetry` and `devil-retention` continue to use `serde`/`serde_json` for durable local metadata indexes.
- No UI ownership of Phase 8 runtimes was introduced.
