Command: cargo deny check
Result: completed

Findings:
- warning[duplicate]: getrandom / rand_core duplicate versions through ChaCha20-Poly1305 AEAD and existing uuid/quickcheck dependency chains.
- warning[duplicate]: thiserror / thiserror-impl duplicate major versions through `cargo_metadata` and workspace crates.
- warning[duplicate]: wit-bindgen duplicate versions through WASI dependency chain.
- advisories ok, bans ok, licenses ok, sources ok.

Policy note: repository `deny.toml` treats duplicate versions as warning-level baseline and allows OSI-approved `BSD-3-Clause` for the reviewed AEAD dependency chain.
