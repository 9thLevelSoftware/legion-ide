# Legion IDE Product Readiness Ledger

This ledger is the product-readiness track for Legion as an enterprise AI-native IDE with required VS Code extension compatibility. It is separate from substrate phase acceptance: a phase can prove architectural safety without proving that the product is ready for daily IDE use.

## Product Target

- Primary v1 customer: enterprise teams that need local or self-hosted AI, auditability, security policy, remote and team workflows, and low-latency native editing.
- Required compatibility target: VS Code extension compatibility staged through manifest ingestion, contribution mapping, activation routing, isolated extension-host planning, and later policy-gated sidecar execution.
- Non-negotiable controls: native Rust core, projection-only UI shell, proposal-mediated mutation, metadata-only defaults, default-deny capability policy, and opt-in cloud or hosted egress.
- Compatibility references: VS Code extension manifest, contribution points, and extension-host model; LSP 3.17; Debug Adapter Protocol.

## Gate Rules

- "Accepted" substrate evidence under Phases 0-8 does not mean the user-facing IDE surface is product-ready.
- Product-ready claims require an explicit gate in this file, current evidence references, passing targeted tests, and a working UX path.
- Milestone acceptance evidence can close a plan milestone or workstream queue while product-readiness remains open; a readiness row changes status only when that row names current evidence, passing targeted tests, and a working UX path.
- Extension compatibility starts metadata-only. Runtime extension host sidecars, webviews, notebooks, custom editors, extension storage, and marketplace execution remain deferred until separate ADR, policy, sandbox, and test evidence exists.
- AI may propose, explain, plan, and verify, but workspace edits remain proposal-mediated unless a later signed enterprise policy explicitly enables autonomous apply.

## Status Vocabulary

- **Not started** — No implemented code or test evidence exists for this gate.
- **In progress** — Partial implementation exists; tests or CI may pass for some sub-flows, but the full gate is not yet substrate-validated.
- **Substrate validated** — Code and tests pass for the underlying architectural surface, but the beta UX path may still require product workflow evidence.
- **Product workflow validated** — A complete, end-to-end user-facing workflow has been demonstrated with current evidence and targeted tests.
- **Deferred with explicit cut line** — The gate is intentionally out of scope for the current milestone; an explicit policy or ADR defines what is deferred and what conditions would unblock it.
- **Blocked** — The gate cannot proceed due to an external dependency, policy gap, or unresolved technical blocker.

## Production Evidence Reconciliation

This table maps accepted production evidence to the product-readiness gates it informs. It is intentionally conservative: evidence can strengthen a gate without promoting the gate to product-ready.

| Evidence | What it proves | Readiness gates informed | Remaining product-readiness gap |
| --- | --- | --- | --- |
| M0 formal acceptance (`evidence/production/M0/M0-milestone-acceptance.md`) | ADR-0032..ADR-0040 are ratified; release-pipeline and perf-harness skeletons exist; baseline gates passed for the then-current tree. | PR-UI-001, PR-UI-002, PR-LANG-001, PR-LANG-002, PR-AI-002, PR-VSC-002, PR-REL-001 | Skeletons and ADRs are not user workflow proof. Product gates still require renderer-backed UX, real language/debug flows, extension-host cut lines, and installability evidence. |
| M1 formal acceptance (`evidence/production/M1/M1-milestone-acceptance.md`) | Editor canvas, tree-sitter, LSP lifecycle, terminal, search, and SCM-oriented surfaces had current-tree substrate evidence. | PR-UI-001, PR-UI-002, PR-LANG-001, PR-LANG-002 | The ledger still needs cross-platform user workflow evidence for daily editing, full LSP UX, terminal cleanup, debug/test explorer, accessibility, and large workspace behavior. |
| M2 formal acceptance (`evidence/production/M2/M2-milestone-acceptance.md`) | Apply/rollback proposal workflows, provider routing, local/provider policy, MCP coverage, semantic fabric, and assist surfaces had current-tree evidence. | PR-AI-001, PR-AI-002 | Proposal safety and inspectable AI are stronger, but adversarial evals, broader provider smoke, cancellation/cost evidence, and product assist/delegate workflows remain bounded by their row evidence. |
| M3 workstream evidence package (`evidence/production/M3/`) | MCP client GA, workflow review/replay, privacy inspector productization, AI second-opinion review, and benchmark posture have task-level evidence. | PR-AI-001, PR-AI-002, PR-ENT-002 | No formal `M3-milestone-acceptance.md` is present in the current tree. Treat M3 as workstream evidence unless a future audit adds formal milestone acceptance. |
| M4 formal acceptance (`evidence/production/M4/M4-milestone-acceptance.md`) | Workflow runtime, fleet console, approval/risk surfaces, and supporting trust/proposal substrate were accepted for the current tree. | PR-AI-002, PR-ENT-002 | Command-center substrate does not prove collaboration/admin product readiness or unrestricted automation. Product rows remain limited to their explicit evidence. |
| M5 formal acceptance (`evidence/production/M5/M5-milestone-acceptance.md`) | Release/signing follow-ons, accessibility/platform parity evidence, terminal hardening, extension launch posture, and docs/support surfaces were accepted or explicitly deferred. | PR-UI-001, PR-LANG-002, PR-VSC-001, PR-REL-001 | Hardware-limited multi-window/DPI coverage, signed installers, auto-update/rollback, crash-report controls, and fresh-machine evidence remain open where the matrix says so. |
| M6 formal acceptance (`evidence/production/M6/M6-milestone-acceptance.md`) | M6 predecessor queue and full phase gates passed after resolving security, license, and session-schema issues. | PR-AI-001, PR-ENT-002, PR-REL-001 | M6 strengthens governance evidence, but remote/collaboration/admin, signed release, crash controls, telemetry flywheel, and external benchmark claims remain constrained by explicit row statuses. |
| WS-P0 Phase 0 truth-repair closure (`evidence/production/WS-P0/phase-0-truth-repair-closure.md`; supersedes the interim `evidence/production/WS-P0/phase-0-gate-baseline.md` run) | Full standing gate set (check-deps, docs-hygiene, claim-audit, no-egui-textedit, verify-kanban-backlog, kanban_backlog tests, fmt, workspace check, full workspace test suite, clippy `-D warnings`, cargo-deny, evals/training pytest) re-run green on commit `fd715c5` (1522 tests passing, 0 failed). Task-to-commit table for Phase 0 Tasks 1-10 recorded there. | All rows below that cite `cargo test --workspace`/`cargo deny check`/`cargo clippy` pass counts | Some Readiness Matrix rows below still cite older raw pass counts (e.g. "909 pass") from earlier evidence captures; those counts are stale relative to the current workspace-wide total in the WS-P0 closure file, but the underlying targeted-test evidence each row cites remains current unless that row's own evidence file says otherwise. Treat the WS-P0 closure file as the current whole-workspace gate baseline; per-row promotion still requires each row's own named evidence. |

## Readiness Matrix

| Track | Gate | Acceptance Criteria | Current Status | Current Evidence |
| --- | --- | --- | --- | --- |
| Performance, UI, accessibility | PR-UI-001 renderer latency and input accessibility | p50/p95 input-to-paint budgets, IME, clipboard, focus, high contrast, screen-reader projection, font sizing, keyboard remapping, floating windows, and multi-monitor restore are tested against renderer-backed workflows. | Substrate validated | `cargo test --workspace --all-targets` (909 pass); `cargo test -p legion-desktop --test projection_rendering` (9 pass); `cargo test -p legion-desktop --test daily_editing_controls` (4 pass); `cargo test -p legion-desktop --test beta_workflow` (5 pass); `cargo test -p legion-app --test daily_editing_contracts` (7 pass); `plans/evidence/production/M5/WS18-T3-platform-parity-matrix.md`. WS-MANUAL-01 adds renderer-backed Manual input-to-paint perf evidence, input/focus/IME/clipboard tests, font/wrapping/degraded-mode renderer evidence, deterministic editor evidence, and an app-level Manual zero-egress smoke under `plans/evidence/production/WS-MANUAL-01/`. Promotion beyond substrate validated still requires current native platform accessibility/focus evidence across supported OSes. |
| Performance, UI, accessibility | PR-UI-002 large workspace behavior | 100MB degraded-mode file path, large tree open, workspace search, diagnostics fanout, extension load, and AI context retrieval do not block typing. | Substrate validated | `cargo test -p legion-app --test workspace_vfs_integration` (60 pass); `cargo test -p legion-desktop --test large_file_guardrails` (2 pass). WS-MANUAL-02 adds: 100MB text model scale tests (`legion-text --test large_scale_100mb`), binary file detection and editor refusal (`legion-text` binary module + `legion-editor --test large_file_scale`), `FileSizeClassification` protocol enum, workspace tree open (<10s for 500 files, `legion-project --test workspace_scale`), watcher burst debounce (`legion-project --test watcher_burst`), search cancellation cleanup (`legion-project --test search_cancellation`), memory ceiling perf gate (1MB buffer <10MB footprint, `xtask perf-harness`), stale snapshot/lease tests, and degraded-mode desktop banner. See `plans/evidence/production/WS-MANUAL-02/WS-MANUAL-02-evidence.md` for full 10/10 SCALE task evidence. |
| Language, debug, test, SCM | PR-LANG-001 Rust language workflow | Rust LSP supervision covers completion, hover, diagnostics, rename, format, code actions, semantic tokens, folding, project-wide errors, and structural search through protocol DTOs. | Substrate validated | `cargo test -p legion-app --test structural_search_workflow` (1 pass); `cargo test -p legion-desktop --test language_terminal_workflow` (3 pass); `cargo test -p legion-app --test language_tooling_workflow` (5 pass). Full LSP completion/diagnostics product UX path is not yet validated. WS-LANG-01 adds: mock-gate tests across all four tiers — `cargo test -p legion-lsp` (lifecycle, transport, pump, discovery, read/write side contracts); `cargo test -p legion-protocol` (LSP health record, 6 pass); `cargo test -p legion-app` (rust_analyzer_download_policy 4 pass, rust_analyzer_session_handshake 3 pass, rust_analyzer_doc_sync 3 pass, rust_analyzer_read_requests 4 pass, language_stale_snapshot 4 pass, language_edit_proposal_routing 5 pass, language_restart_policy 3 pass); `cargo test -p legion-desktop --test language_health_view` (6 pass). Real rust-analyzer 1.95.0 single-OS smoke: `cargo run -p xtask -- rust-analyzer-smoke` passes both `--ignored` tests (lsp: initialize Fresh + ≥1 diagnostic via pump; app: completion/hover/definition/references/formatting/rename all well-formed JSON; restart policy exercised). Status is substrate-validated plus real single-OS (Windows) rust-analyzer smoke evidence available. 3-OS hosted CI smoke is deferred pending CI infrastructure, consistent with PR-REL-001 posture. See `plans/evidence/production/WS-LANG-01/WS-LANG-01-evidence.md`. PKT-LSP-B (branch `m8/lsp-read-ui`, commits 400396c..d678395) adds desktop product UI path: `cargo test -p legion-desktop --test completion_popup` (8 pass — CompletionDismiss, CompletionNext/Prev wrap, CompletionAccept via editor authority, stale popup on tab switch); `cargo test -p legion-desktop --test hover_definition` (7 pass — HoverDismiss, hover auto-show, last_hover_id pre-sync, tab-switch dismiss, GoToDefinition/NavigateToDefinition dispatch); T8 product composition smoke `rust_analyzer_product_composition_smoke` added to `rust_analyzer_workflow` suite (`#[ignore]` gated, exercises AppComposition startup → drain → health → problems → completion dispatch → stale discard). Promotion to product-workflow-validated is deferred until 3-OS real-server smoke and renderer-backed diagnostics panel UX evidence exists. Write-side (rename/code-action/format UI) is explicitly out of scope for this branch (P2.F1.T5). See `plans/evidence/production/M8/WS-LANG-01-product-ui-evidence.md`. |
| Language, debug, test, SCM | PR-LANG-002 debug, tests, and SCM | DAP boundary, debugger projections, breakpoint reliability, zero-config Rust debug, test explorer, Git and jj views, diffs, conflicts, review comments, and local history are product workflows. | Substrate validated | `cargo test -p legion-app --test debug_workflow` (3 pass); `cargo test -p legion-desktop --test debug_workflow` (1 pass); `cargo test -p legion-app --test terminal_workflow` (3 pass); `cargo test -p legion-desktop --test language_terminal_workflow` (3 pass); `cargo test -p legion-app --test git_workflow` (6 pass); `cargo test -p legion-desktop --test git_workflow` (2 pass). DAP/test explorer and zero-config Rust debug product UX paths are not yet validated. |
| AI control plane | PR-AI-001 inspectable local-first AI | Local and self-hosted providers are real first-class paths; cloud providers are opt-in. Context manifests expose files, symbols, diagnostics, terminal excerpts, memory, privacy labels, and egress status before invocation. | Product workflow validated | `cargo test -p legion-app --test control_trust_surfaces` (8 pass); `cargo test -p legion-desktop --test control_trust_view` (4 pass); `cargo test -p legion-retention retention_fixture_audits_access_and_deletes_with_tombstone` (1 pass); `cargo test -p legion-app --test assist_inline_prediction_workflow` (6 pass); `cargo test -p legion-desktop --test assist_inline_prediction_workflow` (1 pass). Privacy inspector rows now prove egress/redaction/consent visibility, and raw-source retention deletion still produces a tombstone with working bundle removal. |
| AI control plane | PR-AI-002 proposal safety and evals | Streaming UI, inline diff review, semantic retrieval provenance, run ledger, cancellation, verification commands, rollback-linked proposals, and adversarial eval fixtures exist. | Substrate validated (proposal safety); Deferred (adversarial evals) | `cargo test -p legion-app --test legion_workflow_integration` (18 pass); `cargo test -p legion-desktop --test save_all_conflict` (5 pass); `cargo test -p legion-desktop --test delegated_task_command_center` (5 pass); `cargo test -p legion-desktop --test legion_workflow_command_center` (6 pass). Adversarial eval fixtures are dry-run scaffolds only (ENGINEERING_AUDIT.yaml finding-training-eval-real-execution-deferred). |
| VS Code compatibility | PR-VSC-001 manifest and contribution compatibility | VSIX/package manifest ingestion, extension identity, contribution mapping, activation-event routing, enable/disable/update metadata, API coverage reporting, and compatibility diagnostics are implemented without runtime execution. | Substrate validated | `cargo test -p legion-vscode-compat --all-targets` (8 pass); `cargo run -p legion-cli -- evidence check --phase gui-phase8` (OK); `cargo run -p xtask -- check-deps` (pass); `cargo deny check` (pass, CI). Runtime extension execution remains deferred. |
| VS Code compatibility | PR-VSC-002 isolated extension host | Node-based extension-host sidecar, `vscode` API facade, versioned RPC bridge, per-command capability checks, proposal-mediated mutation, health UX, crash blame, extension bisect, permission review, and audit logs are implemented. | Deferred with explicit cut line | `AGENTS.md` and `ENGINEERING_AUDIT.yaml` both state runtime extension host sidecars, webviews, notebooks, custom editors, extension storage, and marketplace execution remain deferred until separate ADR, policy, sandbox, and test evidence exist. |
| Remote, collaboration, enterprise | PR-ENT-001 remote development UX | SSH/container connection flow, encrypted transport, reconnect/offline recovery, remote terminal/LSP/filesystem, and visible health are product workflows. | Deferred with explicit cut line | `ENGINEERING_AUDIT.yaml` phase-7-cloud-lane is partially_implemented; `cargo test -p legion-remote --all-targets` (13 pass) validates mock/default-deny policy and client contracts only. Production remote transport is not yet implemented. |
| Remote, collaboration, enterprise | PR-ENT-002 collaboration and admin controls | Presence, shared workspace state, CRDT/operation-log reconciliation, shared proposals, inline review, replay, provider policy, extension allowlist, audit export, retention controls, telemetry consent, raw-source vault controls, and self-hosted diagnostics are implemented. | Deferred with explicit cut line | `AGENTS.md` now distinguishes active phase-gated crates from still-deferred product surfaces; `ENGINEERING_AUDIT.yaml` notes collaboration and admin controls are not yet product workflows. CRDT/operation-log reconciliation, shared proposals, inline review, replay, and admin policy export are deferred. |
| Packaging, licensing, release | PR-REL-001 product promise and installability | Free/local core behavior, paid enterprise controls, transparent AI data use, no hidden code egress, signed installers, auto-update/rollback, Windows/macOS/Linux parity, crash-report controls, and beta feedback capture are documented and verified. | In progress | `cargo test -p legion-desktop --test packaging` (4 pass); `cargo test -p legion-desktop --test platform_smoke` (6 pass); `cargo test -p legion-desktop --test platform_integration` (2 pass); local packaging and smoke dry-run scripts remain available, but no hosted CI workflow currently runs them; `plans/evidence/production/M5/WS17-T2-signing-notarization.md` records the explicit unsigned-beta policy cut line while signed installers / fresh-VM Gatekeeper-SmartScreen evidence remain pending; `plans/evidence/production/M5/WS17-T6-docs-support-surface.md` records the docs/support bundle coverage and docs-hygiene pass; `plans/evidence/production/M6/M6-milestone-acceptance.md` records the accepted M6 milestone gate. Signed installers, auto-update/rollback, and crash-report controls are not yet validated. |

## Beta Acceptance Scenario

The beta loop is not accepted until a user can open a large repository, install an approved VSIX, run Rust LSP completion, ask AI for a multi-file change, inspect the context manifest, review the proposal diff, run tests, debug a failure, collaborate on review, save safely, and export audit evidence without bypassing policy or proposal gates.

## Dogfood Evidence

Weekly Legion-on-Legion dogfood runs should use `plans/dogfood/legion-on-legion-weekly-journal-template.md`. A journal entry is evidence only when it names the branch, commit, OS, workflow attempted, evidence path, result, and product-readiness impact.
