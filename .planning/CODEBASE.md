---
map_schema_version: "2.0"
generated_at: "2026-05-27T12:57:55.9718684-04:00"
analyzed_commit: "beb896492685fadbb4d1669250f0a5f5a145f613"
source_file_count: 182
source_fingerprint: "d659f105ff658b23cd599dfe39bd98ec6aedb99082fc2840467a462783e2b584"
source_fingerprint_kind: "sha256:path-size-mtime"
scope: "project-root"
source_total_bytes: 4767939
source_newest_mtime_utc: "2026-05-27T04:41:28.9335626Z"
---

# Codebase Map: Devil IDE

Generated by `/legion:map --refresh` from the current checkout. Current source files remain the source of truth if this map conflicts with live code.

## Architecture

Devil IDE is a Rust 2024 Cargo workspace for a local-first IDE. The current product track is GUI productization: the repo now contains both the legacy `devil-app` CLI shell proof and the newer `devil-desktop` renderer-backed adapter. Product authority remains outside the renderer.

Core boundary:

- `devil-protocol` is the shared DTO, validation, and port-contract crate. It is the highest fan-in crate and contains editor, workspace, proposal, trust, LSP, semantic, terminal, plugin, collaboration, remote, telemetry, retention, and storage contracts.
- `devil-app` is the composition root. It owns application orchestration, active document state, proposal-mediated saves, language tooling projection refresh, default-deny terminal workflow routing, plugin/collaboration/remote composition, search, storage, and observability wiring.
- `devil-ui` remains projection-only. It accepts `ShellProjectionSnapshot` and emits `CommandDispatchIntent`; it must not own editor text, workspace state, proposal state, storage, terminal sessions, providers, plugins, collaboration, or remote state.
- `devil-desktop` owns native windowing, egui/eframe rendering, adapter-local presentation state, session JSON read/write, input-to-intent translation, smoke harnesses, and display models derived from projections.
- `devil-editor`, `devil-text`, `devil-project`, `devil-platform`, `devil-security`, `devil-observability`, and `devil-storage` provide the current local authority substrate.
- `devil-index`, `devil-terminal`, `devil-ai`, `devil-ai-providers`, `devil-agent`, `devil-memory`, `devil-tracker`, `devil-plugin`, `devil-collaboration`, `devil-remote`, `devil-remote-transport`, `devil-telemetry`, and `devil-retention` provide activated or gated runtime surfaces that must stay behind protocol, policy, proposal, and privacy boundaries.

The high-risk implementation pattern is a deliberate anti-layering rule: renderer and UI code project and dispatch, while `devil-app` and lower authority crates perform stateful work and mutation mediation.

## Frameworks

| Layer | Technology | Evidence |
| --- | --- | --- |
| Language | Rust 2024 | Root `Cargo.toml` workspace package edition |
| Workspace | Cargo resolver 3 workspace | Root `Cargo.toml` lists 26 workspace members |
| Desktop renderer | `eframe`/`egui` 0.34.2 | Root workspace deps plus `crates/devil-desktop/Cargo.toml` |
| CLI parsing | `clap`, `clap_complete` | Root workspace deps and `devil-cli`/`xtask` manifests |
| Serialization | `serde`, `serde_json`, `toml` | Protocol DTOs, session state, storage, config parsing |
| Async/runtime and PTY | `tokio`, platform PTY services | Workspace deps and `devil-platform`/`devil-terminal` |
| Security/network readiness | `rustls`, `tokio-rustls`, `reqwest`, `keyring`, `windows`, `nix` | Workspace deps and remote/telemetry/retention/platform crates |
| Test style | Rust unit and integration tests | 60 Rust files, 48 test-bearing files, 630 `#[test]`/`#[tokio::test]` markers detected |
| Architecture | Cargo monorepo with authority-boundary crates | Root workspace and internal crate dependency graph |

## Language Distribution

| Extension | Count | Notes |
| --- | ---: | --- |
| `.txt` | 103 | Mostly generated fixture/output text under project artifacts |
| `.md` | 89 | Architecture, ADRs, evidence, generated Legion docs, repo guidance |
| `.rs` | 60 | Workspace source, integration tests, and xtask |
| `.toml` | 28 | Root and crate manifests plus Cargo configuration |
| `.lock` | 1 | `Cargo.lock` |
| `.ps1` | 1 | Windows utility script |
| `.sh` | 1 | Shell utility script |

## Entry Points

| Type | Path | Evidence |
| --- | --- | --- |
| CLI app proof | `crates/devil-app/src/main.rs` | `cargo run -p devil-app -- <path>` opens a trusted workspace and supports shell commands |
| App composition library | `crates/devil-app/src/lib.rs` | `AppComposition`, `CommandDispatcher`, `SaveWorkflowService`, language and terminal workflows |
| Desktop GUI adapter | `crates/devil-desktop/src/main.rs` | Calls `devil_desktop::workflow::run_from_env()` |
| Desktop workflow harness | `crates/devil-desktop/src/workflow.rs` | `DesktopLaunchConfig`, `DesktopRuntime`, smoke/session workflow |
| Projection shell | `crates/devil-ui/src/lib.rs` and `crates/devil-ui/src/ui.rs` | Exposes `Shell`, `ShellProjectionSnapshot`, `CommandDispatchIntent` |
| Protocol contracts | `crates/devil-protocol/src/lib.rs` | DTOs, validators, and ports consumed across crates |
| Developer diagnostics CLI | `crates/devil-cli/src/main.rs` | Phase gates, evidence checks, activation/storage/setup diagnostics |
| Repository gates | `xtask/src/main.rs` | `cargo run -p xtask -- check-deps` and phase governance checks |

## Monorepo Structure

**Workspace tool**: Cargo workspace
**Packages**: 26

| Package | Path | Kind | Internal Dependencies |
| --- | --- | --- | --- |
| `devil-protocol` | `crates/devil-protocol` | Shared DTOs, validators, ports | None |
| `devil-app` | `crates/devil-app` | Composition root and CLI proof | Most runtime crates |
| `devil-desktop` | `crates/devil-desktop` | Renderer-backed desktop adapter | `devil-app`, `devil-protocol`, `devil-ui` |
| `devil-ui` | `crates/devil-ui` | Projection shell and intent contracts | `devil-protocol` |
| `devil-editor` | `crates/devil-editor` | Buffer/editor engine | `devil-observability`, `devil-protocol`, `devil-text` |
| `devil-text` | `crates/devil-text` | Rope-backed text and snapshots | `devil-protocol` |
| `devil-project` | `crates/devil-project` | Workspace VFS/file authority | `devil-observability`, `devil-platform`, `devil-protocol`, `devil-security` |
| `devil-platform` | `crates/devil-platform` | Native filesystem/process/PTY/watch abstractions | `devil-protocol` |
| `devil-security` | `crates/devil-security` | Trust, path, command, provider, capability policy | `devil-protocol` |
| `devil-observability` | `crates/devil-observability` | Metadata-only event sinks | `devil-protocol` |
| `devil-storage` | `crates/devil-storage` | In-memory storage and schema/replay surfaces | `devil-observability`, `devil-protocol`, `devil-security` |
| `devil-index` | `crates/devil-index` | Semantic indexing and lexical fallback | `devil-protocol`, `devil-text` |
| `devil-terminal` | `crates/devil-terminal` | Policy-gated terminal runtime/fixture | `devil-platform`, `devil-protocol` |
| `devil-ai` | `crates/devil-ai` | Provider contracts/routing | `devil-protocol`, `devil-security` |
| `devil-ai-providers` | `crates/devil-ai-providers` | Deterministic local provider registry | `devil-ai`, `devil-protocol`, `devil-security` |
| `devil-agent` | `crates/devil-agent` | Agent runtime state machine | `devil-ai`, `devil-protocol`, `devil-tracker` |
| `devil-tracker` | `crates/devil-tracker` | Tracking ledger | `devil-protocol`, `devil-storage` |
| `devil-memory` | `crates/devil-memory` | Memory metadata service | `devil-protocol`, `devil-storage` |
| `devil-plugin` | `crates/devil-plugin` | Plugin host-capability surface | `devil-observability`, `devil-platform`, `devil-protocol`, `devil-security`, `devil-storage` |
| `devil-collaboration` | `crates/devil-collaboration` | Collaboration session runtime | `devil-protocol` |
| `devil-remote` | `crates/devil-remote` | Remote workspace state surface | `devil-protocol` |
| `devil-remote-transport` | `crates/devil-remote-transport` | Remote transport handshake/frame policy | `devil-protocol` |
| `devil-telemetry` | `crates/devil-telemetry` | Hosted telemetry metadata surface | `devil-protocol` |
| `devil-retention` | `crates/devil-retention` | Raw-source retention/vault surface | `devil-protocol` |
| `devil-cli` | `crates/devil-cli` | Diagnostics/setup/evidence CLI | `devil-index`, `devil-protocol`, `devil-storage` |
| `xtask` | `xtask` | Repo policy and phase gates | External `cargo_metadata`, `clap` |

## Functionality Inventory

| Capability | Current Surface | Notes |
| --- | --- | --- |
| Desktop launch/render | `devil-desktop` | Opens a native egui window, projects shell layout, explorer, tabs, active buffer, status, trust/proposal, search, language, terminal, assistant, plugin, and collaboration rows. |
| Desktop input bridge | `devil-desktop::bridge` | Translates adapter actions into `CommandDispatchIntent` or app-level requests with typed errors for missing buffers, tabs, files, dirty prompts, terminal sessions, and invalid paths. |
| CLI shell proof | `devil-app` binary | Still available as proof harness; not the primary GUI renderer. |
| Open/edit/save | `AppComposition`, `EditorEngine`, `WorkspaceActor` | Saves remain proposal-mediated through `SaveWorkflowService` and workspace proposal preconditions. |
| Multi-tab daily editing | `AppComposition`, `devil-ui`, `devil-desktop` | Tabs, dirty prompts, cursor/selection/scroll, save-all, session restore, bounded search. |
| Workspace VFS | `devil-project` | Root-boundary checks, discovery, watcher recovery, file open/save with proposal validation. |
| Text model | `devil-text`, `devil-editor` | Rope-backed snapshots, chunk descriptors, degraded mode, undo/redo, atomicity and retention tests. |
| Language tooling | `devil-app::LanguageToolingWorkflow`, `devil-index`, `devil-protocol` | Diagnostics, hover, completion, definition, references, outline, formatting/rename/import/code-action proposal routing. |
| Terminal | `devil-app::TerminalWorkflow`, `devil-terminal`, `devil-platform` | Default-deny launch/input/resize/close/kill/search/poll with fixture runtime, bounded output, metadata audit. |
| Assisted AI/trust surfaces | `devil-protocol`, `devil-app` | Rich metadata-only DTOs exist; Phase 4 local deterministic provider path is proposal-only. Phase 5 must make control/trust GUI surfaces usable. |
| Plugin/collaboration/remote | `devil-plugin`, `devil-collaboration`, `devil-remote*` | Protocol and app-level surfaces exist but remain gated by future phase acceptance. |
| Storage/observability | `devil-storage`, `devil-observability` | Metadata-only events, replay/schema/health surfaces, terminal/proposal/plugin event persistence. |
| Dependency gates | `xtask`, `plans/dependency-policy.md`, `deny.toml` | Enforce crate boundary policy, renderer gate, and phase governance artifacts. |

## Module Ownership

| Owner Crate/Area | Owns | Must Not Own |
| --- | --- | --- |
| `devil-desktop` | Native renderer, egui panels, adapter-local view/session state, action bridge, smoke harness | Editor buffers, workspace state, proposal lifecycle, terminal authority, provider/plugin/collab runtime authority |
| `devil-ui` | Projection DTO shaping and command-intent parsing | Renderer resources, text ownership, workspace/project state, saves, terminal sessions |
| `devil-app` | Composition, active document binding, command routing, save workflow, search, language and terminal workflow orchestration | Raw filesystem authority outside `devil-project`/`devil-platform`, renderer details |
| `devil-project` | Workspace root/file authority, file preconditions, proposal-mediated mutations | Editor text transactions or renderer UI state |
| `devil-editor`/`devil-text` | Buffer text, transactions, undo/redo, snapshot leases, viewport projections | Disk writes or proposal approval decisions |
| `devil-protocol` | Wire contracts, projections, validation helpers, ports | Runtime state, storage side effects, renderer behavior |
| `devil-security` | Deny-by-default policy and capability decisions | Applying edits or file mutation |
| `xtask`/policy docs | Repo governance and allowed dependency edges | Product runtime behavior |

## Risks

| Risk | Level | Evidence | Mitigation |
| --- | --- | --- | --- |
| Large monolithic protocol/app files | High | `devil-protocol/src/lib.rs` around 19k lines, `devil-app/src/lib.rs` around 14k lines | Keep Phase 5 edits narrow; add regression coverage around exact surfaces touched. |
| UI/renderer authority creep | High | Desktop adapter now exists and can collect many actions | Preserve `devil-ui` projection-only invariant and route mutation through app/workspace/proposal services. |
| Phase 5 trust surfaces have protocol depth but limited GUI usability | High | Context manifest, privacy, budget, approval, checkpoint, rollback, assisted AI DTOs exist; app currently has empty/default projection helpers and Phase 4 local provider projection | Phase 5 should wire real projection population and desktop display controls without autonomous apply. |
| Terminal policy and audit ordering | High | Terminal actions are default-deny and persist metadata audit after workflow operations | Keep terminal runtime unable to mutate editor/disk directly; preserve validator/audit-before-success tests. |
| Map includes generated/planning churn | Medium | `.planning` changed heavily through Phase 4; map excludes `.planning` from fingerprint but includes planning docs in directory mapping counts | Read live source before implementing; use map for orientation only. |
| Root `test.rs` scratch file | Medium | Root file contains a standalone experimental Rust snippet and is outside workspace manifests | Treat as stray/manual artifact unless intentionally added to a future cleanup scope. |
| Large-file degraded mode gap | Medium | Known ignored 100MB benchmark remains a degraded/streaming-mode gap | Do not promote ignored benchmark as green; keep bounded projection/search checks. |

## Dependency Risk

### Outdated Packages

_None detected from local manifests._ The map did not query remote registries, so this is not a current-version audit.

### Heavy Dependencies

- `eframe`/`egui` are deliberate renderer dependencies and should remain isolated to `devil-desktop` by policy.
- `windows`/`nix`/PTY/TLS/keyring dependencies are platform/security sensitive and should remain behind `devil-platform`, remote, telemetry, or retention boundaries.
- `reqwest`/TLS dependencies should not leak into UI or desktop presentation code.

### Potentially Unmaintained

_None detected by local heuristic._ No external maintenance-age lookup was performed.

### Dependency Risk Summary

Local dependency risk is mostly architectural rather than version-based: keep renderer dependencies out of `devil-ui`, keep `devil-editor -> devil-project` forbidden, and keep network/secret/terminal dependencies behind authority crates and policy gates.

## Dependency Graph

### Fan-out

| Crate | Dependency Risk |
| --- | --- |
| `devil-app` | Highest fan-out; composes most authority and feature crates. |
| `devil-plugin` | Crosses protocol, security, observability, storage, and platform boundaries. |
| `devil-desktop` | Depends only on `devil-app`, `devil-protocol`, and `devil-ui` internally; renderer deps are external. |
| `devil-editor` | Depends on text/protocol/observability only. |
| `devil-project` | Depends on protocol/platform/security/observability. |

### Fan-in

| Crate | Fan-in Reason |
| --- | --- |
| `devil-protocol` | Shared by nearly every crate. Contract changes have the largest blast radius. |
| `devil-security` | Consumed by app/project/storage/AI/plugin decisions. |
| `devil-storage` | Consumed by app/tracker/memory/plugin/CLI. |
| `devil-platform` | Consumed by project/terminal/plugin/app platform paths. |
| `devil-ui` | Consumed by app and desktop only; keep it narrow. |

### Key Dependency Chains

- `devil-desktop -> devil-ui -> devil-protocol`: projection render path.
- `devil-desktop -> devil-app -> devil-project/devil-editor/devil-protocol`: desktop action to authority-owned mutation path.
- `devil-app -> SaveWorkflowService -> WorkspaceActor::save_file_with_proposal`: proposal-mediated save path.
- `devil-app -> LanguageToolingWorkflow -> devil-index -> devil-protocol`: Phase 4 language projection path.
- `devil-app -> TerminalWorkflow -> devil-terminal/devil-security/devil-platform -> devil-protocol`: policy-gated terminal path.
- `xtask -> plans/dependency-policy.md`: dependency and phase acceptance governance path.

## Test Coverage Map

| Area | Tests/Evidence |
| --- | --- |
| Protocol contracts | `crates/devil-protocol/tests/dto_contracts.rs`, inline tests in `devil-protocol/src/lib.rs` |
| Workspace/app saves | `crates/devil-app/tests/workspace_vfs_integration.rs`, `crates/devil-project/tests/*`, app inline tests |
| Desktop adapter | `crates/devil-desktop/tests/*` covering projection rendering, bridge, workflow, search, session restore, save-all conflict, platform smoke, language/terminal view/workflow |
| Language and terminal | `crates/devil-app/tests/language_*`, `crates/devil-app/tests/terminal_workflow.rs`, `crates/devil-desktop/tests/language_terminal_*` |
| Editor/text | `crates/devil-editor/tests/*`, `devil-editor`/`devil-text` inline tests |
| Security/storage/observability | Inline and integration tests in `devil-security`, `devil-storage`, `devil-observability` |
| Indexing | `crates/devil-index/tests/index_workflows.rs` plus inline semantic tests |
| Repo gates | `xtask` inline tests and `cargo run -p xtask -- check-deps` |

### Files Without Dedicated External Integration Tests

- Many gated feature crates primarily use inline tests (`devil-agent`, `devil-ai`, `devil-memory`, `devil-tracker`, `devil-plugin`, `devil-collaboration`, `devil-remote`, `devil-telemetry`, `devil-retention`). This is acceptable for inert/gated surfaces but Phase 5+ should add contract-level tests when surfaces become user-facing.
- `crates/devil-desktop/src/main.rs` is thin and covered indirectly through workflow/smoke paths.
- Root `test.rs` is not part of the Cargo workspace and has no tests.

### Critical Untested Files

_No untested critical files detected by the map heuristic._ The largest/highest-fan-in crates have inline or integration coverage, but the map did not run coverage instrumentation.

## API Surface

No web HTTP route surface is present. The repository API surface is crate/library and CLI based:

| Surface | Path | Notes |
| --- | --- | --- |
| Workspace/editor/proposal/terminal/LSP/semantic/plugin ports | `crates/devil-protocol/src/lib.rs` | Shared traits and request/response enums. |
| CLI app proof | `crates/devil-app/src/main.rs` | Interactive shell proof for open/save/quit. |
| Desktop app | `crates/devil-desktop/src/main.rs` | Native GUI entrypoint with smoke/session flags. |
| Diagnostics CLI | `crates/devil-cli/src/main.rs` | Doctor, setup, storage, evidence, activation commands. |
| Xtask gate | `xtask/src/main.rs` | Dependency policy and phase acceptance checks. |

## Config & Environment

### Config Files

| Path | Purpose |
| --- | --- |
| `Cargo.toml` | Workspace members, workspace deps, renderer deps, internal crate aliases |
| `Cargo.lock` | Locked dependency graph |
| `deny.toml` | Cargo deny policy used by CI |
| `plans/dependency-policy.md` | Human-readable crate dependency policy parsed by `xtask` |
| `AGENTS.md` | Repo agent constraints and phase gates |
| `.github/workflows/*` | CI workflows |
| `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` | Legion project state |

### Environment Variables

| Name/Use | Evidence |
| --- | --- |
| Desktop launch args | `devil-desktop` supports `--workspace`, `--file`, `--smoke`, `--duration-ms`, `--evidence`, `--session-state` |
| CLI setup/status paths | `devil-cli` accepts workspace/storage/evidence command args |
| Cargo/CI environment | Phase gates are cargo commands, no application runtime env required for local proof |

### Secret Exposure Warnings

_None detected by local scan._ The map did not print secret values and did not inspect ignored/private files.

## Setup / Runbook

Primary gates from repo instructions:

```powershell
cargo run -p xtask -- check-deps
cargo fmt --all --check
cargo check --workspace --all-targets
cargo test --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
```

Run current app surfaces:

```powershell
cargo run -p devil-app -- .
cargo run -p devil-desktop -- --workspace .
cargo run -p devil-desktop -- --workspace . --smoke --duration-ms 1500 --evidence plans/evidence/gui-productization/phase-2-renderer-smoke.md
cargo run -p devil-cli -- setup status --workspace .
```

Single targeted examples:

```powershell
cargo test -p devil-app --test workspace_vfs_integration workspace_vfs_integration_external_overwrite_between_open_and_save_yields_conflict
cargo test -p devil-desktop --test language_terminal_workflow
cargo test -p devil-protocol --test dto_contracts language_terminal_projection_contracts_roundtrip
```

## Code Patterns

### Projection-only UI pattern

`devil-ui` exposes display DTOs and command intents. `devil-desktop` consumes those projections and maps UI actions back into intents; neither should own editor/workspace/proposal state.

### App-owned authority routing pattern

`AppComposition::dispatch_ui_intent` routes `CommandDispatchIntent` into app command requests. Direct edits go through editor authority; saves go through `SaveWorkflowService`; terminal and language operations update projections through app-owned workflows.

### Proposal-mediated mutation pattern

File saves and edit-producing language/AI actions become proposals or proposal previews before mutation. Stale/conflict/denial outcomes preserve dirty text and return rejected outcomes rather than pretending success.

### Metadata-only audit pattern

Observability, storage, terminal, plugin, AI, collaboration, remote, telemetry, and retention surfaces use metadata-only records with non-zero correlation/event/causality expectations where applicable.

### Default-deny capability pattern

Terminal, provider, plugin, collaboration, remote, telemetry, and retention behavior should begin denied or inert, then become available only through explicit policy contracts and tests.

## Directory Mappings

See `.planning/config/directory-mappings.yaml` for machine-readable mappings. High-value current categories are `app`, `desktop`, `ui`, `protocol`, `workspace`, `editor`, `security`, `storage_observability`, `feature_surfaces`, `planning_docs`, `governance`, and `scripts`.

## Retrieval Artifacts

- `.planning/codebase/index.jsonl` contains queryable chunks for architecture, app, desktop, UI, protocol, saves, language, terminal, trust/AI, plugin/collab/remote, tests, and governance.
- `.planning/codebase/symbols.json` contains entry points, modules, tests, config, dependencies, ownership, and risk areas.
- `.planning/codebase/search.md` documents query planning and readback rules.

## Agent Guidance

- Read live source before editing; this map is orientation, not source of truth.
- For Phase 5, start in `devil-protocol` trust/assisted-AI DTOs, `devil-app` projection helpers/workflows, `devil-ui` projection rows/intents, `devil-desktop` view/bridge/workflow tests, and `xtask` phase 5 gates.
- Keep `devil-desktop` renderer dependencies and state local to the adapter.
- Do not weaken proposal, terminal, privacy, or dependency gates to pass tests.
- Treat `test.rs` at repo root as suspicious because it is not a workspace member.
