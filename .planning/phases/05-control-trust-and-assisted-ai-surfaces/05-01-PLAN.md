---
phase: "05-control-trust-and-assisted-ai-surfaces"
plan: "05-01"
title: "Governance And Acceptance Gate Rebaseline"
wave: 1
agents: ["project-manager-senior", "engineering-security-engineer"]
depends_on: []
requirements: ["R-010", "R-011", "R-013"]
expected_artifacts:
  - path: "plans/dependency-policy.md"
    provides: Compatibility-preserving GUI Phase 5 control/trust policy entry that does not erase legacy plugin Phase 5 evidence
    required: true
  - path: "xtask/src/main.rs"
    provides: GUI Phase 5 acceptance-governance check for control/trust/assisted-AI evidence while preserving legacy plugin checks
    required: true
  - path: "plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md"
    provides: Not-accepted GUI Phase 5 evidence stub with final checklist and artifact inventory
    required: true
  - path: ".planning/phases/05-control-trust-and-assisted-ai-surfaces/05-01-RESULT.md"
    provides: Plan execution record with decisions and command results
    required: true
verification_commands:
  - "rg -q \"GUI Phase 5\" plans/dependency-policy.md"
  - "rg -q \"phase-5-control-trust-assisted-ai\" xtask/src/main.rs"
  - "rg -q \"Phase 5 acceptance: Not accepted\" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md"
  - "cargo run -p xtask -- check-deps"
files_modified:
  - "plans/dependency-policy.md"
  - "xtask/src/main.rs"
  - "plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md"
  - ".planning/phases/05-control-trust-and-assisted-ai-surfaces/05-01-RESULT.md"
files_forbidden:
  - "crates/"
  - "Cargo.toml"
  - "Cargo.lock"
  - "plans/evidence/phase-5/plugin-architecture-map.md"
---

# Plan 05-01: Governance And Acceptance Gate Rebaseline

## Objective

Make GUI Phase 5 governable without breaking the existing accepted legacy plugin Phase 5 gate.

<execution_contract>

- read-before-write: read `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-CONTEXT.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `plans/dependency-policy.md`, `xtask/src/main.rs`, `plans/evidence/phase-5/plugin-architecture-map.md`, and `AGENTS.md` before editing.
- evidence-before-action: confirm that `xtask` currently validates legacy plugin Phase 5 evidence and that the GUI roadmap Phase 5 is control/trust/assisted-AI before changing gate text.
- minimal diff: edit only governance and evidence-stub files; do not touch product crates in this plan.
- verify-before-report: run every frontmatter command and record outcomes in `05-01-RESULT.md`.
- role: governance and security planning implementer.
- task: preserve legacy plugin evidence while adding a distinct GUI Phase 5 acceptance path for control/trust/assisted-AI surfaces.
- scope: dependency policy, `xtask` acceptance-governance constants/helpers/tests, GUI Phase 5 evidence stub, and this plan result file.
- allowed tools/actions: focused Markdown/Rust edits, `rg`, targeted `xtask` tests if needed, and `cargo run -p xtask -- check-deps`.
- forbidden actions: deleting or rewriting legacy plugin Phase 5 evidence, modifying product crates, weakening dependency policy, or marking GUI Phase 5 accepted before implementation.
- verification criteria: `xtask check-deps` passes; the GUI evidence stub is explicitly not accepted; legacy plugin evidence remains accepted and untouched.
- final result format: write `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-01-RESULT.md` with changed files, command outcomes, and any `BLOCKED` items.

</execution_contract>

<stop_gates>

- BLOCKED if `xtask check-deps` cannot pass while both legacy plugin Phase 5 and GUI Phase 5 evidence paths coexist.
- BLOCKED if the only way to add GUI Phase 5 governance is to delete or downgrade `plans/evidence/phase-5/plugin-architecture-map.md`.
- BLOCKED if the policy update would authorize `devil-ui` or `devil-desktop` to own proposal lifecycle, provider routing, editor text, workspace mutation, or storage authority.
- BLOCKED if GUI Phase 5 evidence would have to be marked accepted before implementation.

</stop_gates>

<recovery>

- If `xtask` changes break legacy Phase 5 tests, restore legacy constants and add a separate GUI Phase 5 branch instead of replacing old checks.
- If the dependency policy already contains sufficient GUI Phase 5 language, add only a short cross-reference and tests rather than duplicating policy.
- If the evidence stub conflicts with existing evidence naming, keep the stub under `plans/evidence/gui-productization/` and document the legacy collision.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] `plans/dependency-policy.md` explicitly mentions GUI Phase 5 control/trust/assisted-AI boundaries.
- [ ] `xtask/src/main.rs` can validate the GUI evidence path without removing legacy plugin checks.
- [ ] GUI Phase 5 evidence exists and says `Phase 5 acceptance: Not accepted`.
- [ ] `cargo run -p xtask -- check-deps` passes.
- [ ] No forbidden file is modified.

</verification>

<success_criteria>

- GUI Phase 5 has a distinct acceptance path and artifact inventory.
- Legacy accepted plugin Phase 5 evidence remains intact.
- The policy states that UI/desktop are projection and intent surfaces only.
- Product-code plans can depend on a clear final evidence contract.

</success_criteria>

<output>

Write `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-01-RESULT.md`.

</output>

## Task 1: Rebaseline GUI Phase 5 Policy

### Read Targets

- `plans/dependency-policy.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-CONTEXT.md`
- `plans/evidence/phase-5/plugin-architecture-map.md`

### Write Targets

- `plans/dependency-policy.md`

### Implementation Sequence

1. Add a `GUI Phase 5` paragraph near the existing app/UI/runtime policy entries. The paragraph must authorize only app-owned proposal/trust/assisted-AI composition and must explicitly forbid UI/desktop ownership of proposal lifecycle, provider routing, editor text, workspace mutation, storage authority, and raw-source retention.
   > verification: `rg -q "GUI Phase 5" plans/dependency-policy.md`
2. Preserve the existing legacy plugin Phase 5 section and mention that it is historical runtime evidence, not the active GUI productization phase.
   > verification: `rg -q "Phase 5 activates `devil-plugin`" plans/dependency-policy.md`
3. Do not add dependencies or modify manifests.
   > verification: `git diff --quiet -- Cargo.toml Cargo.lock`

### Edge And Error Cases

- The policy must not authorize hosted provider calls, autonomous proposal apply, or raw source persistence.
- If existing wording conflicts with GUI Phase 5, add a compatibility note instead of deleting prior evidence.

<verify>

```powershell
rg -q "GUI Phase 5" plans/dependency-policy.md
rg -q "Phase 5 activates `devil-plugin`" plans/dependency-policy.md
git diff --quiet -- Cargo.toml Cargo.lock
```

</verify>

### BLOCKED Conditions

- Existing dependency policy cannot distinguish GUI Phase 5 from legacy plugin Phase 5 without weakening accepted policy.

## Task 2: Add GUI Phase 5 Xtask Governance

### Read Targets

- `xtask/src/main.rs`
- `plans/evidence/phase-5/plugin-architecture-map.md`

### Write Targets

- `xtask/src/main.rs`

### Implementation Sequence

1. Add a separate GUI Phase 5 evidence constant for `plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`.
   > verification: `rg -q "phase-5-control-trust-assisted-ai" xtask/src/main.rs`
2. Add validation that permits `Phase 5 acceptance: Not accepted` while work is in progress and enforces a final checked checklist plus required artifacts only when the GUI evidence claims accepted.
   > verification: `rg -q "GUI Phase 5" xtask/src/main.rs`
3. Preserve all existing legacy plugin Phase 5 required artifact checks and unit tests. Add or update targeted `xtask` tests for the GUI evidence branch if the current tests do not cover it.
   > verification: `cargo test -p xtask phase5 -- --nocapture`

### Edge And Error Cases

- Do not change `DEFAULT_PHASE5_EVIDENCE_PATH` semantics unless a compatibility constant preserves legacy plugin validation.
- Do not allow both accepted and not-accepted markers in the same GUI status section.

<verify>

```powershell
rg -q "phase-5-control-trust-assisted-ai" xtask/src/main.rs
cargo test -p xtask phase5 -- --nocapture
```

</verify>

### BLOCKED Conditions

- `xtask` cannot validate both evidence tracks without ambiguous acceptance status.

## Task 3: Create Not-Accepted GUI Evidence Stub

### Read Targets

- `.planning/ROADMAP.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-CONTEXT.md`

### Write Targets

- `plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-01-RESULT.md`

### Implementation Sequence

1. Create `plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md` with sections `## Acceptance Status`, `## Scope`, `## Required Artifacts`, and `## Final Validation Checklist`.
   > verification: `test -f plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`
2. Set acceptance to exactly `Phase 5 acceptance: Not accepted.` and leave every final checklist item unchecked.
   > verification: `rg -q "Phase 5 acceptance: Not accepted" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`
3. List required artifacts for governance, protocol/UI contracts, app routing, assisted-AI routing, desktop controls, safety evidence, and full cargo gates.
   > verification: `rg -q "control-trust-safety" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`

### Edge And Error Cases

- Do not mark acceptance as accepted in this plan.
- Do not copy raw command logs into the stub.

<verify>

```powershell
test -f plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md
rg -q "Phase 5 acceptance: Not accepted" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md
cargo run -p xtask -- check-deps
```

</verify>

### BLOCKED Conditions

- The evidence stub cannot pass `xtask check-deps` while marked not accepted.
