---
phase: "05-control-trust-and-assisted-ai-surfaces"
plan: "05-02"
title: "Trust Projection Contract Completion"
wave: 2
agents: ["engineering-senior-developer", "engineering-security-engineer"]
depends_on: ["05-01"]
requirements: ["R-010", "R-011", "R-013"]
expected_artifacts:
  - path: "crates/devil-protocol/src/lib.rs"
    provides: Completed metadata-only control/trust projection contracts for GUI detail surfaces
    required: true
  - path: "crates/devil-protocol/tests/dto_contracts.rs"
    provides: Round-trip and redaction tests for control/trust/assisted-AI projection contracts
    required: true
  - path: "crates/devil-ui/src/ui.rs"
    provides: Projection-only shell snapshot and command-intent coverage for Phase 5 trust/control data
    required: true
  - path: ".planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md"
    provides: Plan execution record with decisions and command results
    required: true
verification_commands:
  - "cargo test -p devil-protocol --test dto_contracts control_trust -- --nocapture"
  - "cargo test -p devil-ui control_trust -- --nocapture"
  - "cargo check -p devil-ui --all-targets"
files_modified:
  - "crates/devil-protocol/src/lib.rs"
  - "crates/devil-protocol/tests/dto_contracts.rs"
  - "crates/devil-ui/src/ui.rs"
  - ".planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md"
files_forbidden:
  - "crates/devil-app/"
  - "crates/devil-desktop/"
  - "crates/devil-editor/"
  - "crates/devil-project/"
  - "crates/devil-storage/"
  - "crates/devil-ai/"
  - "crates/devil-ai-providers/"
  - "xtask/src/main.rs"
  - "plans/dependency-policy.md"
  - "Cargo.toml"
  - "Cargo.lock"
---

# Plan 05-02: Trust Projection Contract Completion

## Objective

Complete the protocol and projection-only UI contracts that later app and desktop plans will populate and render.

<execution_contract>

- read-before-write: read `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-CONTEXT.md`, `crates/devil-protocol/src/lib.rs`, `crates/devil-protocol/tests/dto_contracts.rs`, `crates/devil-ui/src/ui.rs`, and `05-01-RESULT.md` before editing.
- evidence-before-action: confirm existing trust DTO/helper coverage before adding fields; prefer existing contracts over parallel DTOs.
- minimal diff: modify only protocol DTO/helper gaps, DTO tests, UI snapshot/default/intent tests, and this result file.
- verify-before-report: run every frontmatter command and record outcomes in `05-02-RESULT.md`.
- role: protocol/UI contract implementer with security review.
- task: ensure proposal detail, diff/target, approval, rollback/checkpoint, context manifest, privacy, permission, risk, cost, and assisted-AI projection data can be carried without raw source or renderer authority.
- scope: `devil-protocol` DTO/helpers/tests and `devil-ui` projection-only shell data/intents.
- allowed tools/actions: focused Rust edits, DTO serialization tests, UI unit tests, cargo check.
- forbidden actions: app workflow changes, desktop rendering changes, provider routing changes, storage writes, dependency changes, or adding raw source fields.
- verification criteria: control/trust DTO tests pass, UI tests pass, and `devil-ui` still depends only on allowed projection crates.
- final result format: write `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md` with changed files, command outcomes, and any `BLOCKED` items.

</execution_contract>

<stop_gates>

- BLOCKED if a usable proposal detail view would require `devil-ui` to own proposal lifecycle state or call app services.
- BLOCKED if a contract requires raw source, raw provider output, raw terminal output, secrets, or unredacted paths in persistent projection data.
- BLOCKED if required detail data cannot be represented by existing `ProposalLedgerProjection`, trust projections, or a narrowly added metadata-only field.
- BLOCKED if UI tests require imports from app, desktop, editor, project, storage, AI provider, or renderer crates.

</stop_gates>

<recovery>

- If a new field duplicates an existing protocol field, remove the duplicate and adapt tests to the existing field.
- If serialization tests fail because a required field is missing, add the smallest explicit field and a default/empty constructor update.
- If UI default snapshots become verbose, keep empty projection helpers metadata-only and bounded.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] DTO tests cover proposal details, diff/target summaries, trust projections, AI previews/refusals, and redaction.
- [ ] UI tests prove Phase 5 projections are copied from snapshots and command intents remain projection-only.
- [ ] No raw source or raw provider response field is added to persistent projections.
- [ ] `cargo check -p devil-ui --all-targets` passes.

</verification>

<success_criteria>

- Later app code can populate selected proposal details using protocol DTOs without inventing new renderer-owned state.
- Later desktop code can render control/trust panels from `ShellProjectionSnapshot`.
- Projection contracts expose denial, redaction, risk, budget, approval, rollback, and assisted-AI preview/refusal metadata.

</success_criteria>

<output>

Write `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md`.

</output>

## Task 1: Complete Protocol Trust Detail Contracts

### Read Targets

- `crates/devil-protocol/src/lib.rs`
- `crates/devil-protocol/tests/dto_contracts.rs`
- `.planning/codebase/index.jsonl` chunk `map:protocol-trust-ai:001`
- `.planning/codebase/index.jsonl` chunk `map:protocol-language-terminal:001`

### Write Targets

- `crates/devil-protocol/src/lib.rs`
- `crates/devil-protocol/tests/dto_contracts.rs`

### Implementation Sequence

1. Verify that `ProposalLedgerRow`, `ContextManifestProjection`, `PrivacyInspectorProjection`, `PermissionBudgetProjection`, `ProposalApprovalChecklistProjection`, `CheckpointRollbackProjection`, and `AssistedAiProjection` expose the data needed for proposal details, diff/target summaries, approval blockers, rollback limitations, context items, redaction/denial counts, budget/refusal states, risk labels, cost labels, and AI preview/refusal links.
   > verification: `rg -q "pub struct ProposalLedgerRow" crates/devil-protocol/src/lib.rs; rg -q "pub struct AssistedAiProjection" crates/devil-protocol/src/lib.rs`
2. If one of those surfaces cannot represent the required detail metadata, add a metadata-only field to the existing projection type rather than adding a parallel renderer-specific DTO. The field must include redaction hints or derive from existing redacted summaries.
   > verification: `rg -q "RedactionHint::MetadataOnly" crates/devil-protocol/tests/dto_contracts.rs`
3. Add `control_trust` filtered DTO tests covering round-trip serialization, missing required fields, no raw source/provider output, and refusal/redaction/budget/rollback states.
   > verification: `cargo test -p devil-protocol --test dto_contracts control_trust -- --nocapture`

### Edge And Error Cases

- Empty projections must remain valid.
- Unknown rollback, blocked approval, privacy denial, budget depletion, provider refusal, stale/conflict/failed lifecycle, and redacted target coverage must be serializable.

<verify>

```powershell
cargo test -p devil-protocol --test dto_contracts control_trust -- --nocapture
```

</verify>

### BLOCKED Conditions

- The protocol cannot carry a required GUI Phase 5 state without raw source or app-owned state.

## Task 2: Harden Projection-Only UI Snapshot Coverage

### Read Targets

- `crates/devil-ui/src/ui.rs`
- `crates/devil-protocol/src/lib.rs`

### Write Targets

- `crates/devil-ui/src/ui.rs`

### Implementation Sequence

1. Ensure `ShellProjectionSnapshot`, `Shell::projection_snapshot`, `Shell::replace_projection_snapshot`, and empty projection helpers include every Phase 5 trust/control projection without app-owned behavior.
   > verification: `rg -q "context_manifest_projection" crates/devil-ui/src/ui.rs; rg -q "assisted_ai_projection" crates/devil-ui/src/ui.rs`
2. Ensure `CommandDispatchIntent` covers proposal preview, approve, reject, apply, rollback, cancel, details, and assisted-AI start/cancel/replay/inspect without executing those actions in UI.
   > verification: `rg -q "OpenProposalDetails" crates/devil-ui/src/ui.rs; rg -q "StartAiRun" crates/devil-ui/src/ui.rs`
3. Add `control_trust` UI tests proving projections are copied from static snapshots and proposal/AI commands only enqueue intents without mutating editor/workspace projections.
   > verification: `cargo test -p devil-ui control_trust -- --nocapture`

### Edge And Error Cases

- Keep default empty projections disabled/empty, not success-shaped.
- Do not parse or store raw proposal payloads in UI command handlers.

<verify>

```powershell
cargo test -p devil-ui control_trust -- --nocapture
cargo check -p devil-ui --all-targets
```

</verify>

### BLOCKED Conditions

- UI projection tests require product authority or renderer-local lifecycle state.

## Task 3: Record Contract Readiness

### Read Targets

- `05-01-RESULT.md`
- `crates/devil-protocol/tests/dto_contracts.rs`
- `crates/devil-ui/src/ui.rs`

### Write Targets

- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md`

### Implementation Sequence

1. Record which protocol/UI fields later plans must populate and render.
   > verification: `rg -q "Selected proposal details" .planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md`
2. Record exact command outcomes for DTO tests, UI tests, and UI check.
   > verification: `rg -q "cargo check -p devil-ui --all-targets" .planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md`
3. Record any residual contract risk as `BLOCKED` if a required state is still not representable.
   > verification: `rg -q "Verification" .planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md`

### Edge And Error Cases

- Do not mark downstream app/desktop behavior complete here.

<verify>

```powershell
rg -q "Verification" .planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md
```

</verify>

### BLOCKED Conditions

- Any Phase 5 success criterion cannot be represented by the projection contracts.
