---
phase: "05-control-trust-and-assisted-ai-surfaces"
plan: "05-07"
title: "Phase 5 Evidence And Acceptance Gate"
wave: 7
agents: ["testing-qa-verification-specialist", "product-technical-writer"]
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06"]
requirements: ["R-010", "R-011", "R-013"]
expected_artifacts:
  - path: "plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md"
    provides: Final Phase 5 acceptance evidence with requirement traceability and gate outcomes
    required: true
  - path: ".planning/ROADMAP.md"
    provides: Roadmap marked Phase 5 complete only after acceptance gates pass
    required: true
  - path: ".planning/STATE.md"
    provides: Project state updated to Phase 5 complete and Phase 6 next only after acceptance gates pass
    required: true
  - path: ".planning/phases/05-control-trust-and-assisted-ai-surfaces/05-07-RESULT.md"
    provides: Final build result and verification record
    required: true
verification_commands:
  - "cargo run -p xtask -- check-deps"
  - "cargo fmt --all --check"
  - "cargo check --workspace --all-targets"
  - "cargo test --workspace --all-targets"
  - "cargo clippy --workspace --all-targets -- -D warnings"
  - "rg -q \"Phase 5 acceptance: Accepted\" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md"
files_modified:
  - "plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/phases/05-control-trust-and-assisted-ai-surfaces/05-07-RESULT.md"
files_forbidden:
  - "crates/"
  - "plans/dependency-policy.md"
  - "xtask/src/main.rs"
  - "Cargo.toml"
  - "Cargo.lock"
  - "plans/evidence/phase-5/plugin-architecture-map.md"
---

# Plan 05-07: Phase 5 Evidence And Acceptance Gate

## Objective

Run the final Phase 5 gates, archive acceptance evidence, and update planning state only after the control/trust/assisted-AI GUI phase is proven complete.

<execution_contract>

- read-before-write: read `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-CONTEXT.md`, `05-01-RESULT.md`, `05-02-RESULT.md`, `05-03-RESULT.md`, `05-04-RESULT.md`, `05-05-RESULT.md`, `05-06-RESULT.md`, `plans/evidence/gui-productization/phase-5-control-trust-safety.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `AGENTS.md` before editing.
- evidence-before-action: verify every prior result exists and records passing required commands before marking Phase 5 accepted.
- minimal diff: edit only final evidence and planning state files; do not change product code in this acceptance plan.
- verify-before-report: run every frontmatter command and record outcomes in `05-07-RESULT.md`.
- role: final QA/documentation gate owner.
- task: compile final evidence, run full repository gates, trace success criteria to artifacts, and update roadmap/state after proof.
- scope: final evidence document, roadmap/state status updates, result file, and full gate execution.
- allowed tools/actions: Markdown edits, cargo gates, grep checks, and planning state updates.
- forbidden actions: product-code fixes, weakening tests/lints, changing dependency policy, editing `xtask`, deleting legacy plugin evidence, or marking accepted with failing required gates.
- verification criteria: all full gates pass, final evidence says accepted, roadmap/state reflect Phase 5 completion, and no forbidden file is modified.
- final result format: write `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-07-RESULT.md` with command table, accepted evidence path, and residual risks.

</execution_contract>

<stop_gates>

- BLOCKED if any prior plan result is missing or reports unresolved `BLOCKED`, `FAILED`, or `REWORK`.
- BLOCKED if any frontmatter verification command fails.
- BLOCKED if final evidence cannot trace every Phase 5 roadmap success criterion to tests, code paths, or command results.
- BLOCKED if accepting Phase 5 would require changing product code inside this final gate.
- BLOCKED if legacy plugin Phase 5 evidence would be overwritten.

</stop_gates>

<recovery>

- If a broad cargo gate fails, do not update roadmap/state to complete. Record the failure, owner, and rerun command in `05-07-RESULT.md`.
- If an evidence file is missing details, update only the final evidence with observed command output and source/result references; do not invent earlier results.
- If a failure is unrelated to Phase 5, document the evidence and rerun narrow Phase 5 gates, but keep final acceptance blocked until the full required gate policy is satisfied or the project owner changes the gate.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] Prior plan result files are present and unblocked.
- [ ] `cargo run -p xtask -- check-deps` passes.
- [ ] `cargo fmt --all --check` passes.
- [ ] `cargo check --workspace --all-targets` passes.
- [ ] `cargo test --workspace --all-targets` passes.
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes.
- [ ] Final evidence is accepted and roadmap/state are updated.

</verification>

<success_criteria>

- Final evidence covers proposal ledger, details, diff/target summary, approval checklist, rollback/checkpoint, context manifest, privacy inspector, permission/risk/cost budgets, assisted-AI explain/propose, refusal/redaction visibility, and all lifecycle states.
- Final evidence states that AI-generated edits are proposals only and never self-applied.
- Final evidence states that UI and desktop remain projection/intent layers.
- `.planning/ROADMAP.md` and `.planning/STATE.md` are updated only after required gates pass.

</success_criteria>

<output>

Write `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-07-RESULT.md`.

</output>

## Task 1: Validate Prior Results And Requirement Traceability

### Read Targets

- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-01-RESULT.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-02-RESULT.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-03-RESULT.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-04-RESULT.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-05-RESULT.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-06-RESULT.md`
- `plans/evidence/gui-productization/phase-5-control-trust-safety.md`

### Write Targets

- `plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`

### Implementation Sequence

1. Verify each prior result file exists and has no unresolved blocker markers. Use a command that exits zero only when no blocker markers are found.
   > verification: `if (Get-ChildItem .planning/phases/05-control-trust-and-assisted-ai-surfaces -Filter '*-RESULT.md' | Select-String -Pattern 'BLOCKED|REWORK|FAILED') { exit 1 } else { exit 0 }`
2. Build a requirement traceability section mapping every Phase 5 success criterion to concrete tests, commands, source artifacts, or prior result files.
   > verification: `rg -q "Requirement traceability" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`
3. Keep acceptance status as not accepted until the full gate commands in Task 2 pass.
   > verification: `rg -q "Phase 5 acceptance" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`

### Edge And Error Cases

- If `rg` finds unresolved blockers in prior results, stop and mark this plan `BLOCKED`.
- Do not repeat raw command logs in full; summarize pass/fail and cite result files.
- Do not modify legacy plugin evidence.

<verify>

```powershell
rg -q "Requirement traceability" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md
```

</verify>

### BLOCKED Conditions

- Any prior result file is missing or unresolved.

## Task 2: Run Full Acceptance Gates

### Read Targets

- `AGENTS.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-CONTEXT.md`
- command output from repository gates

### Write Targets

- `plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-07-RESULT.md`

### Implementation Sequence

1. Run the required phase gates in this order: `cargo run -p xtask -- check-deps`, `cargo fmt --all --check`, `cargo check --workspace --all-targets`, `cargo test --workspace --all-targets`, and `cargo clippy --workspace --all-targets -- -D warnings`.
   > verification: each command exits 0
2. Record exact commands and pass/fail outcomes in final evidence and `05-07-RESULT.md`.
   > verification: `rg -q "cargo clippy --workspace --all-targets -- -D warnings" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`
3. If all commands pass and traceability is complete, set final evidence to `Phase 5 acceptance: Accepted.`.
   > verification: `rg -q "Phase 5 acceptance: Accepted" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`

### Edge And Error Cases

- Do not continue to roadmap/state completion if any command fails.
- If a gate fails due to an unrelated issue, document the evidence and keep acceptance blocked.
- Keep command summaries reproducible.

<verify>

```powershell
cargo run -p xtask -- check-deps
cargo fmt --all --check
cargo check --workspace --all-targets
cargo test --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
```

</verify>

### BLOCKED Conditions

- Any full acceptance gate fails.

## Task 3: Update Roadmap And State After Acceptance

### Read Targets

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`

### Write Targets

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-07-RESULT.md`

### Implementation Sequence

1. Update `.planning/ROADMAP.md` to mark Phase 5 complete only after final evidence says accepted.
   > verification: `rg -q "Phase 5.*Complete" .planning/ROADMAP.md`
2. Update `.planning/STATE.md` current position, progress count, Phase 5 result list, recent decisions, and next action for Phase 6 planning.
   > verification: `rg -q "Phase 5 complete" .planning/STATE.md`
3. Write the final `05-07-RESULT.md` with changed files, command table, evidence path, and residual risks.
   > verification: `rg -q "Verification" .planning/phases/05-control-trust-and-assisted-ai-surfaces/05-07-RESULT.md`

### Edge And Error Cases

- Do not change state to complete on partial evidence.
- Do not update product code from this final gate.
- Preserve previous phase history in state.

<verify>

```powershell
rg -q "Phase 5 acceptance: Accepted" plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md
rg -q "Phase 5 complete" .planning/STATE.md
```

</verify>

### BLOCKED Conditions

- Final evidence is not accepted.
