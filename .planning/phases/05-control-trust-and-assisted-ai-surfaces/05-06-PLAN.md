---
phase: "05-control-trust-and-assisted-ai-surfaces"
plan: "05-06"
title: "Control Surface Safety Regression Suite"
wave: 6
agents: ["testing-qa-verification-specialist", "engineering-security-engineer", "testing-test-results-analyzer"]
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
requirements: ["R-010", "R-011", "R-013"]
expected_artifacts:
  - path: "crates/devil-app/tests/control_trust_surfaces.rs"
    provides: End-to-end app regression coverage for proposal lifecycle, trust projections, and assisted-AI proposal-only behavior
    required: true
  - path: "crates/devil-desktop/tests/control_trust_view.rs"
    provides: Desktop projection rendering assertions for detailed control/trust/assistant states
    required: true
  - path: "crates/devil-desktop/tests/control_trust_bridge.rs"
    provides: Desktop bridge/workflow action assertions for proposal and assisted-AI controls
    required: true
  - path: "plans/evidence/gui-productization/phase-5-control-trust-safety.md"
    provides: Safety evidence for proposal-only AI, metadata-only trust data, default-deny routing, and no UI/desktop authority creep
    required: true
  - path: ".planning/phases/05-control-trust-and-assisted-ai-surfaces/05-06-RESULT.md"
    provides: Plan execution record with decisions and command results
    required: true
verification_commands:
  - "cargo test -p devil-app --test control_trust_surfaces -- --nocapture"
  - "cargo test -p devil-desktop --test control_trust_view -- --nocapture"
  - "cargo test -p devil-desktop --test control_trust_bridge -- --nocapture"
  - "cargo test -p devil-ui control_trust -- --nocapture"
  - "rg -q \"No UI or desktop authority creep\" plans/evidence/gui-productization/phase-5-control-trust-safety.md"
files_modified:
  - "crates/devil-app/tests/control_trust_surfaces.rs"
  - "crates/devil-desktop/tests/control_trust_view.rs"
  - "crates/devil-desktop/tests/control_trust_bridge.rs"
  - "plans/evidence/gui-productization/phase-5-control-trust-safety.md"
  - ".planning/phases/05-control-trust-and-assisted-ai-surfaces/05-06-RESULT.md"
files_forbidden:
  - "crates/devil-app/src/lib.rs"
  - "crates/devil-desktop/src/view.rs"
  - "crates/devil-desktop/src/bridge.rs"
  - "crates/devil-desktop/src/workflow.rs"
  - "crates/devil-protocol/src/lib.rs"
  - "crates/devil-ui/src/ui.rs"
  - "xtask/src/main.rs"
  - "plans/dependency-policy.md"
  - "Cargo.toml"
  - "Cargo.lock"
---

# Plan 05-06: Control Surface Safety Regression Suite

## Objective

Prove the Phase 5 control/trust/assisted-AI implementation meets the safety contract before final acceptance.

<execution_contract>

- read-before-write: read `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-CONTEXT.md`, `05-01-RESULT.md`, `05-02-RESULT.md`, `05-03-RESULT.md`, `05-04-RESULT.md`, `05-05-RESULT.md`, `AGENTS.md`, and the listed test files before editing.
- evidence-before-action: verify prior implementation result files exist and have no unresolved blockers before adding safety evidence.
- minimal diff: add or extend focused regression tests and safety evidence only; do not patch product code in this plan.
- verify-before-report: run every frontmatter command and record outcomes in `05-06-RESULT.md`.
- role: safety QA and security evidence owner.
- task: prove proposal lifecycle visibility, AI proposal-only behavior, local/default-deny routing, metadata-only trust data, and UI/desktop boundary preservation.
- scope: focused app/desktop/UI tests, safety evidence document, and result file.
- allowed tools/actions: test edits, evidence Markdown, targeted cargo test commands, grep checks.
- forbidden actions: product-code fixes, dependency changes, weakening tests/lints, changing governance, or marking final acceptance.
- verification criteria: all targeted safety tests pass and evidence maps every Phase 5 safety claim to commands/source artifacts.
- final result format: write `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-06-RESULT.md` with changed files, command outcomes, and any `BLOCKED` items.

</execution_contract>

<stop_gates>

- BLOCKED if any prior result file is missing or reports unresolved `BLOCKED`, `FAILED`, or `REWORK`.
- BLOCKED if targeted safety tests fail and fixing them would require product-code changes; report the failing command and owner plan instead.
- BLOCKED if evidence cannot prove AI proposals are not self-applied.
- BLOCKED if UI or desktop gains app/editor/workspace/provider/storage authority.

</stop_gates>

<recovery>

- If a safety test exposes a product bug, stop and report `BLOCKED`; do not fix product code in this test/evidence plan.
- If broad tests are too expensive here, keep targeted commands exact and leave broad gates to 05-07.
- If evidence lacks command output, rerun targeted commands and update evidence with pass/fail summaries.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] App tests cover proposal lifecycle commands and visible states.
- [ ] App tests cover AI explain/propose, visible refusals, and proposal-only edit output.
- [ ] Desktop tests cover detailed control/trust rendering and action translation.
- [ ] UI tests cover projection-only command intent behavior.
- [ ] Safety evidence traces every claim to commands or source paths.

</verification>

<success_criteria>

- Phase 5 has targeted regression proof before final gates.
- Safety evidence is ready for final acceptance evidence to cite.
- No product code is modified in this verification wave.

</success_criteria>

<output>

Write `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-06-RESULT.md`.

</output>

## Task 1: Verify Proposal Lifecycle And Detail States

### Read Targets

- `crates/devil-app/tests/control_trust_surfaces.rs`
- `crates/devil-app/tests/workspace_vfs_integration.rs`
- `05-03-RESULT.md`

### Write Targets

- `crates/devil-app/tests/control_trust_surfaces.rs`

### Implementation Sequence

1. Add or tighten tests for preview, approve, reject, cancel, apply, rollback, stale, conflict, failed, and rolled-back lifecycle visibility in shell projections.
   > verification: `cargo test -p devil-app --test control_trust_surfaces proposal_states_visible -- --nocapture`
2. Add assertions that dirty text is preserved on rejected/stale/conflict outcomes and that failed lifecycle states stay visible.
   > verification: `cargo test -p devil-app --test control_trust_surfaces dirty_text_preserved -- --nocapture`
3. Add assertions that proposal details include diff/target summary, privacy/risk labels, approval gates, and rollback/checkpoint metadata.
   > verification: `cargo test -p devil-app --test control_trust_surfaces proposal_details -- --nocapture`

### Edge And Error Cases

- Do not skip existing failing tests.
- Do not weaken workspace VFS assertions.

<verify>

```powershell
cargo test -p devil-app --test control_trust_surfaces proposal_states_visible -- --nocapture
cargo test -p devil-app --test control_trust_surfaces dirty_text_preserved -- --nocapture
cargo test -p devil-app --test control_trust_surfaces proposal_details -- --nocapture
```

</verify>

### BLOCKED Conditions

- Lifecycle visibility requires product-code changes not already delivered by 05-03.

## Task 2: Verify Assisted AI And Boundary Safety

### Read Targets

- `crates/devil-app/tests/control_trust_surfaces.rs`
- `crates/devil-desktop/tests/control_trust_view.rs`
- `crates/devil-desktop/tests/control_trust_bridge.rs`
- `crates/devil-ui/src/ui.rs`

### Write Targets

- `crates/devil-app/tests/control_trust_surfaces.rs`
- `crates/devil-desktop/tests/control_trust_view.rs`
- `crates/devil-desktop/tests/control_trust_bridge.rs`

### Implementation Sequence

1. Add or tighten tests proving assisted-AI propose output creates a proposal and leaves active buffer/disk unchanged.
   > verification: `cargo test -p devil-app --test control_trust_surfaces assisted_ai_propose_is_proposal_only -- --nocapture`
2. Add or tighten tests proving explain/refusal/redaction/budget/provider-denied states render through shell and desktop projections.
   > verification: `cargo test -p devil-app --test control_trust_surfaces assisted_ai -- --nocapture`
3. Add or tighten desktop bridge/view tests proving proposal/AI controls translate into intents and do not own app/provider state.
   > verification: `cargo test -p devil-desktop --test control_trust_bridge -- --nocapture; cargo test -p devil-desktop --test control_trust_view -- --nocapture`

### Edge And Error Cases

- Default-deny provider refusal must not be treated as a successful provider call.
- Tests must not require cloud credentials or network egress.

<verify>

```powershell
cargo test -p devil-app --test control_trust_surfaces assisted_ai -- --nocapture
cargo test -p devil-desktop --test control_trust_bridge -- --nocapture
cargo test -p devil-desktop --test control_trust_view -- --nocapture
```

</verify>

### BLOCKED Conditions

- Assisted-AI refusal/proposal-only behavior is not observable through projections.

## Task 3: Archive Safety Evidence

### Read Targets

- Prior result files `05-01-RESULT.md` through `05-05-RESULT.md`
- Targeted command output from Tasks 1 and 2
- `AGENTS.md`

### Write Targets

- `plans/evidence/gui-productization/phase-5-control-trust-safety.md`
- `.planning/phases/05-control-trust-and-assisted-ai-surfaces/05-06-RESULT.md`

### Implementation Sequence

1. Create safety evidence with sections for proposal lifecycle, assisted-AI proposal-only behavior, local/default-deny routing, metadata-only trust data, and no UI or desktop authority creep.
   > verification: `rg -q "No UI or desktop authority creep" plans/evidence/gui-productization/phase-5-control-trust-safety.md`
2. Include exact targeted commands and pass/fail outcomes without dumping raw logs.
   > verification: `rg -q "cargo test -p devil-app --test control_trust_surfaces" plans/evidence/gui-productization/phase-5-control-trust-safety.md`
3. Write `05-06-RESULT.md` with evidence path, command table, and residual risk.
   > verification: `rg -q "Verification" .planning/phases/05-control-trust-and-assisted-ai-surfaces/05-06-RESULT.md`

### Edge And Error Cases

- Evidence remains "safety evidence", not final acceptance.
- If any targeted command fails, record blocker and do not claim pass.

<verify>

```powershell
rg -q "No UI or desktop authority creep" plans/evidence/gui-productization/phase-5-control-trust-safety.md
rg -q "Verification" .planning/phases/05-control-trust-and-assisted-ai-surfaces/05-06-RESULT.md
```

</verify>

### BLOCKED Conditions

- Safety evidence cannot trace claims to passing targeted commands.
