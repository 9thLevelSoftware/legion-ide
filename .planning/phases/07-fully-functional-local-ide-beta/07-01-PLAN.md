---
phase: "07-fully-functional-local-ide-beta"
plan: "07-01"
title: "GUI Phase 7 Governance And Evidence Gate"
wave: 1
agents: ["project-manager-senior", "engineering-infrastructure-devops"]
depends_on: []
requirements: ["R-014"]
expected_artifacts:
  - path: "plans/dependency-policy.md"
    provides: Compatibility note distinguishing GUI Phase 7 local beta from legacy remote Phase 7 evidence
    required: true
  - path: "xtask/src/main.rs"
    provides: GUI Phase 7 evidence validation path that preserves existing remote Phase 7 checks
    required: true
  - path: "crates/devil-cli/src/main.rs"
    provides: `devil-cli evidence check --phase gui-phase7` gate for local beta artifacts
    required: true
  - path: "plans/evidence/gui-productization/phase-7-local-ide-beta.md"
    provides: Not-accepted GUI Phase 7 evidence stub with required artifact inventory and final checklist
    required: true
  - path: ".planning/phases/07-fully-functional-local-ide-beta/07-01-RESULT.md"
    provides: Execution record with command results, decisions, and blockers
    required: true
verification_commands:
  - "rg -q \"GUI Phase 7\" plans/dependency-policy.md"
  - "rg -q \"DEFAULT_GUI_PHASE7_EVIDENCE_PATH\" xtask/src/main.rs"
  - "rg -q \"GuiPhase7\" crates/devil-cli/src/main.rs"
  - "rg -q \"Phase 7 acceptance: Not accepted\" plans/evidence/gui-productization/phase-7-local-ide-beta.md"
  - "cargo test -p xtask gui_phase7 gui_phase6 phase7 -- --nocapture"
  - "cargo test -p devil-cli gui_phase7 gui_phase6 -- --nocapture"
  - "cargo run -p xtask -- check-deps"
  - "cargo run -p devil-cli -- evidence check --phase gui-phase7"
files_modified:
  - "plans/dependency-policy.md"
  - "xtask/src/main.rs"
  - "crates/devil-cli/src/main.rs"
  - "plans/evidence/gui-productization/phase-7-local-ide-beta.md"
  - ".planning/phases/07-fully-functional-local-ide-beta/07-01-RESULT.md"
files_forbidden:
  - "plans/evidence/phase-7/"
  - "crates/devil-desktop/"
  - "crates/devil-ui/"
  - "crates/devil-app/"
  - "Cargo.toml"
  - "Cargo.lock"
  - ".github/workflows/ci.yml"
  - "scripts/"
---

# Plan 07-01: GUI Phase 7 Governance And Evidence Gate

## Objective

Create a distinct GUI Phase 7 local-beta acceptance path while preserving the already accepted legacy Phase 7 remote-development evidence.

<execution_contract>

- read-before-write: read `07-CONTEXT.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `plans/dependency-policy.md`, `xtask/src/main.rs`, `crates/devil-cli/src/main.rs`, `plans/evidence/phase-7/remote-architecture-map.md`, `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`, and `AGENTS.md` before editing.
- evidence-before-action: confirm that legacy Phase 7 evidence is remote-development evidence and that GUI Phase 6 already has a separate productization evidence pattern before adding GUI Phase 7.
- minimal diff: edit only governance/evidence-gate files and this plan result; do not touch desktop implementation in this plan.
- verify-before-report: run every frontmatter command and record outcomes in `07-01-RESULT.md`.
- role: governance and beta-readiness implementer.
- task: add a GUI Phase 7 local-beta evidence gate that can remain not accepted during implementation and later enforce final beta artifacts.
- scope: dependency policy, `xtask` constants/helpers/tests, `devil-cli` evidence enum/helpers/tests, GUI Phase 7 evidence stub, and plan result.
- allowed tools/actions: focused Markdown/Rust edits, `rg`, targeted Rust tests, `cargo run -p xtask -- check-deps`, and `cargo run -p devil-cli -- evidence check --phase gui-phase7`.
- forbidden actions: deleting or weakening legacy Phase 7 remote evidence, modifying desktop/app/UI product code, adding dependencies, or marking GUI Phase 7 accepted before beta workflow evidence exists.
- verification criteria: legacy Phase 7 checks still pass; GUI Phase 7 evidence can be not accepted without blocking `xtask`; accepted GUI Phase 7 claims require checked checklist, artifacts, command markers, and unsupported-surface limitation text.
- final result format: write `.planning/phases/07-fully-functional-local-ide-beta/07-01-RESULT.md` with changed files, command outcomes, and any `BLOCKED` items.

</execution_contract>

<stop_gates>

- BLOCKED if `xtask check-deps` cannot validate both legacy remote Phase 7 and GUI local-beta Phase 7 without ambiguous status markers.
- BLOCKED if implementing the GUI Phase 7 gate requires editing or deleting files under `plans/evidence/phase-7/`.
- BLOCKED if the dependency policy would authorize `devil-ui` or `devil-desktop` to own editor text, workspace mutation, proposal lifecycle state, terminal runtime authority, provider routing, storage authority, or security policy.
- BLOCKED if GUI Phase 7 evidence would have to be marked accepted before beta workflow, operational health, docs, limitation, smoke, and final-gate evidence exists.

</stop_gates>

<recovery>

- If `xtask` tests fail after adding GUI Phase 7 validation, preserve legacy `DEFAULT_PHASE7_EVIDENCE_PATH` and add a separate `DEFAULT_GUI_PHASE7_EVIDENCE_PATH` branch rather than replacing remote checks.
- If `devil-cli` value-enum parsing fails, mirror the existing `GuiPhase6` style exactly and add only `GuiPhase7`.
- If the evidence stub conflicts with existing evidence names, keep GUI local-beta evidence under `plans/evidence/gui-productization/` and document the collision in `07-01-RESULT.md`.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] `plans/dependency-policy.md` explicitly mentions GUI Phase 7 local-beta boundaries and unsupported advanced-surface limitations.
- [ ] `xtask/src/main.rs` has a GUI Phase 7 evidence path separate from `DEFAULT_PHASE7_EVIDENCE_PATH`.
- [ ] `crates/devil-cli/src/main.rs` accepts `--phase gui-phase7`.
- [ ] `plans/evidence/gui-productization/phase-7-local-ide-beta.md` exists and says `Phase 7 acceptance: Not accepted.`
- [ ] `cargo test -p xtask gui_phase7 gui_phase6 phase7 -- --nocapture` passes.
- [ ] `cargo test -p devil-cli gui_phase7 gui_phase6 -- --nocapture` passes.
- [ ] `cargo run -p xtask -- check-deps` passes.
- [ ] `cargo run -p devil-cli -- evidence check --phase gui-phase7` passes.
- [ ] No forbidden file is modified.

</verification>

<success_criteria>

- GUI Phase 7 has a distinct not-accepted evidence stub.
- Legacy accepted remote Phase 7 evidence remains untouched.
- Future accepted GUI Phase 7 claims require beta workflow artifacts, operational health evidence, launch docs, known limitations, plan results, and command markers.
- Product-code plans can depend on a clear final evidence contract.

</success_criteria>

<output>

Write `.planning/phases/07-fully-functional-local-ide-beta/07-01-RESULT.md`.

</output>

## Task 1: Add GUI Phase 7 Policy Boundary

### Read Targets

- `plans/dependency-policy.md`
- `.planning/phases/07-fully-functional-local-ide-beta/07-CONTEXT.md`
- `plans/evidence/phase-7/remote-architecture-map.md`
- `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`

### Write Targets

- `plans/dependency-policy.md`

### Implementation Sequence

1. Add a `GUI Phase 7` compatibility paragraph near the GUI Phase 6 policy note or the legacy Phase 7 remote entry. It must state that GUI Phase 7 authorizes only local-beta evidence, smoke workflows, operational health projections, privacy-safe diagnostics, launch docs, limitations, and acceptance gating.
   > verification: `rg -q "GUI Phase 7" plans/dependency-policy.md`
2. Explicitly preserve the legacy remote-development Phase 7 policy as historical substrate evidence and state that GUI Phase 7 does not alter remote acceptance.
   > verification: `rg -q "Phase 7 activates `devil-remote`" plans/dependency-policy.md`
3. State that GUI Phase 7 does not authorize plugin/collaboration/remote production claims, hosted provider activation, autonomous apply, new dependencies, or UI/desktop ownership of app/editor/workspace/proposal/storage/security/provider/terminal authority.
   > verification: `rg -q "local-beta" plans/dependency-policy.md`

### Edge And Error Cases

- Do not add new allowed internal dependencies.
- Do not loosen the renderer dependency gate or the legacy remote gate.
- If existing policy text is stale or contradictory, add a compatibility note instead of deleting accepted substrate policy.

<verify>

```powershell
rg -q "GUI Phase 7" plans/dependency-policy.md
rg -q "Phase 7 activates `devil-remote`" plans/dependency-policy.md
rg -q "local-beta" plans/dependency-policy.md
```

</verify>

### BLOCKED Conditions

- The policy cannot distinguish GUI Phase 7 local beta from legacy remote Phase 7 without weakening accepted evidence.

## Task 2: Add Xtask GUI Phase 7 Evidence Validation

### Read Targets

- `xtask/src/main.rs`
- `plans/evidence/phase-7/remote-architecture-map.md`
- `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`

### Write Targets

- `xtask/src/main.rs`

### Implementation Sequence

1. Add `DEFAULT_GUI_PHASE7_EVIDENCE_PATH` with value `plans/evidence/gui-productization/phase-7-local-ide-beta.md`.
   > verification: `rg -q "DEFAULT_GUI_PHASE7_EVIDENCE_PATH" xtask/src/main.rs`
2. Add `GUI_PHASE7_REQUIRED_ARTIFACTS` and `GUI_PHASE7_REQUIRED_COMMAND_MARKERS` for beta workflow smoke, operational health diagnostics, launch runbook, known limitations, release-readiness checklist, manual evidence, plan result files `07-01` through `07-05`, smoke scripts, CLI evidence check, and the standard phase gates.
   > verification: `rg -q "GUI_PHASE7_REQUIRED_ARTIFACTS" xtask/src/main.rs`
3. Add validation helpers and tests mirroring GUI Phase 6 behavior: not-accepted status passes without artifacts, conflicting markers fail, accepted status requires checked checklist, required artifacts, required command markers, and limitation markers for unsupported advanced surfaces. Preserve all existing legacy Phase 7 remote tests.
   > verification: `cargo test -p xtask gui_phase7 gui_phase6 phase7 -- --nocapture`

### Edge And Error Cases

- Accepted GUI Phase 7 evidence must fail if any final checklist item is unchecked.
- Accepted GUI Phase 7 evidence must fail if it lacks explicit limitations for unsupported remote, collaboration, plugin, hosted provider, signed installer, cross-platform parity, and autonomous apply behavior.
- The legacy `DEFAULT_PHASE7_EVIDENCE_PATH` must still point at `plans/evidence/phase-7/remote-architecture-map.md`.

<verify>

```powershell
rg -q "DEFAULT_GUI_PHASE7_EVIDENCE_PATH" xtask/src/main.rs
rg -q "GUI_PHASE7_REQUIRED_ARTIFACTS" xtask/src/main.rs
cargo test -p xtask gui_phase7 gui_phase6 phase7 -- --nocapture
```

</verify>

### BLOCKED Conditions

- `xtask` cannot validate GUI Phase 7 without changing legacy remote Phase 7 semantics.

## Task 3: Add Devil CLI GUI Phase 7 Evidence Check And Stub

### Read Targets

- `crates/devil-cli/src/main.rs`
- `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`
- `.planning/ROADMAP.md`

### Write Targets

- `crates/devil-cli/src/main.rs`
- `plans/evidence/gui-productization/phase-7-local-ide-beta.md`
- `.planning/phases/07-fully-functional-local-ide-beta/07-01-RESULT.md`

### Implementation Sequence

1. Add `GuiPhase7` to `EvidencePhase` and route it to `check_gui_phase7_evidence`, following the `GuiPhase6` parser/test pattern.
   > verification: `rg -q "GuiPhase7" crates/devil-cli/src/main.rs`
2. Add GUI Phase 7 constants and `check_gui_phase7_evidence` that accepts not-accepted scaffold mode and enforces artifacts, command markers, checked checklist, and limitation markers when accepted.
   > verification: `cargo test -p devil-cli gui_phase7 gui_phase6 -- --nocapture`
3. Create `plans/evidence/gui-productization/phase-7-local-ide-beta.md` with sections `## Acceptance Status`, `## Scope`, `## Required Artifacts`, `## Required Commands`, `## Known Limitations Required For Acceptance`, `## Final Validation Checklist`, and `## Residual Risks`. Set status exactly to `Phase 7 acceptance: Not accepted.`
   > verification: `rg -q "Phase 7 acceptance: Not accepted" plans/evidence/gui-productization/phase-7-local-ide-beta.md`

### Edge And Error Cases

- Do not mark GUI Phase 7 accepted in this plan.
- Do not copy legacy remote Phase 7 artifacts into the GUI Phase 7 artifact list.
- Do not include command logs in the scaffold.

<verify>

```powershell
rg -q "GuiPhase7" crates/devil-cli/src/main.rs
cargo test -p devil-cli gui_phase7 gui_phase6 -- --nocapture
rg -q "Phase 7 acceptance: Not accepted" plans/evidence/gui-productization/phase-7-local-ide-beta.md
cargo run -p devil-cli -- evidence check --phase gui-phase7
```

</verify>

### BLOCKED Conditions

- The not-accepted GUI Phase 7 stub cannot pass `xtask check-deps` and `devil-cli evidence check --phase gui-phase7`.
