---
phase: "07-fully-functional-local-ide-beta"
plan: "07-05"
title: "Phase 7 Evidence Capture And Acceptance Gate"
wave: 5
agents: ["project-management-project-shepherd", "testing-test-results-analyzer", "product-technical-writer"]
depends_on: ["07-01", "07-02", "07-03", "07-04"]
requirements: ["R-014"]
expected_artifacts:
  - path: "plans/evidence/gui-productization/phase-7-local-ide-beta.md"
    provides: Accepted GUI Phase 7 evidence with complete checklist, artifacts, command markers, limitations, and residual risks
    required: true
  - path: ".planning/ROADMAP.md"
    provides: Phase 7 progress updated only after acceptance evidence passes
    required: true
  - path: ".planning/STATE.md"
    provides: Project state advanced to Phase 8 planning only after final gates pass
    required: true
  - path: ".planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md"
    provides: Final acceptance record with full gate outcomes and go/no-go rationale
    required: true
verification_commands:
  - "cargo run -p xtask -- check-deps"
  - "cargo fmt --all --check"
  - "cargo check --workspace --all-targets"
  - "cargo test --workspace --all-targets"
  - "cargo clippy --workspace --all-targets -- -D warnings"
  - "cargo deny check"
  - "cargo test -p devil-desktop --test beta_workflow -- --nocapture"
  - "cargo test -p devil-desktop --test operational_health -- --nocapture"
  - "cargo test -p devil-desktop --test diagnostics_export -- --nocapture"
  - "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/gui-smoke.ps1 -Beta -DryRun"
  - "bash scripts/gui-smoke.sh --beta --dry-run"
  - "cargo run -p devil-cli -- evidence check --phase gui-phase7"
  - "rg -q \"Phase 7 acceptance: Accepted\" plans/evidence/gui-productization/phase-7-local-ide-beta.md"
files_modified:
  - "plans/evidence/gui-productization/phase-7-local-ide-beta.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md"
files_forbidden:
  - "crates/"
  - "scripts/"
  - ".github/workflows/ci.yml"
  - "xtask/src/main.rs"
  - "crates/devil-cli/src/main.rs"
  - "plans/evidence/phase-7/"
  - "Cargo.toml"
  - "Cargo.lock"
---

# Plan 07-05: Phase 7 Evidence Capture And Acceptance Gate

## Objective

Accept GUI Phase 7 local IDE beta only after workflow smoke, operational health, diagnostics, launch docs, known limitations, release readiness, and full repository gates are evidenced.

<execution_contract>

- read-before-write: read `07-CONTEXT.md`, `07-01-RESULT.md` through `07-04-RESULT.md`, all GUI Phase 7 evidence files, `.planning/ROADMAP.md`, `.planning/STATE.md`, `plans/evidence/gui-productization/phase-7-local-ide-beta.md`, and `AGENTS.md` before editing.
- evidence-before-action: confirm every required GUI Phase 7 artifact exists and every required command has a recorded pass or a documented blocker before changing acceptance status.
- minimal diff: edit only the main GUI Phase 7 evidence file, planning state/roadmap, and final result.
- verify-before-report: run all frontmatter commands and record exact outcomes in `07-05-RESULT.md`.
- role: acceptance-gate coordinator, test-results analyst, and beta documentation verifier.
- task: make a final evidence-gated go/no-go decision for GUI Phase 7 local IDE beta.
- scope: acceptance evidence, planning state, roadmap progress, and final result.
- allowed tools/actions: running repository gates, evidence checks, smoke dry runs, Markdown state edits, and `rg`.
- forbidden actions: product code changes, script/CI fixes, manifest changes, legacy Phase 7 evidence changes, or acceptance without full evidence.
- verification criteria: all required gates pass; GUI Phase 7 evidence says accepted only if checklist is checked and artifacts/commands/limitations are complete; state advances to Phase 8 next action.
- final result format: write `.planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md` with command table, acceptance decision, changed files, residual risks, and any blockers.

</execution_contract>

<stop_gates>

- BLOCKED if any required Phase 7 plan result `07-01` through `07-04` is missing or reports unresolved blockers.
- BLOCKED if any full repository gate fails and the failure is not fixed within scope.
- BLOCKED if `cargo run -p devil-cli -- evidence check --phase gui-phase7` or `cargo run -p xtask -- check-deps` fails.
- BLOCKED if evidence contains unresolved `Phase 7 acceptance: Not accepted.`, unchecked checklist items, missing artifacts, missing command markers, or missing unsupported-surface limitation markers.
- BLOCKED if beta workflow smoke mutated real repository source files instead of the isolated beta workspace.

</stop_gates>

<recovery>

- If a final gate fails due to an implementation issue, do not edit acceptance status. Return to the owning earlier plan's scope or create a blocker with exact failing command and file reference.
- If `cargo deny check` reports warning-level duplicate output matching the repo policy baseline, record the warning and command result exactly; do not claim a clean deny output.
- If manual real-repo evidence is missing, keep Phase 7 not accepted and state the narrow command/evidence needed.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] All required GUI Phase 7 evidence artifacts exist and are cited.
- [ ] `cargo run -p xtask -- check-deps` passes.
- [ ] `cargo fmt --all --check` passes.
- [ ] `cargo check --workspace --all-targets` passes.
- [ ] `cargo test --workspace --all-targets` passes.
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes.
- [ ] `cargo deny check` passes or reports only accepted warning-level baseline output.
- [ ] Targeted beta workflow, operational health, and diagnostics tests pass.
- [ ] GUI Phase 7 smoke dry-run wrappers pass.
- [ ] `cargo run -p devil-cli -- evidence check --phase gui-phase7` passes.
- [ ] ROADMAP/STATE advance only after all acceptance gates pass.

</verification>

<success_criteria>

- GUI Phase 7 local IDE beta is accepted only with complete evidence.
- Planning state points to Phase 8 planning after acceptance.
- Remaining limitations are explicit and not hidden as completed work.

</success_criteria>

<output>

Write `.planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md`.

</output>

## Task 1: Verify Required Evidence And Main Acceptance Artifact

### Read Targets

- `plans/evidence/gui-productization/phase-7-local-ide-beta.md`
- `plans/evidence/gui-productization/phase-7-local-workflow-smoke.md`
- `plans/evidence/gui-productization/phase-7-operational-health-diagnostics.md`
- `plans/evidence/gui-productization/phase-7-launch-runbook.md`
- `plans/evidence/gui-productization/phase-7-known-limitations.md`
- `plans/evidence/gui-productization/phase-7-release-readiness.md`
- `plans/evidence/gui-productization/phase-7-manual-beta-evidence.md`
- `.planning/phases/07-fully-functional-local-ide-beta/07-01-RESULT.md` through `07-04-RESULT.md`

### Write Targets

- `plans/evidence/gui-productization/phase-7-local-ide-beta.md`

### Implementation Sequence

1. Confirm every required artifact in the GUI Phase 7 evidence stub exists and has command outcomes. Do not edit acceptance status if any are missing.
   > verification: `rg -q "phase-7-local-workflow-smoke" plans/evidence/gui-productization/phase-7-local-ide-beta.md`
2. Confirm `phase-7-known-limitations.md` contains required unsupported markers for remote production GUI, collaboration GUI, plugin management, hosted provider activation, autonomous apply, signed installer, OS accessibility inspection, and platform parity.
   > verification: `rg -q "Autonomous apply: unsupported" plans/evidence/gui-productization/phase-7-known-limitations.md`
3. If all required artifacts and commands pass, change the main evidence status to exactly `Phase 7 acceptance: Accepted.` and check every final checklist item.
   > verification: `rg -q "Phase 7 acceptance: Accepted" plans/evidence/gui-productization/phase-7-local-ide-beta.md`

### Edge And Error Cases

- Do not leave both accepted and not-accepted markers in the status section.
- Do not use evidence from `plans/evidence/phase-7/` as GUI Phase 7 proof.
- Do not accept with unchecked checklist items or missing limitation markers.

<verify>

```powershell
rg -q "phase-7-local-workflow-smoke" plans/evidence/gui-productization/phase-7-local-ide-beta.md
rg -q "Autonomous apply: unsupported" plans/evidence/gui-productization/phase-7-known-limitations.md
rg -q "Phase 7 acceptance: Accepted" plans/evidence/gui-productization/phase-7-local-ide-beta.md
```

</verify>

### BLOCKED Conditions

- Required evidence is missing, stale, blocked, or contradictory.

## Task 2: Run Final Repository Gates

### Read Targets

- `AGENTS.md`
- `plans/evidence/gui-productization/phase-7-local-ide-beta.md`
- `deny.toml`

### Write Targets

- `.planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md`

### Implementation Sequence

1. Run full repository gates in this order: `cargo run -p xtask -- check-deps`, `cargo fmt --all --check`, `cargo check --workspace --all-targets`, `cargo test --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo deny check`, targeted desktop tests, smoke dry runs, and `cargo run -p devil-cli -- evidence check --phase gui-phase7`.
   > verification: `cargo run -p xtask -- check-deps`
2. Record exact pass/fail status and notable warning text in `07-05-RESULT.md`. If any command fails, keep Phase 7 not accepted and mark `BLOCKED`.
   > verification: `test -f .planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md`
3. Record in `07-05-RESULT.md` that final acceptance edits are limited to the main evidence, ROADMAP, STATE, and final result. List any pre-existing code/script/CI diffs from earlier Phase 7 plans without reverting them.
   > verification: `rg -q "Final acceptance edits only" .planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md`

### Edge And Error Cases

- If `cargo deny check` emits warning-level duplicate output while returning success, record it as warning-level output.
- If a full gate fails due to an earlier Phase 7 implementation issue, stop and route back to the owning plan.
- If `bash scripts/gui-smoke.sh --beta --dry-run` is unavailable on a Windows host, run the PowerShell wrapper and record the POSIX wrapper as not locally runnable only if CI covers it; otherwise block acceptance.

<verify>

```powershell
cargo run -p xtask -- check-deps
cargo fmt --all --check
cargo check --workspace --all-targets
cargo test --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo deny check
cargo test -p devil-desktop --test beta_workflow -- --nocapture
cargo test -p devil-desktop --test operational_health -- --nocapture
cargo test -p devil-desktop --test diagnostics_export -- --nocapture
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/gui-smoke.ps1 -Beta -DryRun
bash scripts/gui-smoke.sh --beta --dry-run
cargo run -p devil-cli -- evidence check --phase gui-phase7
rg -q "Final acceptance edits only" .planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md
```

</verify>

### BLOCKED Conditions

- Any required gate fails or cannot be run.

## Task 3: Update Planning State After Acceptance

### Read Targets

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `plans/evidence/gui-productization/phase-7-local-ide-beta.md`
- `.planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md`

### Write Targets

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md`

### Implementation Sequence

1. Only after all gates pass, update `.planning/ROADMAP.md` Phase 7 progress to complete with the executed plan count and keep Phase 8 pending.
   > verification: `rg -q "\\| 7 \\| .*Complete" .planning/ROADMAP.md`
2. Update `.planning/STATE.md` current position to Phase 8 next, status to GUI Phase 7 accepted, and next action to plan Phase 8.
   > verification: `rg -q "Phase 8" .planning/STATE.md`
3. Finalize `07-05-RESULT.md` with acceptance decision, changed files, command results, and residual risk.
   > verification: `rg -q "Final Verdict: PASS" .planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md`

### Edge And Error Cases

- Do not update ROADMAP or STATE if final gates fail.
- Keep exact dates concrete in the result.
- Preserve unrelated user edits in planning files.

<verify>

```powershell
rg -q "\\| 7 \\| .*Complete" .planning/ROADMAP.md
rg -q "Phase 8" .planning/STATE.md
rg -q "Final Verdict: PASS" .planning/phases/07-fully-functional-local-ide-beta/07-05-RESULT.md
```

</verify>

### BLOCKED Conditions

- Acceptance evidence has not passed all final gates.
