---
phase: "08-advanced-platform-gui-ga"
plan: "08-07"
title: "Phase 8 GUI Evidence Capture And Acceptance Gate"
wave: 7
agents: ["testing-qa-verification-specialist", "engineering-infrastructure-devops"]
depends_on: ["08-06"]
requirements: ["R-015"]
expected_artifacts:
  - path: "plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md"
    provides: Final GUI Phase 8 acceptance evidence with checked checklist, command outcomes, artifact inventory, supported markers, and residual risks
    required: true
  - path: "plans/evidence/gui-productization/phase-8-final-gates.md"
    provides: Full gate transcript summary for targeted tests, repository gates, smoke scripts, CLI/xtask evidence checks, and platform parity validation
    required: true
  - path: ".planning/ROADMAP.md"
    provides: Phase 8 status updated only after acceptance gates pass
    required: true
  - path: ".planning/STATE.md"
    provides: Project state updated with Phase 8 acceptance or explicit blockers
    required: true
  - path: ".planning/phases/08-advanced-platform-gui-ga/08-07-RESULT.md"
    provides: Execution record with command results, acceptance decision, and blockers
    required: true
verification_commands:
  - "cargo test -p devil-desktop plugin_management collaboration_gui remote_workspace_gui delegated_task_command_center -- --nocapture"
  - "cargo test -p devil-app plugin collaboration remote -- --nocapture"
  - "cargo test -p devil-cli gui_phase8 gui_phase7 phase8 -- --nocapture"
  - "cargo test -p xtask gui_phase8 gui_phase7 phase8 -- --nocapture"
  - "cargo run -p devil-cli -- evidence check --phase gui-phase8"
  - "cargo run -p xtask -- check-deps"
  - "cargo fmt --all --check"
  - "cargo check --workspace --all-targets"
  - "cargo test --workspace --all-targets"
  - "cargo clippy --workspace --all-targets -- -D warnings"
  - "cargo deny check"
files_modified:
  - "plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md"
  - "plans/evidence/gui-productization/phase-8-final-gates.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/phases/08-advanced-platform-gui-ga/08-07-RESULT.md"
files_forbidden:
  - "plans/evidence/phase-8/"
  - "crates/"
  - "Cargo.toml"
  - "Cargo.lock"
  - ".github/workflows/ci.yml"
  - "scripts/"
---

# Plan 08-07: Phase 8 GUI Evidence Capture And Acceptance Gate

## Objective

Run the final GUI Phase 8 acceptance gate, archive evidence, and update roadmap/state only when advanced GUI GA proof is complete.

<execution_contract>

- read-before-write: read `08-CONTEXT.md`, `08-01-RESULT.md`, `08-02-RESULT.md`, `08-03-RESULT.md`, `08-04-RESULT.md`, `08-05-RESULT.md`, `08-06-RESULT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md`, every Phase 8 GUI evidence artifact, `plans/evidence/phase-8/phase-8-architecture-map.md`, and `AGENTS.md` before editing.
- evidence-before-action: confirm every prior Phase 8 plan result exists and has no unresolved `BLOCKED` items before marking GUI Phase 8 accepted.
- minimal diff: edit only final evidence, final gate transcript, roadmap/state acceptance metadata, and this plan result.
- verify-before-report: run every frontmatter command and record outcomes in `08-07-RESULT.md`; do not summarize a gate as passed unless the command was run and passed.
- role: final acceptance verifier.
- task: decide whether GUI Phase 8 is accepted, blocked, or still incomplete based on evidence and command results.
- scope: final evidence inventory, command transcript summary, GUI Phase 8 acceptance status, roadmap/state update, and result record.
- allowed tools/actions: Markdown/planning edits, targeted and full cargo gates, CLI/xtask evidence checks, smoke command checks, `rg`, and `cargo deny check`.
- forbidden actions: product-code changes, evidence backfilling without proof, accepting missing platform parity, editing legacy Phase 8 substrate evidence, weakening tests/gates, deleting failed evidence, or marking acceptance with unresolved blockers.
- verification criteria: all required Phase 8 GUI artifacts exist; all supported advanced-surface markers are present; platform parity markers are present; all required commands pass or acceptance stays blocked with exact failures.
- final result format: write `.planning/phases/08-advanced-platform-gui-ga/08-07-RESULT.md` with changed files, command outcomes, acceptance decision, and any `BLOCKED` items.

</execution_contract>

<stop_gates>

- BLOCKED if any required Phase 8 GUI plan result file is missing or contains unresolved `BLOCKED` items.
- BLOCKED if any required evidence artifact is missing.
- BLOCKED if plugin, collaboration, remote, delegated task, operations, smoke, CI, or platform parity evidence is incomplete.
- BLOCKED if the final evidence still contains stale unsupported labels for plugin management GUI, collaboration GUI, remote workspace GUI, or cross-platform parity while claiming acceptance.
- BLOCKED if any frontmatter verification command fails and the failure is causally related to Phase 8 acceptance.
- BLOCKED if `cargo deny check` reports a policy violation beyond the known warning-level duplicate baseline documented by the repo.

</stop_gates>

<recovery>

- If a targeted test name changed during implementation, inspect test lists with `cargo test -p <crate> -- --list`, update the final evidence with the exact replacement command, and do not erase the original failure.
- If a full repository gate fails due to an unrelated pre-existing issue, record exact evidence and decide whether it blocks acceptance; fix it only if it blocks Phase 8 verification or is causally related.
- If platform parity proof is incomplete, leave `.planning/ROADMAP.md` Phase 8 unchecked and `.planning/STATE.md` blocked with the exact missing platform evidence.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun incomplete verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] `08-01-RESULT.md` through `08-06-RESULT.md` exist and contain no unresolved `BLOCKED` items.
- [ ] Required evidence artifacts for plugin, collaboration, remote, delegated task, operations, smoke, platform parity, and final gates exist.
- [ ] Final GUI Phase 8 evidence contains `Phase 8 acceptance: Accepted.` only after all gates pass.
- [ ] Supported markers are present: `Plugin management GUI: supported`, `Collaboration GUI: supported`, `Remote workspace GUI: supported`, `Delegated task command center: approval-gated`, `Cross-platform parity: Windows/macOS/Linux evidenced`, and `Autonomous apply: unsupported`.
- [ ] CLI and xtask GUI Phase 8 evidence checks pass.
- [ ] Standard repository gates from `AGENTS.md` pass or acceptance remains blocked with evidence.
- [ ] `.planning/ROADMAP.md` and `.planning/STATE.md` are updated consistently with the acceptance decision.
- [ ] No forbidden file is modified.

</verification>

<success_criteria>

- GUI Phase 8 is accepted only with complete evidence and passing gates.
- If acceptance is blocked, roadmap/state/evidence state exactly what is missing and no GA claim is made.
- Accepted legacy Phase 8 substrate evidence remains untouched.
- Final evidence is sufficient for a reviewer to reproduce the acceptance decision.

</success_criteria>

<output>

Write `.planning/phases/08-advanced-platform-gui-ga/08-07-RESULT.md`.

</output>

## Task 1: Audit Phase 8 Evidence Completeness

### Read Targets

- `.planning/phases/08-advanced-platform-gui-ga/08-01-RESULT.md`
- `.planning/phases/08-advanced-platform-gui-ga/08-02-RESULT.md`
- `.planning/phases/08-advanced-platform-gui-ga/08-03-RESULT.md`
- `.planning/phases/08-advanced-platform-gui-ga/08-04-RESULT.md`
- `.planning/phases/08-advanced-platform-gui-ga/08-05-RESULT.md`
- `.planning/phases/08-advanced-platform-gui-ga/08-06-RESULT.md`
- `plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md`
- every required Phase 8 GUI evidence artifact

### Write Targets

- `plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md`
- `plans/evidence/gui-productization/phase-8-final-gates.md`

### Implementation Sequence

1. Verify every prior result file exists, every required artifact exists, and no unresolved `BLOCKED` item remains.
   > verification: `rg -n "BLOCKED" .planning/phases/08-advanced-platform-gui-ga`
2. Populate final evidence with required artifact inventory, command transcript summary, supported advanced-surface markers, platform parity markers, and final checklist.
   > verification: `rg -q "Plugin management GUI: supported" plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md`
3. Write final gate evidence with exact commands, exit status, and short outcome notes for every frontmatter command.
   > verification: `rg -q "cargo clippy --workspace --all-targets -- -D warnings" plans/evidence/gui-productization/phase-8-final-gates.md`

### Edge And Error Cases

- Do not mark final acceptance if `rg -n "BLOCKED"` finds unresolved blockers in any Phase 8 result or evidence file.
- Do not convert missing platform evidence into a residual risk.
- Do not omit failed command output; summarize exact failure cause and location.

<verify>

```powershell
rg -n "BLOCKED" .planning/phases/08-advanced-platform-gui-ga
rg -q "Plugin management GUI: supported" plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md
rg -q "cargo clippy --workspace --all-targets -- -D warnings" plans/evidence/gui-productization/phase-8-final-gates.md
```

</verify>

### BLOCKED Conditions

- Any required result file, evidence artifact, marker, or command result is missing.

## Task 2: Run Final Acceptance Commands

### Read Targets

- `plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md`
- `plans/evidence/gui-productization/phase-8-final-gates.md`
- `AGENTS.md`

### Write Targets

- `plans/evidence/gui-productization/phase-8-final-gates.md`
- `.planning/phases/08-advanced-platform-gui-ga/08-07-RESULT.md`

### Implementation Sequence

1. Run targeted desktop/app/CLI/xtask tests for plugin, collaboration, remote, delegated task, and GUI Phase 8 evidence behavior.
   > verification: `cargo test -p devil-desktop plugin_management collaboration_gui remote_workspace_gui delegated_task_command_center -- --nocapture`
2. Run GUI Phase 8 evidence checks.
   > verification: `cargo run -p devil-cli -- evidence check --phase gui-phase8`
3. Run standard repository gates from `AGENTS.md`: `cargo run -p xtask -- check-deps`, `cargo fmt --all --check`, `cargo check --workspace --all-targets`, `cargo test --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo deny check`.

### Edge And Error Cases

- If a command fails, record exact command, exit status, and failure summary before deciding whether to fix or block.
- Do not skip `cargo deny check`; CI runs it with warning-level policy.
- Do not mark a command passed from memory or from a previous phase.

<verify>

```powershell
cargo test -p devil-desktop plugin_management collaboration_gui remote_workspace_gui delegated_task_command_center -- --nocapture
cargo test -p devil-app plugin collaboration remote -- --nocapture
cargo test -p devil-cli gui_phase8 gui_phase7 phase8 -- --nocapture
cargo test -p xtask gui_phase8 gui_phase7 phase8 -- --nocapture
cargo run -p devil-cli -- evidence check --phase gui-phase8
cargo run -p xtask -- check-deps
cargo fmt --all --check
cargo check --workspace --all-targets
cargo test --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo deny check
```

</verify>

### BLOCKED Conditions

- Any required command fails and cannot be fixed within the final acceptance scope.

## Task 3: Update Planning State With Acceptance Decision

### Read Targets

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md`
- `plans/evidence/gui-productization/phase-8-final-gates.md`

### Write Targets

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/phases/08-advanced-platform-gui-ga/08-07-RESULT.md`

### Implementation Sequence

1. If all gates pass, mark Phase 8 accepted in `.planning/ROADMAP.md`, update completed plan counts, and record acceptance date. If any gate is blocked, leave Phase 8 unchecked and record the exact blocker.
   > verification: `rg -q "Phase 8 status" .planning/ROADMAP.md`
2. Update `.planning/STATE.md` with Phase 8 accepted or blocked status, exact command evidence, and next action.
   > verification: `rg -q "Phase 8" .planning/STATE.md`
3. Write `08-07-RESULT.md` with changed files, final decision, command outcomes, and residual risks.
   > verification: `rg -q "Acceptance decision" .planning/phases/08-advanced-platform-gui-ga/08-07-RESULT.md`

### Edge And Error Cases

- Do not mark Phase 8 complete if platform parity or final gates are incomplete.
- Do not edit product code in this final acceptance plan.
- Preserve accepted legacy Phase 8 substrate evidence unchanged.

<verify>

```powershell
rg -q "Phase 8 status" .planning/ROADMAP.md
rg -q "Phase 8" .planning/STATE.md
rg -q "Acceptance decision" .planning/phases/08-advanced-platform-gui-ga/08-07-RESULT.md
```

</verify>

### BLOCKED Conditions

- Planning state cannot be updated consistently with the evidence decision.
