---
phase: "08-advanced-platform-gui-ga"
plan: "08-01"
title: "GUI Phase 8 Governance And Evidence Gate"
wave: 1
agents: ["project-manager-senior", "engineering-infrastructure-devops"]
depends_on: []
requirements: ["R-015"]
expected_artifacts:
  - path: "plans/dependency-policy.md"
    provides: Compatibility note distinguishing GUI Phase 8 advanced GUI GA from accepted legacy Phase 8 substrate evidence
    required: true
  - path: "xtask/src/main.rs"
    provides: GUI Phase 8 evidence validation path that preserves existing Phase 8 runtime substrate checks
    required: true
  - path: "crates/devil-cli/src/main.rs"
    provides: `devil-cli evidence check --phase gui-phase8` gate for advanced GUI GA artifacts
    required: true
  - path: "plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md"
    provides: Not-accepted GUI Phase 8 evidence stub with required artifact inventory and final checklist
    required: true
  - path: ".planning/phases/08-advanced-platform-gui-ga/08-01-RESULT.md"
    provides: Execution record with command results, decisions, and blockers
    required: true
verification_commands:
  - "rg -q \"GUI Phase 8\" plans/dependency-policy.md"
  - "rg -q \"DEFAULT_GUI_PHASE8_EVIDENCE_PATH\" xtask/src/main.rs"
  - "rg -q \"GuiPhase8\" crates/devil-cli/src/main.rs"
  - "rg -q \"Phase 8 acceptance: Not accepted\" plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md"
  - "cargo test -p xtask gui_phase8 gui_phase7 phase8 -- --nocapture"
  - "cargo test -p devil-cli gui_phase8 gui_phase7 phase8 -- --nocapture"
  - "cargo run -p xtask -- check-deps"
  - "cargo run -p devil-cli -- evidence check --phase gui-phase8"
files_modified:
  - "plans/dependency-policy.md"
  - "xtask/src/main.rs"
  - "crates/devil-cli/src/main.rs"
  - "plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md"
  - ".planning/phases/08-advanced-platform-gui-ga/08-01-RESULT.md"
files_forbidden:
  - "plans/evidence/phase-8/"
  - "crates/devil-app/"
  - "crates/devil-desktop/"
  - "crates/devil-ui/"
  - "crates/devil-protocol/"
  - "Cargo.toml"
  - "Cargo.lock"
  - ".github/workflows/ci.yml"
  - "scripts/"
---

# Plan 08-01: GUI Phase 8 Governance And Evidence Gate

## Objective

Create a distinct GUI Phase 8 advanced GUI GA acceptance path while preserving the already accepted legacy Phase 8 runtime substrate evidence.

<execution_contract>

- read-before-write: read `08-CONTEXT.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `plans/dependency-policy.md`, `xtask/src/main.rs`, `crates/devil-cli/src/main.rs`, `plans/evidence/phase-8/phase-8-architecture-map.md`, `plans/evidence/phase-8/release-readiness-review.md`, `plans/evidence/gui-productization/phase-7-local-ide-beta.md`, and `AGENTS.md` before editing.
- evidence-before-action: confirm that legacy Phase 8 evidence is accepted runtime substrate evidence and GUI Phase 8 needs a separate productization gate before any product code claims GA.
- minimal diff: edit only governance/evidence-gate files and this plan result; do not touch app/UI/desktop/protocol implementation in this plan.
- verify-before-report: run every frontmatter command and record outcomes in `08-01-RESULT.md`.
- role: governance and GA-readiness implementer.
- task: add a GUI Phase 8 evidence gate that can remain not accepted during implementation and later enforce final advanced GUI artifacts.
- scope: dependency policy, `xtask` constants/helpers/tests, `devil-cli` evidence enum/helpers/tests, GUI Phase 8 evidence stub, and plan result.
- allowed tools/actions: focused Markdown/Rust edits, `rg`, targeted Rust tests, `cargo run -p xtask -- check-deps`, and `cargo run -p devil-cli -- evidence check --phase gui-phase8`.
- forbidden actions: deleting or weakening legacy Phase 8 evidence, modifying product crates, adding dependencies, touching CI/scripts, or marking GUI Phase 8 accepted before implementation/evidence exists.
- verification criteria: legacy Phase 8 checks still pass; GUI Phase 8 evidence can be not accepted without blocking `xtask`; accepted GUI Phase 8 claims require checked checklist, required artifacts, command markers, supported advanced-surface markers, platform parity markers, and no stale Phase 7 unsupported labels for plugin/collaboration/remote GUI.
- final result format: write `.planning/phases/08-advanced-platform-gui-ga/08-01-RESULT.md` with changed files, command outcomes, and any `BLOCKED` items.

</execution_contract>

<stop_gates>

- BLOCKED if `xtask check-deps` cannot validate both accepted legacy Phase 8 runtime substrate evidence and not-accepted GUI Phase 8 productization evidence without ambiguous status markers.
- BLOCKED if implementing the GUI Phase 8 gate requires editing or deleting files under `plans/evidence/phase-8/`.
- BLOCKED if the dependency policy would authorize `devil-ui` or `devil-desktop` to own editor text, workspace mutation, proposal lifecycle state, plugin runtime authority, collaboration runtime authority, remote runtime authority, terminal authority, provider routing, storage authority, telemetry, retention, or security policy.
- BLOCKED if GUI Phase 8 evidence would have to be marked accepted before plugin, collaboration, remote, delegated task, release/update/rollback/incident, platform parity, and final gate evidence exists.

</stop_gates>

<recovery>

- If `xtask` tests fail after adding GUI Phase 8 validation, preserve `DEFAULT_PHASE8_EVIDENCE_PATH` and add a separate `DEFAULT_GUI_PHASE8_EVIDENCE_PATH` branch rather than replacing substrate checks.
- If `devil-cli` value-enum parsing fails, mirror the existing `GuiPhase7` style exactly and add only `GuiPhase8`.
- If the evidence stub conflicts with existing evidence names, keep GUI advanced GA evidence under `plans/evidence/gui-productization/` and document the collision in `08-01-RESULT.md`.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] `plans/dependency-policy.md` explicitly mentions GUI Phase 8 advanced GUI GA boundaries.
- [ ] `xtask/src/main.rs` has a GUI Phase 8 evidence path separate from `DEFAULT_PHASE8_EVIDENCE_PATH`.
- [ ] `crates/devil-cli/src/main.rs` accepts `--phase gui-phase8`.
- [ ] `plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md` exists and says `Phase 8 acceptance: Not accepted.`
- [ ] `cargo test -p xtask gui_phase8 gui_phase7 phase8 -- --nocapture` passes.
- [ ] `cargo test -p devil-cli gui_phase8 gui_phase7 phase8 -- --nocapture` passes.
- [ ] `cargo run -p xtask -- check-deps` passes.
- [ ] `cargo run -p devil-cli -- evidence check --phase gui-phase8` passes.
- [ ] No forbidden file is modified.

</verification>

<success_criteria>

- GUI Phase 8 has a distinct not-accepted evidence stub.
- Accepted legacy Phase 8 runtime substrate evidence remains untouched.
- Future accepted GUI Phase 8 claims require plugin, collaboration, remote, delegated task, operations/release, platform parity, plan result, and full gate evidence.
- Product-code plans can depend on a clear final evidence contract.

</success_criteria>

<output>

Write `.planning/phases/08-advanced-platform-gui-ga/08-01-RESULT.md`.

</output>

## Task 1: Add GUI Phase 8 Policy Boundary

### Read Targets

- `plans/dependency-policy.md`
- `.planning/phases/08-advanced-platform-gui-ga/08-CONTEXT.md`
- `plans/evidence/phase-8/phase-8-architecture-map.md`
- `plans/evidence/gui-productization/phase-7-local-ide-beta.md`

### Write Targets

- `plans/dependency-policy.md`

### Implementation Sequence

1. Add a `GUI Phase 8` compatibility paragraph near the existing GUI Phase 6/7 notes or legacy Phase 8 policy entry. It must state that GUI Phase 8 authorizes advanced GUI workflows for plugin management, collaboration, remote workspace, delegated task command-center, and GA operations evidence only through app/protocol authority.
   > verification: `rg -q "GUI Phase 8" plans/dependency-policy.md`
2. Explicitly preserve accepted legacy Phase 8 runtime substrate evidence under `plans/evidence/phase-8/` and state that GUI Phase 8 does not reopen runtime substrate acceptance.
   > verification: `rg -q "plans/evidence/phase-8" plans/dependency-policy.md`
3. State that GUI Phase 8 does not authorize UI/desktop ownership of plugin/collaboration/remote/terminal/provider/storage/security authority, autonomous apply, raw-source diagnostics, or direct mutation outside proposal-mediated app/workspace/editor paths.
   > verification: `rg -q "advanced GUI GA" plans/dependency-policy.md`

### Edge And Error Cases

- Do not add new allowed internal dependencies.
- Do not loosen renderer dependency gates or existing Phase 8 runtime substrate gates.
- If existing policy text is stale or contradictory, add a compatibility note instead of deleting accepted substrate policy.

<verify>

```powershell
rg -q "GUI Phase 8" plans/dependency-policy.md
rg -q "plans/evidence/phase-8" plans/dependency-policy.md
rg -q "advanced GUI GA" plans/dependency-policy.md
```

</verify>

### BLOCKED Conditions

- The policy cannot distinguish GUI Phase 8 from accepted legacy Phase 8 runtime substrate evidence without weakening accepted evidence.

## Task 2: Add Xtask GUI Phase 8 Evidence Validation

### Read Targets

- `xtask/src/main.rs`
- `plans/evidence/phase-8/phase-8-architecture-map.md`
- `plans/evidence/gui-productization/phase-7-local-ide-beta.md`

### Write Targets

- `xtask/src/main.rs`

### Implementation Sequence

1. Add `DEFAULT_GUI_PHASE8_EVIDENCE_PATH` with value `plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md`.
   > verification: `rg -q "DEFAULT_GUI_PHASE8_EVIDENCE_PATH" xtask/src/main.rs`
2. Add GUI Phase 8 required artifact and command marker lists for plugin management, collaboration shared proposal, remote workspace manager, delegated task command center, GA release/update/rollback/incident evidence, platform parity, plan result files `08-01` through `08-07`, GUI Phase 8 CLI evidence check, targeted tests, smoke scripts, and standard repository gates.
   > verification: `rg -q "GUI_PHASE8_REQUIRED_ARTIFACTS" xtask/src/main.rs`
3. Add validation helpers and tests mirroring GUI Phase 7 behavior: not-accepted status passes without artifacts; conflicting markers fail; accepted status requires checked checklist, required artifacts, required command markers, platform parity markers, and supported advanced-surface markers; accepted status fails if it still contains stale Phase 7 unsupported labels for plugin management GUI, collaboration GUI, remote production GUI, or cross-platform parity. Preserve all existing Phase 8 substrate tests.
   > verification: `cargo test -p xtask gui_phase8 gui_phase7 phase8 -- --nocapture`

### Edge And Error Cases

- Accepted GUI Phase 8 evidence must fail if any final checklist item is unchecked.
- Accepted GUI Phase 8 evidence must fail if any GUI Phase 8 required artifact is missing.
- Accepted GUI Phase 8 evidence must not require editing `plans/evidence/phase-8/`.
- The legacy `DEFAULT_PHASE8_EVIDENCE_PATH` must still point at `plans/evidence/phase-8/phase-8-architecture-map.md`.

<verify>

```powershell
rg -q "DEFAULT_GUI_PHASE8_EVIDENCE_PATH" xtask/src/main.rs
rg -q "GUI_PHASE8_REQUIRED_ARTIFACTS" xtask/src/main.rs
cargo test -p xtask gui_phase8 gui_phase7 phase8 -- --nocapture
```

</verify>

### BLOCKED Conditions

- `xtask` cannot validate GUI Phase 8 without changing accepted legacy Phase 8 substrate semantics.

## Task 3: Add Devil CLI GUI Phase 8 Evidence Check And Stub

### Read Targets

- `crates/devil-cli/src/main.rs`
- `plans/evidence/gui-productization/phase-7-local-ide-beta.md`
- `.planning/ROADMAP.md`

### Write Targets

- `crates/devil-cli/src/main.rs`
- `plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md`
- `.planning/phases/08-advanced-platform-gui-ga/08-01-RESULT.md`

### Implementation Sequence

1. Add `GuiPhase8` to `EvidencePhase` and route it to `check_gui_phase8_evidence`, following the `GuiPhase7` parser/test pattern.
   > verification: `rg -q "GuiPhase8" crates/devil-cli/src/main.rs`
2. Add GUI Phase 8 constants and `check_gui_phase8_evidence` that accepts not-accepted scaffold mode and enforces artifacts, command markers, platform parity markers, supported advanced-surface markers, checked checklist, and stale unsupported-label rejection when accepted.
   > verification: `cargo test -p devil-cli gui_phase8 gui_phase7 phase8 -- --nocapture`
3. Create `plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md` with sections `## Acceptance Status`, `## Scope`, `## Required Artifacts`, `## Required Commands`, `## Supported Advanced GUI Surface Markers`, `## Platform Parity Markers`, `## Final Validation Checklist`, and `## Residual Risks`. Set status exactly to `Phase 8 acceptance: Not accepted.`
   > verification: `rg -q "Phase 8 acceptance: Not accepted" plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md`

### Edge And Error Cases

- Do not mark GUI Phase 8 accepted in this plan.
- Do not copy legacy Phase 8 substrate artifacts into the GUI Phase 8 required artifact list as substitutes for GUI evidence.
- Do not include command logs in the scaffold.

<verify>

```powershell
rg -q "GuiPhase8" crates/devil-cli/src/main.rs
cargo test -p devil-cli gui_phase8 gui_phase7 phase8 -- --nocapture
rg -q "Phase 8 acceptance: Not accepted" plans/evidence/gui-productization/phase-8-advanced-platform-gui-ga.md
cargo run -p devil-cli -- evidence check --phase gui-phase8
```

</verify>

### BLOCKED Conditions

- The not-accepted GUI Phase 8 stub cannot pass `xtask check-deps` and `devil-cli evidence check --phase gui-phase8`.
