---
phase: "06-packaging-platform-integration-and-accessibility"
plan: "06-01"
title: "GUI Phase 6 Governance And Evidence Gate"
wave: 1
agents: ["project-manager-senior", "engineering-infrastructure-devops"]
depends_on: []
requirements: ["R-012", "R-013"]
expected_artifacts:
  - path: "plans/dependency-policy.md"
    provides: Compatibility note distinguishing GUI Phase 6 packaging/platform/accessibility from legacy collaboration Phase 6
    required: true
  - path: "xtask/src/main.rs"
    provides: GUI Phase 6 evidence validation path that preserves existing collaboration Phase 6 checks
    required: true
  - path: "plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md"
    provides: Not-accepted GUI Phase 6 evidence stub with required artifact inventory and final checklist
    required: true
  - path: ".planning/phases/06-packaging-platform-integration-and-accessibility/06-01-RESULT.md"
    provides: Execution record with command results, decisions, and blockers
    required: true
verification_commands:
  - "rg -q \"GUI Phase 6\" plans/dependency-policy.md"
  - "rg -q \"DEFAULT_GUI_PHASE6_EVIDENCE_PATH\" xtask/src/main.rs"
  - "rg -q \"Phase 6 acceptance: Not accepted\" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md"
  - "cargo run -p xtask -- check-deps"
files_modified:
  - "plans/dependency-policy.md"
  - "xtask/src/main.rs"
  - "plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md"
  - ".planning/phases/06-packaging-platform-integration-and-accessibility/06-01-RESULT.md"
files_forbidden:
  - "plans/evidence/phase-6/collaboration-architecture-map.md"
  - "plans/evidence/phase-6/"
  - "crates/"
  - "Cargo.toml"
  - "Cargo.lock"
---

# Plan 06-01: GUI Phase 6 Governance And Evidence Gate

## Objective

Create a distinct GUI Phase 6 acceptance path for packaging, platform integration, and accessibility while preserving the already accepted legacy collaboration Phase 6 evidence.

<execution_contract>

- read-before-write: read `.planning/phases/06-packaging-platform-integration-and-accessibility/06-CONTEXT.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `plans/dependency-policy.md`, `xtask/src/main.rs`, `plans/evidence/phase-6/collaboration-architecture-map.md`, and `AGENTS.md` before editing.
- evidence-before-action: confirm that `xtask` currently validates legacy Phase 6 collaboration evidence before adding the GUI Phase 6 branch.
- minimal diff: edit only governance/evidence files and this plan result; do not touch product crates in this plan.
- verify-before-report: run every frontmatter command and record outcomes in `06-01-RESULT.md`.
- role: governance and packaging-readiness implementer.
- task: add a GUI Phase 6 evidence gate that can remain not accepted during implementation and later enforce final packaging/platform/accessibility artifacts.
- scope: dependency policy, `xtask` constants/helpers/tests, GUI Phase 6 evidence stub, and plan result.
- allowed tools/actions: focused Markdown/Rust edits, `rg`, targeted `xtask` tests, and `cargo run -p xtask -- check-deps`.
- forbidden actions: deleting or weakening legacy Phase 6 collaboration evidence, modifying product crates, adding dependencies, or marking GUI Phase 6 accepted before implementation evidence exists.
- verification criteria: legacy Phase 6 checks still pass; GUI Phase 6 evidence can be not accepted without blocking `xtask`; accepted GUI Phase 6 claims require checked checklist and required artifacts.
- final result format: write `.planning/phases/06-packaging-platform-integration-and-accessibility/06-01-RESULT.md` with changed files, command outcomes, and any `BLOCKED` items.

</execution_contract>

<stop_gates>

- BLOCKED if `xtask check-deps` cannot validate both legacy collaboration Phase 6 and GUI Phase 6 evidence without ambiguous status markers.
- BLOCKED if implementing the GUI Phase 6 gate requires editing or deleting files under `plans/evidence/phase-6/`.
- BLOCKED if the dependency policy would authorize `devil-ui` or `devil-desktop` to own editor text, workspace mutation, proposal lifecycle state, terminal runtime authority, provider routing, storage authority, or security policy.
- BLOCKED if GUI Phase 6 evidence would have to be marked accepted before package, platform, accessibility, session, diagnostics, smoke, and gate evidence exists.

</stop_gates>

<recovery>

- If `xtask` tests fail after adding GUI Phase 6 validation, restore legacy Phase 6 behavior and add a separate `DEFAULT_GUI_PHASE6_EVIDENCE_PATH` branch rather than replacing collaboration checks.
- If the policy already contains sufficient renderer boundary text, add only a GUI Phase 6 compatibility note and avoid duplicate broad policy.
- If the evidence stub conflicts with existing evidence names, keep the GUI stub under `plans/evidence/gui-productization/` and document the collision in `06-01-RESULT.md`.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] `plans/dependency-policy.md` explicitly mentions GUI Phase 6 packaging/platform/accessibility boundaries.
- [ ] `xtask/src/main.rs` has a GUI Phase 6 evidence path separate from `DEFAULT_PHASE6_EVIDENCE_PATH`.
- [ ] `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md` exists and says `Phase 6 acceptance: Not accepted.`
- [ ] `cargo run -p xtask -- check-deps` passes.
- [ ] No forbidden file is modified.

</verification>

<success_criteria>

- GUI Phase 6 has a distinct not-accepted evidence stub.
- Legacy accepted collaboration Phase 6 evidence remains untouched.
- Future accepted GUI Phase 6 claims require required artifacts, checked checklist, and command markers.
- Product-code plans can depend on a clear final evidence contract.

</success_criteria>

<output>

Write `.planning/phases/06-packaging-platform-integration-and-accessibility/06-01-RESULT.md`.

</output>

## Task 1: Add GUI Phase 6 Policy Boundary

### Read Targets

- `plans/dependency-policy.md`
- `.planning/phases/06-packaging-platform-integration-and-accessibility/06-CONTEXT.md`
- `plans/evidence/phase-6/collaboration-architecture-map.md`

### Write Targets

- `plans/dependency-policy.md`

### Implementation Sequence

1. Add a `GUI Phase 6` compatibility paragraph near the renderer/desktop policy entries or existing GUI Phase notes. It must state that GUI Phase 6 authorizes packaging scripts, desktop adapter platform observation, crash-safe metadata-only session persistence, diagnostics export formatting, and smoke evidence only.
   > verification: `rg -q "GUI Phase 6" plans/dependency-policy.md`
2. Explicitly preserve the legacy collaboration Phase 6 policy as historical substrate evidence and state that GUI Phase 6 does not alter collaboration acceptance.
   > verification: `rg -q "Phase 6 collaboration activation" plans/dependency-policy.md`
3. State that GUI Phase 6 does not authorize renderer/windowing dependencies outside `devil-desktop`, nor UI/desktop ownership of app/editor/workspace/proposal/storage/security/provider/terminal authority.
   > verification: `rg -q "packaging/platform/accessibility" plans/dependency-policy.md`

### Edge And Error Cases

- Do not add new allowed internal dependencies.
- Do not loosen the renderer dependency gate.
- If existing policy text is stale or contradictory, add a compatibility note instead of deleting accepted substrate policy.

<verify>

```powershell
rg -q "GUI Phase 6" plans/dependency-policy.md
rg -q "Phase 6 collaboration activation" plans/dependency-policy.md
rg -q "packaging/platform/accessibility" plans/dependency-policy.md
```

</verify>

### BLOCKED Conditions

- The policy cannot distinguish GUI Phase 6 from legacy collaboration Phase 6 without weakening accepted evidence.

## Task 2: Add Xtask GUI Phase 6 Evidence Validation

### Read Targets

- `xtask/src/main.rs`
- `plans/evidence/phase-6/collaboration-architecture-map.md`
- `plans/evidence/gui-productization/phase-5-control-trust-assisted-ai.md`

### Write Targets

- `xtask/src/main.rs`

### Implementation Sequence

1. Add `DEFAULT_GUI_PHASE6_EVIDENCE_PATH` with value `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`.
   > verification: `rg -q "DEFAULT_GUI_PHASE6_EVIDENCE_PATH" xtask/src/main.rs`
2. Add `GUI_PHASE6_REQUIRED_ARTIFACTS` and `GUI_PHASE6_REQUIRED_COMMAND_MARKERS` for package runbook, package smoke, platform/accessibility smoke, diagnostics/session safety, workflow smoke, CI parity plan, plan result files `06-01` through `06-06`, and the standard phase gates.
   > verification: `rg -q "GUI_PHASE6_REQUIRED_ARTIFACTS" xtask/src/main.rs`
3. Add validation helpers and tests mirroring GUI Phase 5 behavior: not-accepted status passes without artifacts, conflicting markers fail, accepted status requires checked checklist, required artifacts, and required command markers. Preserve all existing Phase 6 collaboration tests.
   > verification: `cargo test -p xtask gui_phase6 phase6 -- --nocapture`

### Edge And Error Cases

- Accepted GUI Phase 6 evidence must fail if any final checklist item is unchecked.
- Accepted GUI Phase 6 evidence must fail if it still contains "not accepted", "pending", "not observed" as a final accepted artifact status, unless the wording explicitly labels a residual risk outside acceptance.
- The legacy `DEFAULT_PHASE6_EVIDENCE_PATH` must still point at `plans/evidence/phase-6/collaboration-architecture-map.md`.

<verify>

```powershell
rg -q "DEFAULT_GUI_PHASE6_EVIDENCE_PATH" xtask/src/main.rs
rg -q "GUI_PHASE6_REQUIRED_ARTIFACTS" xtask/src/main.rs
cargo test -p xtask gui_phase6 phase6 -- --nocapture
```

</verify>

### BLOCKED Conditions

- `xtask` cannot validate GUI Phase 6 without changing legacy collaboration Phase 6 semantics.

## Task 3: Create GUI Phase 6 Evidence Stub

### Read Targets

- `.planning/ROADMAP.md`
- `.planning/phases/06-packaging-platform-integration-and-accessibility/06-CONTEXT.md`

### Write Targets

- `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`
- `.planning/phases/06-packaging-platform-integration-and-accessibility/06-01-RESULT.md`

### Implementation Sequence

1. Create the GUI Phase 6 evidence stub with sections `## Acceptance Status`, `## Scope`, `## Required Artifacts`, `## Required Command Markers`, `## Final Validation Checklist`, and `## Residual Risks`.
   > verification: `test -f plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`
2. Set acceptance to exactly `Phase 6 acceptance: Not accepted.` and leave every final checklist item unchecked.
   > verification: `rg -q "Phase 6 acceptance: Not accepted" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`
3. List required artifacts for packaging, platform/accessibility smoke, crash-safe session restore, diagnostics export, workflow smoke scripts, CI parity, plan results, and full cargo gates.
   > verification: `rg -q "phase-6-platform-accessibility-smoke" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`

### Edge And Error Cases

- Do not mark GUI Phase 6 accepted in this plan.
- Do not copy command logs into the stub.
- Do not reference legacy collaboration artifacts as satisfying GUI packaging/platform evidence.

<verify>

```powershell
test -f plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md
rg -q "Phase 6 acceptance: Not accepted" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md
rg -q "phase-6-platform-accessibility-smoke" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md
cargo run -p xtask -- check-deps
```

</verify>

### BLOCKED Conditions

- The not-accepted GUI Phase 6 stub cannot pass `xtask check-deps`.
