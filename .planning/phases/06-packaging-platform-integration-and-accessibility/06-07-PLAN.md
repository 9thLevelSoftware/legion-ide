---
phase: "06-packaging-platform-integration-and-accessibility"
plan: "06-07"
title: "Phase 6 Acceptance Gate"
wave: 7
agents: ["project-manager-senior", "testing-qa-verification-specialist", "product-technical-writer"]
depends_on: ["06-06"]
requirements: ["R-012", "R-013"]
expected_artifacts:
  - path: "plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md"
    provides: Accepted GUI Phase 6 evidence with complete checklist, artifact inventory, command markers, and residual risks
    required: true
  - path: ".planning/ROADMAP.md"
    provides: Phase 6 progress updated only after acceptance evidence passes
    required: true
  - path: ".planning/STATE.md"
    provides: Project state advanced to Phase 7 planning only after final gates pass
    required: true
  - path: ".planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md"
    provides: Final acceptance record with full gate outcomes and go/no-go rationale
    required: true
verification_commands:
  - "cargo run -p xtask -- check-deps"
  - "cargo fmt --all --check"
  - "cargo check --workspace --all-targets"
  - "cargo test --workspace --all-targets"
  - "cargo clippy --workspace --all-targets -- -D warnings"
  - "cargo deny check"
  - "cargo run -p devil-cli -- evidence check --phase gui-phase6"
  - "rg -q \"Phase 6 acceptance: Accepted\" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md"
files_modified:
  - "plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md"
files_forbidden:
  - "crates/"
  - "Cargo.toml"
  - "Cargo.lock"
  - "xtask/src/main.rs"
  - ".github/workflows/ci.yml"
  - "scripts/"
  - "plans/evidence/phase-6/"
---

# Plan 06-07: Phase 6 Acceptance Gate

## Objective

Accept GUI Phase 6 only after the package, platform, accessibility, session, diagnostics, smoke, parity, and full repository gates are evidenced.

<execution_contract>

- read-before-write: read `06-CONTEXT.md`, `06-01-RESULT.md` through `06-06-RESULT.md`, all GUI Phase 6 evidence files, `.planning/ROADMAP.md`, `.planning/STATE.md`, `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`, and `AGENTS.md` before editing.
- evidence-before-action: confirm every required GUI Phase 6 artifact exists and every required command has a recorded pass or a documented blocker before changing acceptance status.
- minimal diff: edit only the main GUI Phase 6 evidence file, planning state/roadmap, and final result.
- verify-before-report: run all frontmatter commands and record exact outcomes in `06-07-RESULT.md`.
- role: acceptance-gate coordinator and QA verifier.
- task: make a final evidence-gated go/no-go decision for GUI Phase 6.
- scope: acceptance evidence, planning state, roadmap progress, and final result.
- allowed tools/actions: running repository gates, evidence checks, Markdown state edits, and `rg`.
- forbidden actions: product code changes, script/CI fixes, manifest changes, legacy Phase 6 evidence changes, or acceptance without full evidence.
- verification criteria: all required gates pass; GUI Phase 6 evidence says accepted only if checklist is checked and artifacts/commands are complete; state advances to Phase 7 next action.
- final result format: write `.planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md` with command table, acceptance decision, changed files, residual risks, and any blockers.

</execution_contract>

<stop_gates>

- BLOCKED if any required Phase 6 plan result `06-01` through `06-06` is missing or reports unresolved blockers.
- BLOCKED if any full repository gate fails and the failure is not fixed within scope.
- BLOCKED if `cargo run -p devil-cli -- evidence check --phase gui-phase6` or `cargo run -p xtask -- check-deps` fails.
- BLOCKED if evidence contains unresolved `Phase 6 acceptance: Not accepted.`, unchecked checklist items, missing artifacts, or unqualified `not observed` claims for accepted platform/accessibility criteria.

</stop_gates>

<recovery>

- If a final gate fails due to an implementation issue, do not edit acceptance status. Return to the owning earlier plan's scope or create a blocker with exact failing command and file reference.
- If `cargo deny check` reports warning-level duplicate dependency findings matching the repo policy baseline, record the warning and command result exactly; do not claim a clean deny output.
- If a required manual Windows smoke artifact is missing, keep Phase 6 not accepted and state the narrow command/evidence needed.
- On interruption, inspect `git diff`, compare modified files against `files_modified` and `files_forbidden`, rerun verification, and continue only from unverified work.

</recovery>

<verification>

- [ ] All required GUI Phase 6 evidence artifacts exist and are cited.
- [ ] `cargo run -p xtask -- check-deps` passes.
- [ ] `cargo fmt --all --check` passes.
- [ ] `cargo check --workspace --all-targets` passes.
- [ ] `cargo test --workspace --all-targets` passes.
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes.
- [ ] `cargo deny check` passes or reports only accepted warning-level baseline output.
- [ ] `cargo run -p devil-cli -- evidence check --phase gui-phase6` passes.
- [ ] ROADMAP/STATE advance only after all acceptance gates pass.

</verification>

<success_criteria>

- GUI Phase 6 is accepted only with complete evidence.
- Planning state points to Phase 7 planning after acceptance.
- Remaining platform or parity risks are explicitly documented and not hidden as completed work.

</success_criteria>

<output>

Write `.planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md`.

</output>

## Task 1: Verify Required Evidence And Main Acceptance Artifact

### Read Targets

- `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`
- `plans/evidence/gui-productization/phase-6-package-runbook.md`
- `plans/evidence/gui-productization/phase-6-packaging-smoke.md`
- `plans/evidence/gui-productization/phase-6-platform-accessibility-smoke.md`
- `plans/evidence/gui-productization/phase-6-session-diagnostics-safety.md`
- `plans/evidence/gui-productization/phase-6-workflow-smoke.md`
- `plans/evidence/gui-productization/phase-6-performance-reliability.md`
- `plans/evidence/gui-productization/phase-6-ci-parity-plan.md`
- `.planning/phases/06-packaging-platform-integration-and-accessibility/06-01-RESULT.md` through `06-06-RESULT.md`

### Write Targets

- `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`

### Implementation Sequence

1. Confirm every required artifact in the GUI Phase 6 evidence stub exists and has command outcomes. Do not edit acceptance status if any are missing.
   > verification: `rg -q "phase-6-platform-accessibility-smoke" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`
2. If all required artifacts and commands pass, change the main evidence status to exactly `Phase 6 acceptance: Accepted.` and check every final checklist item.
   > verification: `rg -q "Phase 6 acceptance: Accepted" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`
3. Add a residual risk section that distinguishes Windows-first package acceptance from future signed installer and broader OS parity work.
   > verification: `rg -q "Residual Risk" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`

### Edge And Error Cases

- Do not leave both accepted and not-accepted markers in the status section.
- Do not use evidence from `plans/evidence/phase-6/` as GUI Phase 6 proof.
- Do not accept with unchecked checklist items.

<verify>

```powershell
rg -q "phase-6-platform-accessibility-smoke" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md
rg -q "Phase 6 acceptance: Accepted" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md
rg -q "Residual Risk" plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md
```

</verify>

### BLOCKED Conditions

- Required evidence is missing, stale, blocked, or contradictory.

## Task 2: Run Final Repository Gates

### Read Targets

- `AGENTS.md`
- `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`
- `deny.toml`

### Write Targets

- `.planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md`

### Implementation Sequence

1. Run full repository gates in this order: `cargo run -p xtask -- check-deps`, `cargo fmt --all --check`, `cargo check --workspace --all-targets`, `cargo test --workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo deny check`, and `cargo run -p devil-cli -- evidence check --phase gui-phase6`.
   > verification: `cargo run -p xtask -- check-deps`
2. Record exact pass/fail status and notable warning text in `06-07-RESULT.md`. If any command fails, keep Phase 6 not accepted and mark `BLOCKED`.
   > verification: `test -f .planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md`
3. Confirm no product code changed during this final gate.
   > verification: `git diff --quiet -- crates Cargo.toml Cargo.lock`

### Edge And Error Cases

- If `cargo deny check` emits warning-level duplicate output while returning success, record it as warning-level output.
- If a full gate fails due to an earlier Phase 6 implementation issue, stop and route back to the owning plan.

<verify>

```powershell
cargo run -p xtask -- check-deps
cargo fmt --all --check
cargo check --workspace --all-targets
cargo test --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo deny check
cargo run -p devil-cli -- evidence check --phase gui-phase6
git diff --quiet -- crates Cargo.toml Cargo.lock
```

</verify>

### BLOCKED Conditions

- Any required gate fails or cannot be run.

## Task 3: Update Planning State After Acceptance

### Read Targets

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `plans/evidence/gui-productization/phase-6-packaging-platform-accessibility.md`
- `.planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md`

### Write Targets

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md`

### Implementation Sequence

1. Only after all gates pass, update `.planning/ROADMAP.md` Phase 6 progress to complete with the executed plan count and keep later phases pending.
   > verification: `rg -q "\\| 6 \\| .*Complete" .planning/ROADMAP.md`
2. Update `.planning/STATE.md` current position to Phase 7 next, status to Phase 6 accepted, and next action to plan Phase 7.
   > verification: `rg -q "Phase 7" .planning/STATE.md`
3. Finalize `06-07-RESULT.md` with acceptance decision, changed files, command results, and residual risk.
   > verification: `rg -q "Final Verdict: PASS" .planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md`

### Edge And Error Cases

- Do not update ROADMAP or STATE if final gates fail.
- Keep exact dates concrete in the result.
- Preserve unrelated user edits in planning files.

<verify>

```powershell
rg -q "\\| 6 \\| .*Complete" .planning/ROADMAP.md
rg -q "Phase 7" .planning/STATE.md
rg -q "Final Verdict: PASS" .planning/phases/06-packaging-platform-integration-and-accessibility/06-07-RESULT.md
```

</verify>

### BLOCKED Conditions

- Acceptance evidence has not passed all final gates.
