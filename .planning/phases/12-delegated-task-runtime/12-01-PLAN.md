---
phase: 12-delegated-task-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "crates/devil-agent/src/lib.rs"
files_forbidden:
  - "crates/devil-app/src/lib.rs"
  - "crates/devil-desktop/src/lib.rs"
sequential_files: []
expected_artifacts:
  - path: "crates/devil-agent/src/lib.rs"
    provides: "Isolated sandbox orchestration and edit proposal generation inside devil-agent."
    required: true
autonomous: false
agents:
  - "engineering-senior-developer"
  - "testing-qa-verification-specialist"
requirements:
  - "R-016"
  - "R-017"
user_setup: []
verification_commands:
  - "cargo check -p devil-agent"
  - "cargo test -p devil-agent"
must_haves:
  truths:
    - "sandbox directory creation uses target/delegated-tasks/ and isolates execution."
    - "edit proposals are emitted as DTO structures instead of applying mutations."
    - "path containment checks block any file reads or writes outside the sandbox."
  artifacts:
    - path: "crates/devil-agent/src/lib.rs"
      provides: "devil-agent isolated execution implementation"
      min_lines: 200
      contains: "pub struct DelegatedTaskSandboxOrchestrator"
  key_links: []
---

<objective>
Implement the isolated sandbox environment and proposal generator inside the devil-agent crate.

Purpose: Ensures that delegated agent tasks run in strict isolation without mutating the main workspace, yielding verified proposal artifacts instead.
Output: Isolated sandbox orchestrator and proposal generator in crates/devil-agent/src/lib.rs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-delegated-task-runtime/12-CONTEXT.md

Relevant source files:
- [lib.rs](file:///C:/Users/dasbl/RustroverProjects/devil-ide/crates/devil-agent/src/lib.rs)
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: Lead developer implementing mutation-safety boundary and QA verifying isolation checks.
Task: Implement R-016 and R-017.
Scope:
- Read targets: crates/devil-agent/src/lib.rs, crates/devil-protocol/src/lib.rs.
- Write targets: crates/devil-agent/src/lib.rs.
- Forbidden targets: crates/devil-app/src/lib.rs.
Allowed tools/actions:
- Read/Edit on crates/devil-agent/src/lib.rs.
- Cargo test on devil-agent.
Forbidden actions:
- Writing or modifying files outside devil-agent crate.
- Storing task state globally or bypassing the audit logging.
Implementation sequence:
1. Define DelegatedTaskSandboxOrchestrator to initialize and cleanup git worktrees under target/delegated-tasks/.
2. Implement path containment validator that checks if any requested path escapes the sandbox directory (returning errors on path traversal).
3. Implement DelegatedTaskProposalGenerator which translates local sandbox changes into AssistedAiEditProposalOutput.
Required interfaces/content structure:
- pub struct DelegatedTaskSandboxOrchestrator { sandbox_path: std::path::PathBuf }
- pub fn validate_containment(base: &std::path::Path, path: &std::path::Path) -> Result<(), AgentError>
Edge/error cases:
- Directory already exists (cleanup or auto-increment run id).
- Out of space or missing git binary (fail gracefully with clean AgentError).
- Path traversal escapes target directory (fail-closed, returning path violation error).
Verification criteria:
- All unit tests for sandbox creation and containment validate correctly.
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- Git command-line utility is missing or exits with non-zero code during worktree setup.
- The path containment utility fails to block relative traversal patterns (`../`).
- The devil-protocol DTO signatures differ from assumptions.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and any RESULT/SUMMARY artifact for this plan.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`.
4. Re-run completed task verification commands before continuing.
5. Continue only from the next unverified task.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Sandbox Orchestrator with Fallback</name>
  <files>
    - "crates/devil-agent/src/lib.rs"
  </files>
  <action>
    Read crates/devil-agent/src/lib.rs. Implement DelegatedTaskSandboxOrchestrator which spawns git command-line processes (using std::process::Command) to check out a new worktree under target/delegated-tasks/task-{run_id} derived from the current repository commit.
    Critique refinement: Implement a robust fallback to standard directory sandboxing (deep directory copying) if the git command fails, is missing, or if the repository is locked/bare. Include a cleanup method to prune the worktree or directory upon completion.
    Ensure standard library path parsing is used to resolve and validate absolute paths.

    > verification: cargo check -p devil-agent
  </action>
  <verify>
    cargo check -p devil-agent
  </verify>
  <done>DelegatedTaskSandboxOrchestrator successfully checked out and cleaned up temporary git worktrees or fallback sandbox directories.</done>
</task>

<task type="auto">
  <name>Task 2: Path Containment and Robust Proposal Generator</name>
  <files>
    - "crates/devil-agent/src/lib.rs"
  </files>
  <action>
    Add validation method in crates/devil-agent/src/lib.rs ensuring all read/write paths resolve strictly under the orchestrated sandbox directory.
    Critique refinement: Handle virtual or not-yet-created files safely by validating relative paths programmatically before calling canonicalize (since std::path::Path::canonicalize requires the path to exist on disk).
    Implement proposal generator which compares the sandbox directory state with the parent HEAD checkout, matches changed files, limits diffing to text files under 5MB to prevent out-of-memory, and builds AssistedAiEditProposalOutput structures populated with the modified line diffs.

    > verification: cargo check -p devil-agent
  </action>
  <verify>
    cargo check -p devil-agent
  </verify>
  <done>Path containment validation and DTO proposal generation are robustly implemented and secure.</done>
</task>

<task type="auto">
  <name>Task 3: Unit and Integration Test Suite</name>
  <files>
    - "crates/devil-agent/src/lib.rs"
  </files>
  <action>
    Add unit and integration tests inside crates/devil-agent/src/lib.rs (in mod tests) proving:
    1. Git worktree sandboxes are created and cleaned up.
    2. Path traversal attempts outside the sandbox are detected and blocked.
    3. Modified sandbox files correctly produce AssistedAiEditProposalOutput diffs.
    Run the full suite to verify.

    > verification: cargo test -p devil-agent
  </action>
  <verify>
    cargo test -p devil-agent
  </verify>
  <done>Test suite created and all tests pass cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Sandbox worktree creation is robust and cleans up correctly.
- [ ] Traversal attacks are safely blocked at the library boundary.
- [ ] Edit proposals emit syntactically valid line diffs.
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- `cargo check -p devil-agent` succeeds without warning or clippy diagnostics.
- `cargo test -p devil-agent` runs and passes all sandbox, containment, and proposal generation tests.
</success_criteria>

<output>
After completion, create `.planning/phases/12-delegated-task-runtime/12-01-SUMMARY.md`
</output>
