---
phase: 12-delegated-task-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - "crates/devil-app/src/lib.rs"
files_forbidden:
  - "crates/devil-desktop/src/lib.rs"
sequential_files: []
expected_artifacts:
  - path: "crates/devil-app/src/lib.rs"
    provides: "Composition and fail-closed security gating of the delegated task runtime."
    required: true
autonomous: false
agents:
  - "engineering-senior-developer"
  - "engineering-security-engineer"
requirements:
  - "R-018"
user_setup: []
verification_commands:
  - "cargo check -p devil-app"
  - "cargo test -p devil-app"
must_haves:
  truths:
    - "gated composition enforces DelegatedTaskRuntimeActivationState transitions."
    - "command execution requests are filtered against the security policy boundary."
    - "event sequences, correlation ids, and causality ids are fully validated."
  artifacts:
    - path: "crates/devil-app/src/lib.rs"
      provides: "gated runtime composition"
      min_lines: 5000
  key_links: []
---

<objective>
Integrate the isolated sandbox orchestrator and proposal generator in devil-app, and enforce fail-closed security policy gates and lifecycle state transitions.

Purpose: Ensures that all task executions follow proper lifecycle state transitions, are validated against security policy boundaries, and respect event causality.
Output: Integrated runtime and policy checks in crates/devil-app/src/lib.rs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-delegated-task-runtime/12-CONTEXT.md
@.planning/phases/12-delegated-task-runtime/12-01-SUMMARY.md

Relevant source files:
- [lib.rs](file:///C:/Users/dasbl/RustroverProjects/devil-ide/crates/devil-app/src/lib.rs)
- [lib.rs](file:///C:/Users/dasbl/RustroverProjects/devil-ide/crates/devil-agent/src/lib.rs)
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: Lead developer composing the runtime with security engineer gating command and path access.
Task: Implement R-018.
Scope:
- Read targets: crates/devil-app/src/lib.rs, crates/devil-agent/src/lib.rs.
- Write targets: crates/devil-app/src/lib.rs.
- Forbidden targets: crates/devil-desktop/src/lib.rs.
Allowed tools/actions:
- Read/Edit on crates/devil-app/src/lib.rs.
- Cargo test on devil-app.
Forbidden actions:
- Bypassing the AppComposition proposal ledger or modifying disk directly during delegated task execution.
- Allowing unverified or unmapped state transitions.
Implementation sequence:
1. Compose DelegatedTaskSandboxOrchestrator within AppComposition.
2. Implement DelegatedTaskRuntimeActivationState checks in AppComposition.
3. Validate that event sequences carry non-zero CorrelationId and CausalityId.
Required interfaces/content structure:
- pub fn run_delegated_task(&mut self, plan_contract: &DelegatedTaskPlanContract) -> Result<(), AppError>
- State transition validator enforcing legal transition gates.
Edge/error cases:
- Invalid or zero correlation/causality identifiers (fail-closed immediately).
- Sandbox directory setup failure (block transition and log event).
- Task execution violates allowed command classes (cancel task execution, set state to failed).
Verification criteria:
- App composition unit tests execute successfully.
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- AppComposition signatures or structs lack fields to attach runtime orchestrators.
- Event sequence or correlation validators are too loose and accept nil/zero values.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and any RESULT/SUMMARY artifact for this plan.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`.
4. Re-run completed task verification commands before continuing.
5. Continue only from the next unverified task.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Runtime State Transition Composition</name>
  <files>
    - "crates/devil-app/src/lib.rs"
  </files>
  <action>
    Read crates/devil-app/src/lib.rs. Integrate the AgentRuntime lifecycle within AppComposition. Implement methods to progress the state from Observing to Planning, Proposing, and Verifying. Verify that illegal transitions (e.g. going from Proposing directly to Completed without approval) are blocked and raise AppError.

    > verification: cargo check -p devil-app
  </action>
  <verify>
    cargo check -p devil-app
  </verify>
  <done>Lifecycle state transition composition is completed and fully validated.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-Closed Security Gating</name>
  <files>
    - "crates/devil-app/src/lib.rs"
  </files>
  <action>
    Implement security gates in AppComposition. Check that every command executed by the delegated task matches the allowed DelegatedTaskOperationClass set in the plan contract. Check that paths are strictly restricted to the sandbox directory, and reject any commands with zero correlation, nil causality, or out-of-sequence events.

    > verification: cargo check -p devil-app
  </action>
  <verify>
    cargo check -p devil-app
  </verify>
  <done>Security policy gates for command classes and identifiers are fully active and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 3: Seed Proposal Ledger Integration</name>
  <files>
    - "crates/devil-app/src/lib.rs"
  </files>
  <action>
    Update seed_delegated_task_plan_contracts to associate generated AssistedAiEditProposalOutput diffs with the corresponding DelegatedTaskPlanContract. Add unit tests in devil-app verifying that executing a plan contract initializes the worktree, validates the commands, registers the proposals, and cleans up the sandbox on finish.

    > verification: cargo test -p devil-app
  </action>
  <verify>
    cargo test -p devil-app
  </verify>
  <done>Delegated task seeding and proposal ledger integration are complete and tested.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Transition lifecycle states adhere strictly to security gates.
- [ ] Any invalid identifiers (zero/nil) result in immediate fail-closed state.
- [ ] Generated edit proposals are saved cleanly to the application ledger.
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- `cargo check -p devil-app` builds cleanly.
- `cargo test -p devil-app` passes all gated composition and state machine integration tests.
</success_criteria>

<output>
After completion, create `.planning/phases/12-delegated-task-runtime/12-02-SUMMARY.md`
</output>
