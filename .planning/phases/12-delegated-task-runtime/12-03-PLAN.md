---
phase: 12-delegated-task-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - "12-01"
  - "12-02"
files_modified:
  - "crates/devil-desktop/src/bridge.rs"
  - "crates/devil-desktop/src/workflow.rs"
files_forbidden:
  - "crates/devil-app/src/lib.rs"
  - "crates/devil-agent/src/lib.rs"
sequential_files: []
expected_artifacts:
  - path: "crates/devil-desktop/src/bridge.rs"
    provides: "Desktop action bridging for inspecting delegated task plan contracts."
    required: true
  - path: "crates/devil-desktop/src/workflow.rs"
    provides: "Workflow state transitions for reviewing and approving task proposals."
    required: true
autonomous: false
agents:
  - "testing-qa-verification-specialist"
  - "engineering-frontend-developer"
requirements:
  - "R-019"
user_setup: []
verification_commands:
  - "cargo check -p devil-desktop"
  - "cargo test -p devil-desktop"
must_haves:
  truths:
    - "desktop actions bridge and workflow route proposal previews correctly."
    - "approval and review states update the UI projections without bypassing the app."
    - "full end-to-end verification harness runs task executions cleanly."
  artifacts:
    - path: "crates/devil-desktop/src/bridge.rs"
      provides: "desktop bridge integration"
      contains: "InspectDelegatedTaskPlan"
  key_links: []
---

<objective>
Integrate delegated task review actions and state routing in devil-desktop and devil-ui, and build a full end-to-end integration verification harness and smoke test.

Purpose: Completes the feedback loop by exposing the executed task proposals to the desktop user interface, and verifies the full system with a complete end-to-end test suite.
Output: Desktop actions and end-to-end integration tests under crates/devil-desktop/.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-delegated-task-runtime/12-CONTEXT.md
@.planning/phases/12-delegated-task-runtime/12-02-SUMMARY.md

Relevant source files:
- [bridge.rs](file:///C:/Users/dasbl/RustroverProjects/devil-ide/crates/devil-desktop/src/bridge.rs)
- [workflow.rs](file:///C:/Users/dasbl/RustroverProjects/devil-ide/crates/devil-desktop/src/workflow.rs)
- [delegated_task_command_center.rs](file:///C:/Users/dasbl/RustroverProjects/devil-ide/crates/devil-desktop/tests/delegated_task_command_center.rs)
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: QA specialist building the integration test harness, alongside UI developer wiring actions.
Task: Implement R-019.
Scope:
- Read targets: crates/devil-desktop/src/bridge.rs, crates/devil-desktop/src/workflow.rs, crates/devil-desktop/tests/delegated_task_command_center.rs.
- Write targets: crates/devil-desktop/src/bridge.rs, crates/devil-desktop/src/workflow.rs.
- Forbidden targets: crates/devil-app/src/lib.rs, crates/devil-agent/src/lib.rs.
Allowed tools/actions:
- Read/Edit on crates/devil-desktop/src/bridge.rs and workflow.rs.
- Cargo test on devil-desktop.
Forbidden actions:
- Direct mutation of files outside the desktop crate during tests.
- Bypassing the desktop bridge to execute workspace writes directly.
Implementation sequence:
1. Wire up bridge routing for DesktopAction::InspectDelegatedTaskPlan in bridge.rs.
2. Update workflow.rs to handle DesktopAppRequest::InspectDelegatedTaskPlan and transition the workflow state.
3. Build and execute end-to-end integration tests under crates/devil-desktop/tests/.
Required interfaces/content structure:
- Integration of InspectDelegatedTaskPlan within desktop bridge and workflow logic.
- Tests verifying that proposal details are mapped to desktop projection snapshots.
Edge/error cases:
- Plan ID does not exist in the contract seeding (return UnknownDelegatedTaskPlan).
- Proposal details contain unmapped target summaries (render placeholders safely).
- Execution sequence is out of order (raise validation error cleanly).
Verification criteria:
- All desktop tests build and execute successfully.
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- Desktop bridge or workflow structs cannot access the contract ledgers seeded by devil-app.
- The integration test harness relies on network providers or vector/embeddings endpoints.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and any RESULT/SUMMARY artifact for this plan.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`.
4. Re-run completed task verification commands before continuing.
5. Continue only from the next unverified task.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Desktop Bridge Action Integration</name>
  <files>
    - "crates/devil-desktop/src/bridge.rs"
  </files>
  <action>
    Read crates/devil-desktop/src/bridge.rs. Complete the routing logic for DesktopAction::InspectDelegatedTaskPlan. Ensure that plan inspection routes to the correct application requests, and map any UnknownDelegatedTaskPlan errors back to the desktop presentation layer.

    > verification: cargo check -p devil-desktop
  </action>
  <verify>
    cargo check -p devil-desktop
  </verify>
  <done>Desktop bridge actions for delegated task inspection are fully integrated and compile.</done>
</task>

<task type="auto">
  <name>Task 2: Workflow State Transition Wiring</name>
  <files>
    - "crates/devil-desktop/src/workflow.rs"
  </files>
  <action>
    Read crates/devil-desktop/src/workflow.rs. Wire up DesktopAppRequest::InspectDelegatedTaskPlan to advance workflow states. Return DesktopWorkflowOutcome::DelegatedTaskReviewed with PlanInspected, ProposalPreviewOpened, and ProposalDetailsOpened states, verifying the visual projection models are accurately populated.

    > verification: cargo check -p devil-desktop
  </action>
  <verify>
    cargo check -p devil-desktop
  </verify>
  <done>Workflow state transitions and visual projection models are correctly wired.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-End Integration and Headless Diagnostics Harness</name>
  <files>
    - "crates/devil-desktop/tests/delegated_task_command_center.rs"
  </files>
  <action>
    Inspect crates/devil-desktop/tests/delegated_task_command_center.rs. Write and execute end-to-end integration tests that seed a delegated task, orchestrate its isolated execution, verify security and path boundaries, emit edit proposals, and project the outcomes onto the desktop bridge workflow.
    Critique refinement: Ensure all desktop integration tests run strictly headlessly and do not attempt to open real GPU/display windows, making them fully compatible with bare-metal or virtualized CI environments. Prove everything runs successfully.

    > verification: cargo test -p devil-desktop --test delegated_task_command_center
  </action>
  <verify>
    cargo test -p devil-desktop --test delegated_task_command_center
  </verify>
  <done>End-to-end integration tests run strictly headlessly and pass cleanly, validating Phase 12 completion.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] UI action routing for inspecting plans maps correctly onto desktop workflow.
- [ ] Integration tests prove full isolated sandbox checking, proposal diffing, and audit logging.
- [ ] Diagnostics smoke tests execute perfectly and exit with code 0.
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- `cargo check -p devil-desktop` builds cleanly without warnings.
- `cargo test -p devil-desktop` executes and passes all tests including delegated_task_command_center.
</success_criteria>

<output>
After completion, create `.planning/phases/12-delegated-task-runtime/12-03-SUMMARY.md`
</output>
